legacy-peer-deps=true

# Save exact version, instead implicit latest semver range. This avoid potentially compromised or breaking dependencies
save-exact=true

# Minimum release age in days
# https://docs.npmjs.com/cli/v11/using-npm/config#min-release-age
min-release-age=21