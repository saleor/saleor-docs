---
id: checkout-add-promo-code
title: checkoutAddPromoCode
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Adds a gift card or a voucher to a checkout.

```graphql
checkoutAddPromoCode(
  checkoutId: ID
  id: ID
  promoCode: String!
  token: UUID
): CheckoutAddPromoCode
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>checkoutAddPromoCode.<b>checkoutId</b></code>](#)<Bullet />[`ID`](../../../api-storefront/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The ID of the checkout.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

#### [<code style={{ fontWeight: 'normal' }}>checkoutAddPromoCode.<b>id</b></code>](#)<Bullet />[`ID`](../../../api-storefront/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The checkout's ID.
>
> <Badge text="Added in Saleor 3.4" class="secondary margin-bottom--sm" />

#### [<code style={{ fontWeight: 'normal' }}>checkoutAddPromoCode.<b>promoCode</b></code>](#)<Bullet />[`String!`](../../../api-storefront/miscellaneous/scalars/string) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/>

> Gift card code or voucher code.

#### [<code style={{ fontWeight: 'normal' }}>checkoutAddPromoCode.<b>token</b></code>](#)<Bullet />[`UUID`](../../../api-storefront/miscellaneous/scalars/uuid) <Badge class="badge badge--secondary" text="scalar"/>

> Checkout token.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

### Type

#### [`CheckoutAddPromoCode`](../../../api-storefront/checkout/objects/checkout-add-promo-code) <Badge class="badge badge--secondary" text="object"/>

> Adds a gift card or a voucher to a checkout.
