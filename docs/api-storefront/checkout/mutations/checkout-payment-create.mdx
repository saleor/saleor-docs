---
id: checkout-payment-create
title: checkoutPaymentCreate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Create a new payment for given checkout.

```graphql
checkoutPaymentCreate(
  checkoutId: ID
  id: ID
  input: PaymentInput!
  token: UUID
): CheckoutPaymentCreate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>checkoutPaymentCreate.<b>checkoutId</b></code>](#)<Bullet />[`ID`](../../../api-storefront/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The ID of the checkout.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

#### [<code style={{ fontWeight: 'normal' }}>checkoutPaymentCreate.<b>id</b></code>](#)<Bullet />[`ID`](../../../api-storefront/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The checkout's ID.
>
> <Badge text="Added in Saleor 3.4" class="secondary margin-bottom--sm" />

#### [<code style={{ fontWeight: 'normal' }}>checkoutPaymentCreate.<b>input</b></code>](#)<Bullet />[`PaymentInput!`](../../../api-storefront/payments/inputs/payment-input) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/>

> Data required to create a new payment.

#### [<code style={{ fontWeight: 'normal' }}>checkoutPaymentCreate.<b>token</b></code>](#)<Bullet />[`UUID`](../../../api-storefront/miscellaneous/scalars/uuid) <Badge class="badge badge--secondary" text="scalar"/>

> Checkout token.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

### Type

#### [`CheckoutPaymentCreate`](../../../api-storefront/checkout/objects/checkout-payment-create) <Badge class="badge badge--secondary" text="object"/>

> Create a new payment for given checkout.
