---
id: external-logout
title: ExternalLogout
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Logout user by custom plugin.

```graphql
type ExternalLogout {
  logoutData: JSONString
  accountErrors: [AccountError!]!
  errors: [AccountError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ExternalLogout.<b>logoutData</b></code>](#)<Bullet />[`JSONString`](../../../api-storefront/miscellaneous/scalars/jsonstring) <Badge class="secondary" text="scalar"/>

> The data returned by authentication plugin.

#### [<code style={{ fontWeight: 'normal' }}>ExternalLogout.<b>accountErrors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-storefront/users/objects/account-error) <Badge class="secondary" text="deprecated"/> <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

> <Badge
>   class="warning"
>   text="DEPRECATED: This field will be removed in Saleor 4.0. Use `errors` field instead."
> />

#### [<code style={{ fontWeight: 'normal' }}>ExternalLogout.<b>errors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-storefront/users/objects/account-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

>

### Returned by

[`externalLogout`](../../../api-storefront/authentication/mutations/external-logout) <Badge class="secondary" text="mutation"/>
