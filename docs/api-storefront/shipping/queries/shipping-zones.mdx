---
id: shipping-zones
title: shippingZones
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

List of the shop's shipping zones.

Requires one of the following permissions: MANAGE_SHIPPING.

```graphql
shippingZones(
  filter: ShippingZoneFilterInput
  channel: String
  before: String
  after: String
  first: Int
  last: Int
): ShippingZoneCountableConnection
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>filter</b></code>](#)<Bullet />[`ShippingZoneFilterInput`](../../../storefront/shipping/inputs/shipping-zone-filter-input) <Badge class="secondary" text="input"/>

> Filtering options for shipping zones.

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>channel</b></code>](#)<Bullet />[`String`](../../../storefront/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> Slug of a channel for which the data should be returned.

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>before</b></code>](#)<Bullet />[`String`](../../../storefront/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> Return the elements in the list that come before the specified cursor.

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>after</b></code>](#)<Bullet />[`String`](../../../storefront/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> Return the elements in the list that come after the specified cursor.

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>first</b></code>](#)<Bullet />[`Int`](../../../storefront/miscellaneous/scalars/int) <Badge class="secondary" text="scalar"/>

> Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code style={{ fontWeight: 'normal' }}>shippingZones.<b>last</b></code>](#)<Bullet />[`Int`](../../../storefront/miscellaneous/scalars/int) <Badge class="secondary" text="scalar"/>

> Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

### Type

#### [`ShippingZoneCountableConnection`](../../../storefront/miscellaneous/objects/shipping-zone-countable-connection) <Badge class="secondary" text="object"/>

>
