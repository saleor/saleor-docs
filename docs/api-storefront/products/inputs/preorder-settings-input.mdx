---
id: preorder-settings-input
title: PreorderSettingsInput
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
input PreorderSettingsInput {
  globalThreshold: Int
  endDate: DateTime
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>PreorderSettingsInput.<b>globalThreshold</b></code>](#)<Bullet />[`Int`](../../../api-storefront/miscellaneous/scalars/int) <Badge class="secondary" text="scalar"/>

> The global threshold for preorder variant.

#### [<code style={{ fontWeight: 'normal' }}>PreorderSettingsInput.<b>endDate</b></code>](#)<Bullet />[`DateTime`](../../../api-storefront/miscellaneous/scalars/date-time) <Badge class="secondary" text="scalar"/>

> The end date for preorder.

### Member of

[`ProductVariantBulkCreateInput`](../../../api-storefront/products/inputs/product-variant-bulk-create-input) <Badge class="secondary" text="input"/><Bullet />[`ProductVariantBulkUpdateInput`](../../../api-storefront/products/inputs/product-variant-bulk-update-input) <Badge class="secondary" text="input"/><Bullet />[`ProductVariantCreateInput`](../../../api-storefront/products/inputs/product-variant-create-input) <Badge class="secondary" text="input"/><Bullet />[`ProductVariantInput`](../../../api-storefront/products/inputs/product-variant-input) <Badge class="secondary" text="input"/>
