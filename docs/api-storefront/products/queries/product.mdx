---
id: product
title: product
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Look up a product by ID.

<Permissions
  permissions={"MANAGE_ORDERS,MANAGE_DISCOUNTS,MANAGE_PRODUCTS"}
  text={
    "Requires one of the following permissions to include the unpublished items"
  }
/>

```graphql
product(
  id: ID
  slug: String
  externalReference: String
  channel: String
): Product
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>product.<b>id</b></code>](#)<Bullet />[`ID`](../../../api-storefront/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> ID of the product.

#### [<code style={{ fontWeight: 'normal' }}>product.<b>slug</b></code>](#)<Bullet />[`String`](../../../api-storefront/miscellaneous/scalars/string) <Badge class="badge badge--secondary" text="scalar"/>

> Slug of the product.

#### [<code style={{ fontWeight: 'normal' }}>product.<b>externalReference</b></code>](#)<Bullet />[`String`](../../../api-storefront/miscellaneous/scalars/string) <Badge class="badge badge--secondary" text="scalar"/>

> External ID of the product.
>
> <Badge text="Added in Saleor 3.10" class="secondary margin-bottom--sm" />

#### [<code style={{ fontWeight: 'normal' }}>product.<b>channel</b></code>](#)<Bullet />[`String`](../../../api-storefront/miscellaneous/scalars/string) <Badge class="badge badge--secondary" text="scalar"/>

> Slug of a channel for which the data should be returned.

### Type

#### [`Product`](../../../api-storefront/products/objects/product) <Badge class="badge badge--secondary" text="object"/>

> Represents an individual item for sale in the storefront.

import Permissions from "@site/components/Permissions";
