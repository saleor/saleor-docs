---
id: tax-calculation-strategy
title: TaxCalculationStrategy
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
enum TaxCalculationStrategy {
  FLAT_RATES
  TAX_APP
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>TaxCalculationStrategy.<b>FLAT_RATES</b></code>](#)

>

#### [<code style={{ fontWeight: 'normal' }}>TaxCalculationStrategy.<b>TAX_APP</b></code>](#)

>

### Member of

[`TaxConfiguration`](../../../api-storefront/taxes/objects/tax-configuration) <Badge class="secondary" text="object"/><Bullet />[`TaxConfigurationPerCountry`](../../../api-storefront/taxes/objects/tax-configuration-per-country) <Badge class="secondary" text="object"/><Bullet />[`TaxConfigurationPerCountryInput`](../../../api-storefront/taxes/inputs/tax-configuration-per-country-input) <Badge class="secondary" text="input"/><Bullet />[`TaxConfigurationUpdateInput`](../../../api-storefront/taxes/inputs/tax-configuration-update-input) <Badge class="secondary" text="input"/>
