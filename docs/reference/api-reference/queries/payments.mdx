---
id: payments
title: payments
---

List of payments.

Requires one of the following permissions: MANAGE_ORDERS.

```graphql
payments(
  filter: PaymentFilterInput
  before: String
  after: String
  first: Int
  last: Int
): PaymentCountableConnection
```

### Arguments

#### `filter` ([`PaymentFilterInput`](../../../developer/api-reference/inputs/payment-filter-input))

Filtering options for payments.

#### `before` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](../../../developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](../../../developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`PaymentCountableConnection`](../../../developer/api-reference/objects/payment-countable-connection)
