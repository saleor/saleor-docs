---
api_reference: true
id: external-authentication-url
title: externalAuthenticationUrl
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Prepare external authentication URL for user by custom plugin.

```graphql
externalAuthenticationUrl(
  input: JSONString!
  pluginId: String!
): ExternalAuthenticationUrl
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ExternalAuthenticationUrl {
  authenticationData: JSONString
  accountErrors: [AccountError!]! @deprecated
  errors: [AccountError!]!
}
```

</Details>

### Arguments

#### [<code>input</code>](#input)<Bullet />[<code>JSONString!</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx) \{#input\}

The data required by plugin to create external authentication url.

#### [<code>pluginId</code>](#plugin-id)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#plugin-id\}

The ID of the authentication plugin.

### Type

#### [<code>ExternalAuthenticationUrl</code>](/api-reference/authentication/objects/external-authentication-url.mdx)

Prepare external authentication URL for user by custom plugin.
