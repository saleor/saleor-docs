---
api_reference: true
id: event-delivery-attempt
title: EventDeliveryAttempt
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Event delivery attempts.

```graphql
type EventDeliveryAttempt implements Node {
  id: ID!
  createdAt: DateTime!
  taskId: String
  duration: Float
  response: String
  responseHeaders: String
  responseStatusCode: Int
  requestHeaders: String
  status: EventDeliveryStatusEnum!
}
```

### Fields

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

The ID of Event Delivery Attempt.

#### [<code>createdAt</code>](#created-at)<Bullet />[<code>DateTime!</code>](/api-reference/miscellaneous/scalars/date-time.mdx) \{#created-at\}

Event delivery creation date and time.

#### [<code>taskId</code>](#task-id)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#task-id\}

Task id for delivery attempt.

#### [<code>duration</code>](#duration)<Bullet />[<code>Float</code>](/api-reference/miscellaneous/scalars/float.mdx) \{#duration\}

Delivery attempt duration.

#### [<code>response</code>](#response)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#response\}

Delivery attempt response content.

#### [<code>responseHeaders</code>](#response-headers)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#response-headers\}

Response headers for delivery attempt.

#### [<code>responseStatusCode</code>](#response-status-code)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#response-status-code\}

Delivery attempt response status code.

#### [<code>requestHeaders</code>](#request-headers)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#request-headers\}

Request headers for delivery attempt.

#### [<code>status</code>](#status)<Bullet />[<code>EventDeliveryStatusEnum!</code>](/api-reference/webhooks/enums/event-delivery-status-enum.mdx) \{#status\}

Event delivery status.

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`EventDeliveryAttemptCountableEdge`](/api-reference/miscellaneous/objects/event-delivery-attempt-countable-edge.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
