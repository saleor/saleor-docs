---
id: webhook-delete
title: WebhookDelete
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Delete a webhook. Before the deletion, the webhook is deactivated to pause any deliveries that are already scheduled. The deletion might fail if delivery is in progress. In such a case, the webhook is not deleted but remains deactivated.

Requires one of the following permissions: MANAGE_APPS, AUTHENTICATED_APP.

```graphql
type WebhookDelete {
  webhookErrors: [WebhookError!]!
  errors: [WebhookError!]!
  webhook: Webhook
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>WebhookDelete.<b>webhookErrors</b></code>](#)<Bullet />[`[WebhookError!]!`](../../../api-reference/webhooks/objects/webhook-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/> <Badge class="secondary" text="webhooks"/>

>

#### [<code style={{ fontWeight: 'normal' }}>WebhookDelete.<b>errors</b></code>](#)<Bullet />[`[WebhookError!]!`](../../../api-reference/webhooks/objects/webhook-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/> <Badge class="secondary" text="webhooks"/>

>

#### [<code style={{ fontWeight: 'normal' }}>WebhookDelete.<b>webhook</b></code>](#)<Bullet />[`Webhook`](../../../api-reference/webhooks/objects/webhook) <Badge class="secondary" text="object"/> <Badge class="secondary" text="webhooks"/>

>

### Returned by

[`webhookDelete`](../../../api-reference/webhooks/mutations/webhook-delete) <Badge class="secondary" text="mutation"/>
