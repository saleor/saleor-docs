---
api_reference: true
id: webhook
title: Webhook
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Webhook.

```graphql
type Webhook implements Node {
  id: ID!
  name: String
  events: [WebhookEvent!]! @deprecated
  syncEvents: [WebhookEventSync!]!
  asyncEvents: [WebhookEventAsync!]!
  app: App!
  eventDeliveries(
    sortBy: EventDeliverySortingInput
    filter: EventDeliveryFilterInput
    before: String
    after: String
    first: Int
    last: Int
  ): EventDeliveryCountableConnection
  targetUrl: String!
  isActive: Boolean!
  secretKey: String @deprecated
  subscriptionQuery: String
  customHeaders: JSONString
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of webhook.

#### [<code>name</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of webhook.

#### [<code>syncEvents</code>](#)<Bullet />[<code>[WebhookEventSync!]!</code>](/api-reference/webhooks/objects/webhook-event-sync.mdx)

List of synchronous webhook events.

#### [<code>asyncEvents</code>](#)<Bullet />[<code>[WebhookEventAsync!]!</code>](/api-reference/webhooks/objects/webhook-event-async.mdx)

List of asynchronous webhook events.

#### [<code>app</code>](#)<Bullet />[<code>App!</code>](/api-reference/apps/objects/app.mdx)

The app associated with Webhook.

#### [<code>eventDeliveries</code>](#)<Bullet />[<code>EventDeliveryCountableConnection</code>](/api-reference/miscellaneous/objects/event-delivery-countable-connection.mdx)

Event deliveries.

##### [<code>sortBy</code>](#)<Bullet />[<code>EventDeliverySortingInput</code>](/api-reference/webhooks/inputs/event-delivery-sorting-input.mdx)

Event delivery sorter.

##### [<code>filter</code>](#)<Bullet />[<code>EventDeliveryFilterInput</code>](/api-reference/miscellaneous/inputs/event-delivery-filter-input.mdx)

Event delivery filter options.

##### [<code>before</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come before the specified cursor.

##### [<code>after</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come after the specified cursor.

##### [<code>first</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

##### [<code>last</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>targetUrl</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Target URL for webhook.

#### [<code>isActive</code>](#)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Informs if webhook is activated.

#### [<code>subscriptionQuery</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Used to define payloads for specific events.

#### [<code>customHeaders</code>](#)<Bullet />[<code>JSONString</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx)

Custom headers, which will be added to HTTP request.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>events</code>](#)<Bullet />[<code>[WebhookEvent!]!</code>](/api-reference/webhooks/objects/webhook-event.mdx)

:::warning[DEPRECATED]

Use `asyncEvents` or `syncEvents` instead.

:::

List of webhook events.

#### [<code>secretKey</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

:::warning[DEPRECATED]

As of Saleor 3.5, webhook payloads default to signing using a verifiable JWS.

:::

Used to create a hash signature for each payload.

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Returned By

[`webhook`](/api-reference/webhooks/queries/webhook.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>

### Member Of

[`App`](/api-reference/apps/objects/app.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`WebhookCreate`](/api-reference/webhooks/objects/webhook-create.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`WebhookDelete`](/api-reference/webhooks/objects/webhook-delete.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`WebhookUpdate`](/api-reference/webhooks/objects/webhook-update.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
