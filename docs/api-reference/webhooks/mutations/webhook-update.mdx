---
api_reference: true
id: webhook-update
title: webhookUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates a webhook subscription.

<Permissions
  permissions={"MANAGE_APPS,AUTHENTICATED_APP"}
  text={"Requires one of the following permissions"}
/>

```graphql
webhookUpdate(
  id: ID
  identifier: String
  input: WebhookUpdateInput!
): WebhookUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type WebhookUpdate {
  webhookErrors: [WebhookError!]! @deprecated
  errors: [WebhookError!]!
  webhook: Webhook
}
```

</Details>

### Arguments

#### [<code>id</code>](#id)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

ID of a webhook to update. Cannot be used together with `identifier`.

#### [<code>identifier</code>](#identifier)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#identifier\}

App-provided identifier of a webhook to update. Identifiers are unique per app only, so this argument is available exclusively to an app referencing its own webhook; staff users must use `id`. Cannot be used together with `id`.

<Badge
  text="Added in Saleor 3.23"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>input</code>](#input)<Bullet />[<code>WebhookUpdateInput!</code>](/api-reference/webhooks/inputs/webhook-update-input.mdx) \{#input\}

Fields required to update a webhook.

### Type

#### [<code>WebhookUpdate</code>](/api-reference/webhooks/objects/webhook-update.mdx)

Updates a webhook subscription.

<Permissions
  permissions={"MANAGE_APPS,AUTHENTICATED_APP"}
  text={"Requires one of the following permissions"}
/>
