---
api_reference: true
id: webhook-update-input
title: WebhookUpdateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input WebhookUpdateInput {
  name: String
  targetUrl: String
  events: [WebhookEventTypeEnum!] @deprecated
  asyncEvents: [WebhookEventTypeAsyncEnum!]
  syncEvents: [WebhookEventTypeSyncEnum!]
  app: ID
  isActive: Boolean
  secretKey: String @deprecated
  query: String
  customHeaders: JSONString
}
```

### Fields

#### [<code>name</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The new name of the webhook.

#### [<code>targetUrl</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The url to receive the payload.

#### [<code>asyncEvents</code>](#)<Bullet />[<code>[WebhookEventTypeAsyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-async-enum.mdx)

The asynchronous events that webhook wants to subscribe.

#### [<code>syncEvents</code>](#)<Bullet />[<code>[WebhookEventTypeSyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-sync-enum.mdx)

The synchronous events that webhook wants to subscribe.

#### [<code>app</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the app to which webhook belongs.

#### [<code>isActive</code>](#)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Determine if webhook will be set active or not.

#### [<code>query</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Subscription query used to define a webhook payload.

#### [<code>customHeaders</code>](#)<Bullet />[<code>JSONString</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx)

Custom headers, which will be added to HTTP request. There is a limitation of 5 headers per webhook and 998 characters per header.Only `X-*`, `Authorization*`, and `BrokerProperties` keys are allowed.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>events</code>](#)<Bullet />[<code>[WebhookEventTypeEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-enum.mdx)

:::warning[DEPRECATED]

Use `asyncEvents` or `syncEvents` instead.

:::

The events that webhook wants to subscribe.

#### [<code>secretKey</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

:::warning[DEPRECATED]

As of Saleor 3.5, webhook payloads default to signing using a verifiable JWS.

:::

Use to create a hash signature with each payload.

</Details>

### Member Of

[`webhookUpdate`](/api-reference/webhooks/mutations/webhook-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
