---
api_reference: true
id: webhook-create-input
title: WebhookCreateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input WebhookCreateInput {
  name: String
  targetUrl: String
  events: [WebhookEventTypeEnum!] @deprecated
  asyncEvents: [WebhookEventTypeAsyncEnum!]
  syncEvents: [WebhookEventTypeSyncEnum!]
  app: ID
  isActive: Boolean
  secretKey: String @deprecated
  query: String
  customHeaders: JSONString
}
```

### Fields

#### [<code>name</code>](#name)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#name\}

The name of the webhook.

#### [<code>targetUrl</code>](#target-url)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#target-url\}

The url to receive the payload.

#### [<code>asyncEvents</code>](#async-events)<Bullet />[<code>[WebhookEventTypeAsyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-async-enum.mdx) \{#async-events\}

The asynchronous events that webhook wants to subscribe.

#### [<code>syncEvents</code>](#sync-events)<Bullet />[<code>[WebhookEventTypeSyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-sync-enum.mdx) \{#sync-events\}

The synchronous events that webhook wants to subscribe.

#### [<code>app</code>](#app)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#app\}

ID of the app to which webhook belongs.

#### [<code>isActive</code>](#is-active)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-active\}

Determine if webhook will be set active or not.

#### [<code>query</code>](#query)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#query\}

Subscription query used to define a webhook payload.

#### [<code>customHeaders</code>](#custom-headers)<Bullet />[<code>JSONString</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx) \{#custom-headers\}

Custom headers, which will be added to HTTP request. There is a limitation of 5 headers per webhook and 998 characters per header.Only `X-*`, `Authorization*`, and `BrokerProperties` keys are allowed.

<details class="graphql-markdown-details">
<summary>
<span class="graphql-markdown-details-label-closed">Show deprecated</span>
<span class="graphql-markdown-details-label-open">Hide deprecated</span>
</summary>

#### [<code>events</code>](#events)<Bullet />[<code>[WebhookEventTypeEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-enum.mdx) \{#events\}

:::warning[DEPRECATED]

Use `asyncEvents` or `syncEvents` instead.

:::

The events that webhook wants to subscribe.

#### [<code>secretKey</code>](#secret-key)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#secret-key\}

:::warning[DEPRECATED]

As of Saleor 3.5, webhook payloads default to signing using a verifiable JWS.

:::

The secret key used to create a hash signature with each payload.

</details>

### Member Of

[`webhookCreate`](/api-reference/webhooks/mutations/webhook-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
