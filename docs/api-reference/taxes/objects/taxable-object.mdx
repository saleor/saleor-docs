---
api_reference: true
id: taxable-object
title: TaxableObject
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Taxable object.

```graphql
type TaxableObject {
  sourceObject: TaxSourceObject!
  pricesEnteredWithTax: Boolean!
  currency: String!
  shippingPrice: Money!
  address: Address
  discounts: [TaxableObjectDiscount!]!
  lines: [TaxableObjectLine!]!
  channel: Channel!
}
```

### Fields

#### [<code>sourceObject</code>](#)<Bullet />[<code>TaxSourceObject!</code>](/api-reference/miscellaneous/unions/tax-source-object.mdx)

The source object related to this tax object.

#### [<code>pricesEnteredWithTax</code>](#)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Determines if prices contain entered tax..

#### [<code>currency</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

The currency of the object.

#### [<code>shippingPrice</code>](#)<Bullet />[<code>Money!</code>](/api-reference/miscellaneous/objects/money.mdx)

The price of shipping method, includes shipping voucher discount if applied.

#### [<code>address</code>](#)<Bullet />[<code>Address</code>](/api-reference/users/objects/address.mdx)

The address data.

#### [<code>discounts</code>](#)<Bullet />[<code>[TaxableObjectDiscount!]!</code>](/api-reference/taxes/objects/taxable-object-discount.mdx)

List of discounts.

#### [<code>lines</code>](#)<Bullet />[<code>[TaxableObjectLine!]!</code>](/api-reference/taxes/objects/taxable-object-line.mdx)

List of lines assigned to the object.

#### [<code>channel</code>](#)<Bullet />[<code>Channel!</code>](/api-reference/channels/objects/channel.mdx)

### Member Of

[`CalculateTaxes`](/api-reference/taxes/objects/calculate-taxes.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
