---
api_reference: true
id: tax-class-country-rate
title: TaxClassCountryRate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Tax rate for a country. When tax class is null, it represents the default tax rate for that country; otherwise it's a country tax rate specific to the given tax class.

```graphql
type TaxClassCountryRate {
  country: CountryDisplay!
  rate: Float!
  taxClass: TaxClass
}
```

### Fields

#### [<code>country</code>](#)<Bullet />[<code>CountryDisplay!</code>](/api-reference/miscellaneous/objects/country-display.mdx)

Country in which this tax rate applies.

#### [<code>rate</code>](#)<Bullet />[<code>Float!</code>](/api-reference/miscellaneous/scalars/float.mdx)

Tax rate value.

#### [<code>taxClass</code>](#)<Bullet />[<code>TaxClass</code>](/api-reference/taxes/objects/tax-class.mdx)

Related tax class.

### Member Of

[`TaxClass`](/api-reference/taxes/objects/tax-class.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`TaxCountryConfiguration`](/api-reference/taxes/objects/tax-country-configuration.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
