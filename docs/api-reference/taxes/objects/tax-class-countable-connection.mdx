---
api_reference: true
id: tax-class-countable-connection
title: TaxClassCountableConnection
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
type TaxClassCountableConnection {
  pageInfo: PageInfo!
  edges: [TaxClassCountableEdge!]!
  totalCount: Int
}
```

### Fields

#### [<code>pageInfo</code>](#)<Bullet />[<code>PageInfo!</code>](/api-reference/miscellaneous/objects/page-info.mdx)

Pagination data for this connection.

#### [<code>edges</code>](#)<Bullet />[<code>[TaxClassCountableEdge!]!</code>](/api-reference/taxes/objects/tax-class-countable-edge.mdx)

#### [<code>totalCount</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

A total count of items in the collection.

### Returned By

[`taxClasses`](/api-reference/taxes/queries/tax-classes.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
