---
api_reference: true
id: vat
title: VAT
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents a VAT rate for a country.

```graphql
type VAT {
  countryCode: String!
  standardRate: Float
  reducedRates: [ReducedRate!]!
}
```

### Fields

#### [<code>countryCode</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Country code.

#### [<code>standardRate</code>](#)<Bullet />[<code>Float</code>](/api-reference/miscellaneous/scalars/float.mdx)

Standard VAT rate in percent.

#### [<code>reducedRates</code>](#)<Bullet />[<code>[ReducedRate!]!</code>](/api-reference/taxes/objects/reduced-rate.mdx)

Country's VAT rate exceptions for specific types of goods.

### Member Of

[`CountryDisplay`](/api-reference/miscellaneous/objects/country-display.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
