---
api_reference: true
id: tax-class-update
title: taxClassUpdate
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

Updates a tax class.

Requires one of the following permissions: MANAGE_TAXES.

```graphql
taxClassUpdate(
  id: ID!
  input: TaxClassUpdateInput!
): TaxClassUpdate
```

### Arguments

#### [`id`](#)<Bullet />[`ID!`](/api-reference/miscellaneous/scalars/id.mdx)

ID of the tax class.

#### [`input`](#)<Bullet />[`TaxClassUpdateInput!`](/api-reference/taxes/inputs/tax-class-update-input.mdx)

Fields required to update a tax class.

### Type

#### [`TaxClassUpdate`](/api-reference/taxes/objects/tax-class-update.mdx)

Updates a tax class.

Requires one of the following permissions: MANAGE_TAXES.
