---
api_reference: true
id: shop-domain-update
title: shopDomainUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

:::warning[DEPRECATED]

Use `PUBLIC_URL` environment variable instead.

:::

Updates site domain of the shop.

<Permissions
  permissions={"MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>

```graphql
shopDomainUpdate(
  input: SiteDomainInput
): ShopDomainUpdate @deprecated
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ShopDomainUpdate {
  shop: Shop
  shopErrors: [ShopError!]! @deprecated
  errors: [ShopError!]!
}
```

</Details>

### Arguments

#### [<code>input</code>](#)<Bullet />[<code>SiteDomainInput</code>](/api-reference/miscellaneous/inputs/site-domain-input.mdx)

Fields required to update site.

### Type

#### [<code>ShopDomainUpdate</code>](/api-reference/shop/objects/shop-domain-update.mdx)

Updates site domain of the shop.

<Permissions
  permissions={"MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>
