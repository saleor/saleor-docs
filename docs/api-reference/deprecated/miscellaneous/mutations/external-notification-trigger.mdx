---
api_reference: true
id: external-notification-trigger
title: externalNotificationTrigger
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

:::warning[DEPRECATED]

No longer supported

:::

Trigger sending a notification with the notify plugin method. Serializes nodes provided as ids parameter and includes this data in the notification payload.

```graphql
externalNotificationTrigger(
  channel: String!
  input: ExternalNotificationTriggerInput!
  pluginId: String
): ExternalNotificationTrigger @deprecated
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ExternalNotificationTrigger {
  errors: [ExternalNotificationError!]!
}
```

</Details>

### Arguments

#### [<code>channel</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Channel slug. Saleor will send a notification within a provided channel. Please, make sure that necessary plugins are active.

#### [<code>input</code>](#)<Bullet />[<code>ExternalNotificationTriggerInput!</code>](/api-reference/miscellaneous/inputs/external-notification-trigger-input.mdx)

Input for External Notification Trigger.

#### [<code>pluginId</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The ID of notification plugin.

### Type

#### [<code>ExternalNotificationTrigger</code>](/api-reference/miscellaneous/objects/external-notification-trigger.mdx)

Trigger sending a notification with the notify plugin method. Serializes nodes provided as ids parameter and includes this data in the notification payload.
