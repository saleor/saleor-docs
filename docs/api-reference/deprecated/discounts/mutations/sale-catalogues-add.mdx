---
api_reference: true
id: sale-catalogues-add
title: saleCataloguesAdd
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

:::warning[DEPRECATED]

Use `promotionRuleCreate` and `promotionRuleUpdate` mutations instead.

:::

Adds products, categories, collections to a sale.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- SALE_UPDATED (async): A sale was updated.

```graphql
saleCataloguesAdd(
  id: ID!
  input: CatalogueInput!
): SaleAddCatalogues @deprecated
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type SaleAddCatalogues {
  sale: Sale
  discountErrors: [DiscountError!]! @deprecated
  errors: [DiscountError!]!
}
```

</Details>

### Arguments

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of a sale.

#### [<code>input</code>](#)<Bullet />[<code>CatalogueInput!</code>](/api-reference/discounts/inputs/catalogue-input.mdx)

Fields required to modify catalogue IDs of sale.

### Type

#### [<code>SaleAddCatalogues</code>](/api-reference/discounts/objects/sale-add-catalogues.mdx)

Adds products, categories, collections to a sale.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- SALE_UPDATED (async): A sale was updated.
