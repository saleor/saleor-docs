---
api_reference: true
id: order-add-note
title: orderAddNote
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

:::warning[DEPRECATED]

Use `orderNoteAdd` instead.

:::

Adds note to the order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
orderAddNote(
  order: ID!
  input: OrderAddNoteInput!
): OrderAddNote @deprecated
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type OrderAddNote {
  order: Order
  event: OrderEvent
  orderErrors: [OrderError!]! @deprecated
  errors: [OrderError!]!
}
```

</Details>

### Arguments

#### [<code>order</code>](#order)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#order\}

ID of the order to add a note for.

#### [<code>input</code>](#input)<Bullet />[<code>OrderAddNoteInput!</code>](/api-reference/orders/inputs/order-add-note-input.mdx) \{#input\}

Fields required to create a note for the order.

### Type

#### [<code>OrderAddNote</code>](/api-reference/orders/objects/order-add-note.mdx)

Adds note to the order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>
