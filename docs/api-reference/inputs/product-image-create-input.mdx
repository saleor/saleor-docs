---
id: product-image-create-input
title: ProductImageCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProductImageCreateInput {
  alt: String
  image: Upload!
  product: ID!
}
```

### Fields

#### `alt` ([`String`](/api-reference/scalars/string.mdx))

Alt text for an image.

#### `image` ([`Upload!`](/api-reference/scalars/upload.mdx))

Represents an image file in a multipart request.

#### `product` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an product.
