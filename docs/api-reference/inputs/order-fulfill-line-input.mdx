---
id: order-fulfill-line-input
title: OrderFulfillLineInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type OrderFulfillLineInput {
  orderLineId: ID
  stocks: [OrderFulfillStockInput!]!
}
```

### Fields

#### `orderLineId` ([`ID`](/api-reference/scalars/id.mdx))

The ID of the order line.

#### `stocks` ([`[OrderFulfillStockInput!]!`](/api-reference/inputs/order-fulfill-stock-input.mdx))

List of stock items to create.
