---
id: digital-content-input
title: DigitalContentInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DigitalContentInput {
  useDefaultSettings: Boolean!
  maxDownloads: Int
  urlValidDays: Int
  automaticFulfillment: Boolean
}
```

### Fields

#### `useDefaultSettings` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

Use default digital content settings for this product.

#### `maxDownloads` ([`Int`](/api-reference/scalars/int.mdx))

Determines how many times a download link can be accessed by a customer.

#### `urlValidDays` ([`Int`](/api-reference/scalars/int.mdx))

Determines for how many days a download link is active since it was generated.

#### `automaticFulfillment` ([`Boolean`](/api-reference/scalars/boolean.mdx))

Overwrite default automatic_fulfillment setting for variant.
