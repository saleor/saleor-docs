---
id: menu-create-input
title: MenuCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MenuCreateInput {
  name: String!
  slug: String
  items: [MenuItemInput]
}
```

### Fields

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

Name of the menu.

#### `slug` ([`String`](/api-reference/scalars/string.mdx))

Slug of the menu. Will be generated if not provided.

#### `items` ([`[MenuItemInput]`](/api-reference/inputs/menu-item-input.mdx))

List of menu items.
