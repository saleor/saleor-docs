---
id: gift-card-create-input
title: GiftCardCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GiftCardCreateInput {
  startDate: Date
  endDate: Date
  balance: PositiveDecimal
  userEmail: String
  code: String
}
```

### Fields

#### `startDate` ([`Date`](/api-reference/scalars/date.mdx))

Start date of the gift card in ISO 8601 format.

#### `endDate` ([`Date`](/api-reference/scalars/date.mdx))

End date of the gift card in ISO 8601 format.

#### `balance` ([`PositiveDecimal`](/api-reference/scalars/positive-decimal.mdx))

Value of the gift card.

#### `userEmail` ([`String`](/api-reference/scalars/string.mdx))

The customer's email of the gift card buyer.

#### `code` ([`String`](/api-reference/scalars/string.mdx))

Code to use the gift card.
