---
id: channel-delete-input
title: ChannelDeleteInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ChannelDeleteInput {
  targetChannel: ID!
}
```

### Fields

#### `targetChannel` ([`ID!`](/api-reference/scalars/id.mdx))

ID of channel to migrate orders from origin channel.
