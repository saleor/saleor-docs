---
id: product-variant-bulk-create-input
title: ProductVariantBulkCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProductVariantBulkCreateInput {
  attributes: [BulkAttributeValueInput]!
  sku: String!
  trackInventory: Boolean
  weight: WeightScalar
  stocks: [StockInput!]
  channelListings: [ProductVariantChannelListingAddInput!]
}
```

### Fields

#### `attributes` ([`[BulkAttributeValueInput]!`](/api-reference/inputs/bulk-attribute-value-input.mdx))

List of attributes specific to this variant.

#### `sku` ([`String!`](/api-reference/scalars/string.mdx))

Stock keeping unit.

#### `trackInventory` ([`Boolean`](/api-reference/scalars/boolean.mdx))

Determines if the inventory of this variant should be tracked. If false, the quantity won't change when customers buy this item.

#### `weight` ([`WeightScalar`](/api-reference/scalars/weight-scalar.mdx))

Weight of the Product Variant.

#### `stocks` ([`[StockInput!]`](/api-reference/inputs/stock-input.mdx))

Stocks of a product available for sale.

#### `channelListings` ([`[ProductVariantChannelListingAddInput!]`](/api-reference/inputs/product-variant-channel-listing-add-input.mdx))

List of prices assigned to channels.
