---
id: warehouse-create-input
title: WarehouseCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type WarehouseCreateInput {
  slug: String
  companyName: String
  email: String
  name: String!
  address: WarehouseAddressInput!
  shippingZones: [ID]
}
```

### Fields

#### `slug` ([`String`](/api-reference/scalars/string.mdx))

Warehouse slug.

#### `companyName` ([`String`](/api-reference/scalars/string.mdx))

Company name.

#### `email` ([`String`](/api-reference/scalars/string.mdx))

The email address of the warehouse.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

Warehouse name.

#### `address` ([`WarehouseAddressInput!`](/api-reference/inputs/warehouse-address-input.mdx))

Address of the warehouse.

#### `shippingZones` ([`[ID]`](/api-reference/scalars/id.mdx))

Shipping zones supported by the warehouse.
