---
id: menu-sorting-input
title: MenuSortingInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MenuSortingInput {
  direction: OrderDirection!
  field: MenuSortField!
}
```

### Fields

#### `direction` ([`OrderDirection!`](/api-reference/enums/order-direction.mdx))

Specifies the direction in which to sort products.

#### `field` ([`MenuSortField!`](/api-reference/enums/menu-sort-field.mdx))

Sort menus by the selected field.
