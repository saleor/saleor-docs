---
id: site-domain-input
title: SiteDomainInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SiteDomainInput {
  domain: String
  name: String
}
```

### Fields

#### `domain` ([`String`](/api-reference/scalars/string.mdx))

Domain name for shop.

#### `name` ([`String`](/api-reference/scalars/string.mdx))

Shop site name.
