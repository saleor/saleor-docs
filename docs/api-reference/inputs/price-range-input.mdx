---
id: price-range-input
title: PriceRangeInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PriceRangeInput {
  gte: Float
  lte: Float
}
```

### Fields

#### `gte` ([`Float`](/api-reference/scalars/float.mdx))

Price greater than or equal to.

#### `lte` ([`Float`](/api-reference/scalars/float.mdx))

Price less than or equal to.
