---
id: product-attribute-assign-input
title: ProductAttributeAssignInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProductAttributeAssignInput {
  id: ID!
  type: ProductAttributeType!
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the attribute to assign.

#### `type` ([`ProductAttributeType!`](/api-reference/enums/product-attribute-type.mdx))

The attribute type to be assigned as.
