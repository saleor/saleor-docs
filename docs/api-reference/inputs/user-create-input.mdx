---
id: user-create-input
title: UserCreateInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type UserCreateInput {
  defaultBillingAddress: AddressInput
  defaultShippingAddress: AddressInput
  firstName: String
  lastName: String
  email: String
  isActive: Boolean
  note: String
  redirectUrl: String
}
```

### Fields

#### `defaultBillingAddress` ([`AddressInput`](/api-reference/inputs/address-input.mdx))

Billing address of the customer.

#### `defaultShippingAddress` ([`AddressInput`](/api-reference/inputs/address-input.mdx))

Shipping address of the customer.

#### `firstName` ([`String`](/api-reference/scalars/string.mdx))

Given name.

#### `lastName` ([`String`](/api-reference/scalars/string.mdx))

Family name.

#### `email` ([`String`](/api-reference/scalars/string.mdx))

The unique email address of the user.

#### `isActive` ([`Boolean`](/api-reference/scalars/boolean.mdx))

User account is active.

#### `note` ([`String`](/api-reference/scalars/string.mdx))

A note about the user.

#### `redirectUrl` ([`String`](/api-reference/scalars/string.mdx))

URL of a view where users should be redirected to set the password. URL in RFC 1808 format.
