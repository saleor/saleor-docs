---
id: fulfillment-cancel-input
title: FulfillmentCancelInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type FulfillmentCancelInput {
  warehouseId: ID!
}
```

### Fields

#### `warehouseId` ([`ID!`](/api-reference/scalars/id.mdx))

ID of warehouse where items will be restock.
