---
id: staff-user-input
title: StaffUserInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type StaffUserInput {
  status: StaffMemberStatus
  search: String
}
```

### Fields

#### `status` ([`StaffMemberStatus`](/api-reference/enums/staff-member-status.mdx))

#### `search` ([`String`](/api-reference/scalars/string.mdx))
