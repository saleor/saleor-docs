---
id: sale-channel-listing-input
title: SaleChannelListingInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SaleChannelListingInput {
  addChannels: [SaleChannelListingAddInput!]
  removeChannels: [ID!]
}
```

### Fields

#### `addChannels` ([`[SaleChannelListingAddInput!]`](/api-reference/inputs/sale-channel-listing-add-input.mdx))

List of channels to which the sale should be assigned.

#### `removeChannels` ([`[ID!]`](/api-reference/scalars/id.mdx))

List of channels from which the sale should be unassigned.
