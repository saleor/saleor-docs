---
id: stock-input
title: StockInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type StockInput {
  warehouse: ID!
  quantity: Int
}
```

### Fields

#### `warehouse` ([`ID!`](/api-reference/scalars/id.mdx))

Warehouse in which stock is located.

#### `quantity` ([`Int`](/api-reference/scalars/int.mdx))

Quantity of items available for sell.
