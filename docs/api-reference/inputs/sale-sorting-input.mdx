---
id: sale-sorting-input
title: SaleSortingInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SaleSortingInput {
  direction: OrderDirection!
  channel: String
  field: SaleSortField!
}
```

### Fields

#### `direction` ([`OrderDirection!`](/api-reference/enums/order-direction.mdx))

Specifies the direction in which to sort products.

#### `channel` ([`String`](/api-reference/scalars/string.mdx))

Specifies the channel in which to sort the data.

#### `field` ([`SaleSortField!`](/api-reference/enums/sale-sort-field.mdx))

Sort sales by the selected field.
