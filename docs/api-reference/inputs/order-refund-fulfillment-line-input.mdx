---
id: order-refund-fulfillment-line-input
title: OrderRefundFulfillmentLineInput
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type OrderRefundFulfillmentLineInput {
  fulfillmentLineId: ID!
  quantity: Int!
}
```

### Fields

#### `fulfillmentLineId` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the fulfillment line to refund.

#### `quantity` ([`Int!`](/api-reference/scalars/int.mdx))

The number of items to be refunded.
