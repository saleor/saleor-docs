---
api_reference: true
id: gift-card-settings-update-input
title: GiftCardSettingsUpdateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input GiftCardSettingsUpdateInput {
  expiryType: GiftCardSettingsExpiryTypeEnum
  expiryPeriod: TimePeriodInputType
}
```

### Fields

#### [<code>expiryType</code>](#)<Bullet />[<code>GiftCardSettingsExpiryTypeEnum</code>](/api-reference/gift-cards/enums/gift-card-settings-expiry-type-enum.mdx)

Defines gift card default expiry settings.

#### [<code>expiryPeriod</code>](#)<Bullet />[<code>TimePeriodInputType</code>](/api-reference/miscellaneous/inputs/time-period-input-type.mdx)

Defines gift card expiry period.

### Member Of

[`giftCardSettingsUpdate`](/api-reference/gift-cards/mutations/gift-card-settings-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
