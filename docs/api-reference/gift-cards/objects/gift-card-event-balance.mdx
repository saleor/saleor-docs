---
api_reference: true
id: gift-card-event-balance
title: GiftCardEventBalance
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
type GiftCardEventBalance {
  initialBalance: Money
  currentBalance: Money!
  oldInitialBalance: Money
  oldCurrentBalance: Money
}
```

### Fields

#### [<code>initialBalance</code>](#initial-balance)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx) \{#initial-balance\}

Initial balance of the gift card.

#### [<code>currentBalance</code>](#current-balance)<Bullet />[<code>Money!</code>](/api-reference/miscellaneous/objects/money.mdx) \{#current-balance\}

Current balance of the gift card.

#### [<code>oldInitialBalance</code>](#old-initial-balance)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx) \{#old-initial-balance\}

Previous initial balance of the gift card.

#### [<code>oldCurrentBalance</code>](#old-current-balance)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx) \{#old-current-balance\}

Previous current balance of the gift card.

### Member Of

[`GiftCardEvent`](/api-reference/gift-cards/objects/gift-card-event.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
