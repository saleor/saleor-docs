---
api_reference: true
id: gift-card-events-enum
title: GiftCardEventsEnum
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>ISSUED</code>](#issued) \{#issued\}

#### [<code>BOUGHT</code>](#bought) \{#bought\}

#### [<code>UPDATED</code>](#updated) \{#updated\}

#### [<code>ACTIVATED</code>](#activated) \{#activated\}

#### [<code>DEACTIVATED</code>](#deactivated) \{#deactivated\}

#### [<code>BALANCE_RESET</code>](#balance-reset) \{#balance-reset\}

#### [<code>EXPIRY_DATE_UPDATED</code>](#expiry-date-updated) \{#expiry-date-updated\}

#### [<code>TAGS_UPDATED</code>](#tags-updated) \{#tags-updated\}

#### [<code>SENT_TO_CUSTOMER</code>](#sent-to-customer) \{#sent-to-customer\}

#### [<code>RESENT</code>](#resent) \{#resent\}

#### [<code>NOTE_ADDED</code>](#note-added) \{#note-added\}

#### [<code>USED_IN_ORDER</code>](#used-in-order) \{#used-in-order\}

#### [<code>REFUNDED_IN_ORDER</code>](#refunded-in-order) \{#refunded-in-order\}

### Member Of

[`GiftCardEvent`](/api-reference/gift-cards/objects/gift-card-event.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`GiftCardEventFilterInput`](/api-reference/gift-cards/inputs/gift-card-event-filter-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
