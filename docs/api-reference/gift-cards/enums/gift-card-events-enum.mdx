---
api_reference: true
id: gift-card-events-enum
title: GiftCardEventsEnum
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>ISSUED</code>](#)

#### [<code>BOUGHT</code>](#)

#### [<code>UPDATED</code>](#)

#### [<code>ACTIVATED</code>](#)

#### [<code>DEACTIVATED</code>](#)

#### [<code>BALANCE_RESET</code>](#)

#### [<code>EXPIRY_DATE_UPDATED</code>](#)

#### [<code>TAGS_UPDATED</code>](#)

#### [<code>SENT_TO_CUSTOMER</code>](#)

#### [<code>RESENT</code>](#)

#### [<code>NOTE_ADDED</code>](#)

#### [<code>USED_IN_ORDER</code>](#)

### Member Of

[`GiftCardEvent`](/api-reference/gift-cards/objects/gift-card-event.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`GiftCardEventFilterInput`](/api-reference/gift-cards/inputs/gift-card-event-filter-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
