---
id: shipping-zone-bulk-delete
title: shippingZoneBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes shipping zones.

```graphql
shippingZoneBulkDelete(
  ids: [ID]!
): ShippingZoneBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of shipping zone IDs to delete.

### Type

#### [`ShippingZoneBulkDelete`](/api-reference/objects/shipping-zone-bulk-delete.mdx)

Deletes shipping zones.
