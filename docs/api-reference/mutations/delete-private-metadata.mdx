---
id: delete-private-metadata
title: deletePrivateMetadata
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Delete object's private metadata.

```graphql
deletePrivateMetadata(
  id: ID!
  keys: [String!]!
): DeletePrivateMetadata

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an object to update.

#### `keys` ([`[String!]!`](/api-reference/scalars/string.mdx))

Metadata keys to delete.

### Type

#### [`DeletePrivateMetadata`](/api-reference/objects/delete-private-metadata.mdx)

Delete object's private metadata.
