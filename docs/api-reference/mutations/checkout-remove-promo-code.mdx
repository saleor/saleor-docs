---
id: checkout-remove-promo-code
title: checkoutRemovePromoCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Remove a gift card or a voucher from a checkout.

```graphql
checkoutRemovePromoCode(
  checkoutId: ID!
  promoCode: String!
): CheckoutRemovePromoCode

```

### Arguments

#### `checkoutId` ([`ID!`](/api-reference/scalars/id.mdx))

Checkout ID.

#### `promoCode` ([`String!`](/api-reference/scalars/string.mdx))

Gift card code or voucher code.

### Type

#### [`CheckoutRemovePromoCode`](/api-reference/objects/checkout-remove-promo-code.mdx)

Remove a gift card or a voucher from a checkout.
