---
id: order-fulfillment-update-tracking
title: orderFulfillmentUpdateTracking
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a fulfillment for an order.

```graphql
orderFulfillmentUpdateTracking(
  id: ID!
  input: FulfillmentUpdateTrackingInput!
): FulfillmentUpdateTracking

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an fulfillment to update.

#### `input` ([`FulfillmentUpdateTrackingInput!`](/api-reference/inputs/fulfillment-update-tracking-input.mdx))

Fields required to update an fulfillment.

### Type

#### [`FulfillmentUpdateTracking`](/api-reference/objects/fulfillment-update-tracking.mdx)

Updates a fulfillment for an order.
