---
id: product-variant-create
title: productVariantCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new variant for a product.

```graphql
productVariantCreate(
  input: ProductVariantCreateInput!
): ProductVariantCreate

```

### Arguments

#### `input` ([`ProductVariantCreateInput!`](/api-reference/inputs/product-variant-create-input.mdx))

Fields required to create a product variant.

### Type

#### [`ProductVariantCreate`](/api-reference/objects/product-variant-create.mdx)

Creates a new variant for a product.
