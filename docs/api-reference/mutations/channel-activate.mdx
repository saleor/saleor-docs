---
id: channel-activate
title: channelActivate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Activate a channel.

```graphql
channelActivate(
  id: ID!
): ChannelActivate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the channel to activate.

### Type

#### [`ChannelActivate`](/api-reference/objects/channel-activate.mdx)

Activate a channel.
