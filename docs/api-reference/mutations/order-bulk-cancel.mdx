---
id: order-bulk-cancel
title: orderBulkCancel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Cancels orders.

```graphql
orderBulkCancel(
  ids: [ID]!
): OrderBulkCancel

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of orders IDs to cancel.

### Type

#### [`OrderBulkCancel`](/api-reference/objects/order-bulk-cancel.mdx)

Cancels orders.
