---
id: order-fulfill
title: orderFulfill
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates new fulfillments for an order.

```graphql
orderFulfill(
  input: OrderFulfillInput!
  order: ID = 0
): OrderFulfill

```

### Arguments

#### `input` ([`OrderFulfillInput!`](/api-reference/inputs/order-fulfill-input.mdx))

Fields required to create an fulfillment.

#### `order` ([`ID`](/api-reference/scalars/id.mdx))

ID of the order to be fulfilled.

### Type

#### [`OrderFulfill`](/api-reference/objects/order-fulfill.mdx)

Creates new fulfillments for an order.
