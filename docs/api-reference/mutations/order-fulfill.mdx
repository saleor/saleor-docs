---
id: order-fulfill
title: orderFulfill
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Creates new fulfillments for an order.

Requires one of the following permissions: MANAGE_ORDERS.

```graphql
orderFulfill(
  input: OrderFulfillInput!
  order: ID
): OrderFulfill
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>orderFulfill.<b>input</b></code>](#)<Bullet />[`OrderFulfillInput!`](../../api-reference/inputs/order-fulfill-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

> Fields required to create a fulfillment.

#### [<code style={{ fontWeight: 'normal' }}>orderFulfill.<b>order</b></code>](#)<Bullet />[`ID`](../../api-reference/scalars/id) <Badge class="secondary" text="scalar"/>

> ID of the order to be fulfilled.

### Type

#### [`OrderFulfill`](../../api-reference/objects/order-fulfill) <Badge class="secondary" text="object"/>

> Creates new fulfillments for an order.

Requires one of the following permissions: MANAGE_ORDERS.

>
