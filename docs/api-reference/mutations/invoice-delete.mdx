---
id: invoice-delete
title: invoiceDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes an invoice.

```graphql
invoiceDelete(
  id: ID!
): InvoiceDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an invoice to delete.

### Type

#### [`InvoiceDelete`](/api-reference/objects/invoice-delete.mdx)

Deletes an invoice.
