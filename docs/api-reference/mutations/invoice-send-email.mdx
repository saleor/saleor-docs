---
id: invoice-send-email
title: invoiceSendEmail
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Send an invoice by email.

```graphql
invoiceSendEmail(
  id: ID!
): InvoiceSendEmail

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an invoice to be sent.

### Type

#### [`InvoiceSendEmail`](/api-reference/objects/invoice-send-email.mdx)

Send an invoice by email.
