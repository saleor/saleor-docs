---
id: app-fetch-manifest
title: appFetchManifest
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Fetch and validate manifest.

```graphql
appFetchManifest(
  manifestUrl: String!
): AppFetchManifest

```

### Arguments

#### `manifestUrl` ([`String!`](/api-reference/scalars/string.mdx))

### Type

#### [`AppFetchManifest`](/api-reference/objects/app-fetch-manifest.mdx)

Fetch and validate manifest.
