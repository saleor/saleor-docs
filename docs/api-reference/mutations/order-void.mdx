---
id: order-void
title: orderVoid
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Void an order.

```graphql
orderVoid(
  id: ID!
): OrderVoid

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the order to void.

### Type

#### [`OrderVoid`](/api-reference/objects/order-void.mdx)

Void an order.
