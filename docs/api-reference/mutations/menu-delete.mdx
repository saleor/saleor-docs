---
id: menu-delete
title: menuDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a menu.

```graphql
menuDelete(
  id: ID!
): MenuDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a menu to delete.

### Type

#### [`MenuDelete`](/api-reference/objects/menu-delete.mdx)

Deletes a menu.
