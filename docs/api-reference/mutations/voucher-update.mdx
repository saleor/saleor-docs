---
id: voucher-update
title: voucherUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a voucher.

```graphql
voucherUpdate(
  id: ID!
  input: VoucherInput!
): VoucherUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a voucher to update.

#### `input` ([`VoucherInput!`](/api-reference/inputs/voucher-input.mdx))

Fields required to update a voucher.

### Type

#### [`VoucherUpdate`](/api-reference/objects/voucher-update.mdx)

Updates a voucher.
