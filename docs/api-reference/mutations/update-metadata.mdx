---
id: update-metadata
title: updateMetadata
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Updates metadata of an object. To use it, you need to have access to the modified object.

```graphql
updateMetadata(
  id: ID!
  input: [MetadataInput!]!
): UpdateMetadata
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>updateMetadata.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID or token (for Order and Checkout) of an object to update.

#### [<code style={{ fontWeight: 'normal' }}>updateMetadata.<b>input</b></code>](#)<Bullet />[`[MetadataInput!]!`](../../api-reference/inputs/metadata-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

> Fields required to update the object's metadata.

### Type

#### [`UpdateMetadata`](../../api-reference/objects/update-metadata) <Badge class="secondary" text="object"/>

> Updates metadata of an object. To use it, you need to have access to the modified object.
