---
id: account-update
title: accountUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates the account of the logged-in user.

```graphql
accountUpdate(
  input: AccountInput!
): AccountUpdate

```

### Arguments

#### `input` ([`AccountInput!`](/api-reference/inputs/account-input.mdx))

Fields required to update the account of the logged-in user.

### Type

#### [`AccountUpdate`](/api-reference/objects/account-update.mdx)

Updates the account of the logged-in user.
