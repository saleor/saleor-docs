---
id: shipping-price-bulk-delete
title: shippingPriceBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes shipping prices.

```graphql
shippingPriceBulkDelete(
  ids: [ID]!
): ShippingPriceBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of shipping price IDs to delete.

### Type

#### [`ShippingPriceBulkDelete`](/api-reference/objects/shipping-price-bulk-delete.mdx)

Deletes shipping prices.
