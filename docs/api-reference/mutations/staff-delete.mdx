---
id: staff-delete
title: staffDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a staff user.

```graphql
staffDelete(
  id: ID!
): StaffDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a staff user to delete.

### Type

#### [`StaffDelete`](/api-reference/objects/staff-delete.mdx)

Deletes a staff user.
