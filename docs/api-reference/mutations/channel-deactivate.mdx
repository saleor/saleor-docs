---
id: channel-deactivate
title: channelDeactivate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deactivate a channel.

```graphql
channelDeactivate(
  id: ID!
): ChannelDeactivate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the channel to deactivate.

### Type

#### [`ChannelDeactivate`](/api-reference/objects/channel-deactivate.mdx)

Deactivate a channel.
