---
id: gift-card-deactivate
title: giftCardDeactivate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Deactivate a gift card.

Requires one of the following permissions: MANAGE_GIFT_CARD.

```graphql
giftCardDeactivate(
  id: ID!
): GiftCardDeactivate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>giftCardDeactivate.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID of a gift card to deactivate.

### Type

#### [`GiftCardDeactivate`](../../api-reference/objects/gift-card-deactivate) <Badge class="secondary" text="object"/>

> Deactivate a gift card.

Requires one of the following permissions: MANAGE_GIFT_CARD.

>
