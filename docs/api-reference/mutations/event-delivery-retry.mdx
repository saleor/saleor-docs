---
id: event-delivery-retry
title: eventDeliveryRetry
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Retries event delivery.

Requires one of the following permissions: MANAGE_APPS.

```graphql
eventDeliveryRetry(
  id: ID!
): EventDeliveryRetry
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>eventDeliveryRetry.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID of the event delivery to retry.

### Type

#### [`EventDeliveryRetry`](../../api-reference/objects/event-delivery-retry) <Badge class="secondary" text="object"/>

> Retries event delivery.

Requires one of the following permissions: MANAGE_APPS.

>
