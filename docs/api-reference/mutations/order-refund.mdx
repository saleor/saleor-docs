---
id: order-refund
title: orderRefund
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Refund an order.

```graphql
orderRefund(
  amount: PositiveDecimal!
  id: ID!
): OrderRefund

```

### Arguments

#### `amount` ([`PositiveDecimal!`](/api-reference/scalars/positive-decimal.mdx))

Amount of money to refund.

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the order to refund.

### Type

#### [`OrderRefund`](/api-reference/objects/order-refund.mdx)

Refund an order.
