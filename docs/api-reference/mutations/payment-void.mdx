---
id: payment-void
title: paymentVoid
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Voids the authorized payment.

```graphql
paymentVoid(
  paymentId: ID!
): PaymentVoid

```

### Arguments

#### `paymentId` ([`ID!`](/api-reference/scalars/id.mdx))

Payment ID.

### Type

#### [`PaymentVoid`](/api-reference/objects/payment-void.mdx)

Voids the authorized payment.
