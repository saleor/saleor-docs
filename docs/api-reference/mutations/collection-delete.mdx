---
id: collection-delete
title: collectionDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a collection.

```graphql
collectionDelete(
  id: ID!
): CollectionDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a collection to delete.

### Type

#### [`CollectionDelete`](/api-reference/objects/collection-delete.mdx)

Deletes a collection.
