---
id: permission-group-update
title: permissionGroupUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Update permission group.

```graphql
permissionGroupUpdate(
  id: ID!
  input: PermissionGroupUpdateInput!
): PermissionGroupUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the group to update.

#### `input` ([`PermissionGroupUpdateInput!`](/api-reference/inputs/permission-group-update-input.mdx))

Input fields to create permission group.

### Type

#### [`PermissionGroupUpdate`](/api-reference/objects/permission-group-update.mdx)

Update permission group.
