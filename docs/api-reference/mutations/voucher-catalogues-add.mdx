---
id: voucher-catalogues-add
title: voucherCataloguesAdd
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Adds products, categories, collections to a voucher.

```graphql
voucherCataloguesAdd(
  id: ID!
  input: CatalogueInput!
): VoucherAddCatalogues

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a voucher.

#### `input` ([`CatalogueInput!`](/api-reference/inputs/catalogue-input.mdx))

Fields required to modify catalogue IDs of voucher.

### Type

#### [`VoucherAddCatalogues`](/api-reference/objects/voucher-add-catalogues.mdx)

Adds products, categories, collections to a voucher.
