---
id: voucher-catalogues-remove
title: voucherCataloguesRemove
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Removes products, categories, collections from a voucher.

```graphql
voucherCataloguesRemove(
  id: ID!
  input: CatalogueInput!
): VoucherRemoveCatalogues

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a voucher.

#### `input` ([`CatalogueInput!`](/api-reference/inputs/catalogue-input.mdx))

Fields required to modify catalogue IDs of voucher.

### Type

#### [`VoucherRemoveCatalogues`](/api-reference/objects/voucher-remove-catalogues.mdx)

Removes products, categories, collections from a voucher.
