---
id: menu-update
title: menuUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a menu.

```graphql
menuUpdate(
  id: ID!
  input: MenuInput!
): MenuUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a menu to update.

#### `input` ([`MenuInput!`](/api-reference/inputs/menu-input.mdx))

Fields required to update a menu.

### Type

#### [`MenuUpdate`](/api-reference/objects/menu-update.mdx)

Updates a menu.
