---
id: product-create
title: productCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new product.

```graphql
productCreate(
  input: ProductCreateInput!
): ProductCreate

```

### Arguments

#### `input` ([`ProductCreateInput!`](/api-reference/inputs/product-create-input.mdx))

Fields required to create a product.

### Type

#### [`ProductCreate`](/api-reference/objects/product-create.mdx)

Creates a new product.
