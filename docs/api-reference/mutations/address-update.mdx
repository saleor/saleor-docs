---
id: address-update
title: addressUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates an address.

```graphql
addressUpdate(
  id: ID!
  input: AddressInput!
): AddressUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the address to update.

#### `input` ([`AddressInput!`](/api-reference/inputs/address-input.mdx))

Fields required to update the address.

### Type

#### [`AddressUpdate`](/api-reference/objects/address-update.mdx)

Updates an address.
