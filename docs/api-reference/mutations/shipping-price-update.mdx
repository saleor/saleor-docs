---
id: shipping-price-update
title: shippingPriceUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a new shipping price.

```graphql
shippingPriceUpdate(
  id: ID!
  input: ShippingPriceInput!
): ShippingPriceUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a shipping price to update.

#### `input` ([`ShippingPriceInput!`](/api-reference/inputs/shipping-price-input.mdx))

Fields required to update a shipping price.

### Type

#### [`ShippingPriceUpdate`](/api-reference/objects/shipping-price-update.mdx)

Updates a new shipping price.
