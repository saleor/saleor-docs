---
id: collection-update
title: collectionUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a collection.

```graphql
collectionUpdate(
  id: ID!
  input: CollectionInput!
): CollectionUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a collection to update.

#### `input` ([`CollectionInput!`](/api-reference/inputs/collection-input.mdx))

Fields required to update a collection.

### Type

#### [`CollectionUpdate`](/api-reference/objects/collection-update.mdx)

Updates a collection.
