---
id: app-token-delete
title: appTokenDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes an authentication token assigned to app.

```graphql
appTokenDelete(
  id: ID!
): AppTokenDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an auth token to delete.

### Type

#### [`AppTokenDelete`](/api-reference/objects/app-token-delete.mdx)

Deletes an authentication token assigned to app.
