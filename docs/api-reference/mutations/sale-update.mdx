---
id: sale-update
title: saleUpdate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Updates a sale.

Requires one of the following permissions: MANAGE_DISCOUNTS.

```graphql
saleUpdate(
  id: ID!
  input: SaleInput!
): SaleUpdate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>saleUpdate.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID of a sale to update.

#### [<code style={{ fontWeight: 'normal' }}>saleUpdate.<b>input</b></code>](#)<Bullet />[`SaleInput!`](../../api-reference/inputs/sale-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

> Fields required to update a sale.

### Type

#### [`SaleUpdate`](../../api-reference/objects/sale-update) <Badge class="secondary" text="object"/>

> Updates a sale.

Requires one of the following permissions: MANAGE_DISCOUNTS.

>
