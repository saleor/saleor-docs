---
id: order-fulfillment-refund-products
title: orderFulfillmentRefundProducts
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Refund products.

```graphql
orderFulfillmentRefundProducts(
  input: OrderRefundProductsInput!
  order: ID!
): FulfillmentRefundProducts

```

### Arguments

#### `input` ([`OrderRefundProductsInput!`](/api-reference/inputs/order-refund-products-input.mdx))

Fields required to create an refund fulfillment.

#### `order` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the order to be refunded.

### Type

#### [`FulfillmentRefundProducts`](/api-reference/objects/fulfillment-refund-products.mdx)

Refund products.
