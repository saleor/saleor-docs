---
id: shipping-price-create
title: shippingPriceCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new shipping price.

```graphql
shippingPriceCreate(
  input: ShippingPriceInput!
): ShippingPriceCreate

```

### Arguments

#### `input` ([`ShippingPriceInput!`](/api-reference/inputs/shipping-price-input.mdx))

Fields required to create a shipping price.

### Type

#### [`ShippingPriceCreate`](/api-reference/objects/shipping-price-create.mdx)

Creates a new shipping price.
