---
id: draft-order-complete
title: draftOrderComplete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Completes creating an order.

```graphql
draftOrderComplete(
  id: ID!
): DraftOrderComplete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the order that will be completed.

### Type

#### [`DraftOrderComplete`](/api-reference/objects/draft-order-complete.mdx)

Completes creating an order.
