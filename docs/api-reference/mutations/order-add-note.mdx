---
id: order-add-note
title: orderAddNote
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Adds note to the order.

Requires one of the following permissions: MANAGE_ORDERS.

```graphql
orderAddNote(
  order: ID!
  input: OrderAddNoteInput!
): OrderAddNote
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>orderAddNote.<b>order</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID of the order to add a note for.

#### [<code style={{ fontWeight: 'normal' }}>orderAddNote.<b>input</b></code>](#)<Bullet />[`OrderAddNoteInput!`](../../api-reference/inputs/order-add-note-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

> Fields required to create a note for the order.

### Type

#### [`OrderAddNote`](../../api-reference/objects/order-add-note) <Badge class="secondary" text="object"/>

> Adds note to the order.

Requires one of the following permissions: MANAGE_ORDERS.

>
