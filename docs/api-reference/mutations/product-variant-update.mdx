---
id: product-variant-update
title: productVariantUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates an existing variant for product.

```graphql
productVariantUpdate(
  id: ID!
  input: ProductVariantInput!
): ProductVariantUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a product variant to update.

#### `input` ([`ProductVariantInput!`](/api-reference/inputs/product-variant-input.mdx))

Fields required to update a product variant.

### Type

#### [`ProductVariantUpdate`](/api-reference/objects/product-variant-update.mdx)

Updates an existing variant for product.
