---
id: tokens-deactivate-all
title: tokensDeactivateAll
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deactivate all JWT tokens of the currently authenticated user.

```graphql
tokensDeactivateAll: DeactivateAllUserTokens

```

### Type

#### [`DeactivateAllUserTokens`](/api-reference/objects/deactivate-all-user-tokens.mdx)

Deactivate all JWT tokens of the currently authenticated user.
