---
id: product-type-delete
title: productTypeDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a product type.

```graphql
productTypeDelete(
  id: ID!
): ProductTypeDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a product type to delete.

### Type

#### [`ProductTypeDelete`](/api-reference/objects/product-type-delete.mdx)

Deletes a product type.
