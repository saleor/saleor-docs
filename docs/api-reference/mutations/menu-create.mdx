---
id: menu-create
title: menuCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new Menu.

```graphql
menuCreate(
  input: MenuCreateInput!
): MenuCreate

```

### Arguments

#### `input` ([`MenuCreateInput!`](/api-reference/inputs/menu-create-input.mdx))

Fields required to create a menu.

### Type

#### [`MenuCreate`](/api-reference/objects/menu-create.mdx)

Creates a new Menu.
