---
id: attribute-bulk-delete
title: attributeBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes attributes.

```graphql
attributeBulkDelete(
  ids: [ID]!
): AttributeBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of attribute IDs to delete.

### Type

#### [`AttributeBulkDelete`](/api-reference/objects/attribute-bulk-delete.mdx)

Deletes attributes.
