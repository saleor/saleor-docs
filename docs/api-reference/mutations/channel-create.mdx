---
id: channel-create
title: channelCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates new channel.

```graphql
channelCreate(
  input: ChannelCreateInput!
): ChannelCreate

```

### Arguments

#### `input` ([`ChannelCreateInput!`](/api-reference/inputs/channel-create-input.mdx))

Fields required to create channel.

### Type

#### [`ChannelCreate`](/api-reference/objects/channel-create.mdx)

Creates new channel.
