---
id: page-type-create
title: pageTypeCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Create a new page type.

```graphql
pageTypeCreate(
  input: PageTypeCreateInput!
): PageTypeCreate

```

### Arguments

#### `input` ([`PageTypeCreateInput!`](/api-reference/inputs/page-type-create-input.mdx))

Fields required to create page type.

### Type

#### [`PageTypeCreate`](/api-reference/objects/page-type-create.mdx)

Create a new page type.
