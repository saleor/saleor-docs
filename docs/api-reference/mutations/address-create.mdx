---
id: address-create
title: addressCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates user address.

```graphql
addressCreate(
  input: AddressInput!
  userId: ID!
): AddressCreate

```

### Arguments

#### `input` ([`AddressInput!`](/api-reference/inputs/address-input.mdx))

Fields required to create address.

#### `userId` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a user to create address for.

### Type

#### [`AddressCreate`](/api-reference/objects/address-create.mdx)

Creates user address.
