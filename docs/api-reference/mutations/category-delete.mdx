---
id: category-delete
title: categoryDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a category.

```graphql
categoryDelete(
  id: ID!
): CategoryDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a category to delete.

### Type

#### [`CategoryDelete`](/api-reference/objects/category-delete.mdx)

Deletes a category.
