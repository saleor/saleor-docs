---
id: webhook-create
title: webhookCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new webhook subscription.

```graphql
webhookCreate(
  input: WebhookCreateInput!
): WebhookCreate

```

### Arguments

#### `input` ([`WebhookCreateInput!`](/api-reference/inputs/webhook-create-input.mdx))

Fields required to create a webhook.

### Type

#### [`WebhookCreate`](/api-reference/objects/webhook-create.mdx)

Creates a new webhook subscription.
