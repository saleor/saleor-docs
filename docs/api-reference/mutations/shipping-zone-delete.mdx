---
id: shipping-zone-delete
title: shippingZoneDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a shipping zone.

```graphql
shippingZoneDelete(
  id: ID!
): ShippingZoneDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a shipping zone to delete.

### Type

#### [`ShippingZoneDelete`](/api-reference/objects/shipping-zone-delete.mdx)

Deletes a shipping zone.
