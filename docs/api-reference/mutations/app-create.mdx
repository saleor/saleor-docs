---
id: app-create
title: appCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new app.

```graphql
appCreate(
  input: AppInput!
): AppCreate

```

### Arguments

#### `input` ([`AppInput!`](/api-reference/inputs/app-input.mdx))

Fields required to create a new app.

### Type

#### [`AppCreate`](/api-reference/objects/app-create.mdx)

Creates a new app.
