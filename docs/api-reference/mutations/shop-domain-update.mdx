---
id: shop-domain-update
title: shopDomainUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates site domain of the shop.

```graphql
shopDomainUpdate(
  input: SiteDomainInput
): ShopDomainUpdate

```

### Arguments

#### `input` ([`SiteDomainInput`](/api-reference/inputs/site-domain-input.mdx))

Fields required to update site.

### Type

#### [`ShopDomainUpdate`](/api-reference/objects/shop-domain-update.mdx)

Updates site domain of the shop.
