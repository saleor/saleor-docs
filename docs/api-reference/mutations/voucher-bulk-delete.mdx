---
id: voucher-bulk-delete
title: voucherBulkDelete
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Deletes vouchers.

Requires one of the following permissions: MANAGE_DISCOUNTS.

```graphql
voucherBulkDelete(
  ids: [ID!]!
): VoucherBulkDelete
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>voucherBulkDelete.<b>ids</b></code>](#)<Bullet />[`[ID!]!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> List of voucher IDs to delete.

### Type

#### [`VoucherBulkDelete`](../../api-reference/objects/voucher-bulk-delete) <Badge class="secondary" text="object"/>

> Deletes vouchers.

Requires one of the following permissions: MANAGE_DISCOUNTS.

>
