---
id: page-delete
title: pageDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a page.

```graphql
pageDelete(
  id: ID!
): PageDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a page to delete.

### Type

#### [`PageDelete`](/api-reference/objects/page-delete.mdx)

Deletes a page.
