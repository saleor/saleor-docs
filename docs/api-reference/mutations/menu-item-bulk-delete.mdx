---
id: menu-item-bulk-delete
title: menuItemBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes menu items.

```graphql
menuItemBulkDelete(
  ids: [ID]!
): MenuItemBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of menu item IDs to delete.

### Type

#### [`MenuItemBulkDelete`](/api-reference/objects/menu-item-bulk-delete.mdx)

Deletes menu items.
