---
id: webhook-update
title: webhookUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates a webhook subscription.

```graphql
webhookUpdate(
  id: ID!
  input: WebhookUpdateInput!
): WebhookUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a webhook to update.

#### `input` ([`WebhookUpdateInput!`](/api-reference/inputs/webhook-update-input.mdx))

Fields required to update a webhook.

### Type

#### [`WebhookUpdate`](/api-reference/objects/webhook-update.mdx)

Updates a webhook subscription.
