---
id: app-delete
title: appDelete
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Deletes an app.

Requires one of the following permissions: MANAGE_APPS.

```graphql
appDelete(
  id: ID!
): AppDelete
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>appDelete.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID of an app to delete.

### Type

#### [`AppDelete`](../../api-reference/objects/app-delete) <Badge class="secondary" text="object"/>

> Deletes an app.

Requires one of the following permissions: MANAGE_APPS.

>
