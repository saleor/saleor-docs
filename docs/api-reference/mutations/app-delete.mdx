---
id: app-delete
title: appDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes an app.

```graphql
appDelete(
  id: ID!
): AppDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an app to delete.

### Type

#### [`AppDelete`](/api-reference/objects/app-delete.mdx)

Deletes an app.
