---
id: digital-content-delete
title: digitalContentDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Remove digital content assigned to given variant.

```graphql
digitalContentDelete(
  variantId: ID!
): DigitalContentDelete

```

### Arguments

#### `variantId` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a product variant with digital content to remove.

### Type

#### [`DigitalContentDelete`](/api-reference/objects/digital-content-delete.mdx)

Remove digital content assigned to given variant.
