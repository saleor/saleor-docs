---
id: gift-card-create
title: giftCardCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new gift card.

```graphql
giftCardCreate(
  input: GiftCardCreateInput!
): GiftCardCreate

```

### Arguments

#### `input` ([`GiftCardCreateInput!`](/api-reference/inputs/gift-card-create-input.mdx))

Fields required to create a gift card.

### Type

#### [`GiftCardCreate`](/api-reference/objects/gift-card-create.mdx)

Creates a new gift card.
