---
id: menu-bulk-delete
title: menuBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes menus.

```graphql
menuBulkDelete(
  ids: [ID]!
): MenuBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of menu IDs to delete.

### Type

#### [`MenuBulkDelete`](/api-reference/objects/menu-bulk-delete.mdx)

Deletes menus.
