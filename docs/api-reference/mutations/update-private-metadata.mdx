---
id: update-private-metadata
title: updatePrivateMetadata
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Updates private metadata of an object. To use it, you need to be an authenticated staff user or an app and have access to the modified object.

```graphql
updatePrivateMetadata(
  id: ID!
  input: [MetadataInput!]!
): UpdatePrivateMetadata
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>updatePrivateMetadata.<b>id</b></code>](#)<Bullet />[`ID!`](../../api-reference/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> ID or token (for Order and Checkout) of an object to update.

#### [<code style={{ fontWeight: 'normal' }}>updatePrivateMetadata.<b>input</b></code>](#)<Bullet />[`[MetadataInput!]!`](../../api-reference/inputs/metadata-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

> Fields required to update the object's metadata.

### Type

#### [`UpdatePrivateMetadata`](../../api-reference/objects/update-private-metadata) <Badge class="secondary" text="object"/>

> Updates private metadata of an object. To use it, you need to be an authenticated staff user or an app and have access to the modified object.
