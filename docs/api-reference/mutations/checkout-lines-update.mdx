---
id: checkout-lines-update
title: checkoutLinesUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates checkout line in the existing checkout.

```graphql
checkoutLinesUpdate(
  checkoutId: ID!
  lines: [CheckoutLineInput]!
): CheckoutLinesUpdate

```

### Arguments

#### `checkoutId` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the checkout.

#### `lines` ([`[CheckoutLineInput]!`](/api-reference/inputs/checkout-line-input.mdx))

A list of checkout lines, each containing information about an item in the checkout.

### Type

#### [`CheckoutLinesUpdate`](/api-reference/objects/checkout-lines-update.mdx)

Updates checkout line in the existing checkout.
