---
id: page-create
title: pageCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new page.

```graphql
pageCreate(
  input: PageCreateInput!
): PageCreate

```

### Arguments

#### `input` ([`PageCreateInput!`](/api-reference/inputs/page-create-input.mdx))

Fields required to create a page.

### Type

#### [`PageCreate`](/api-reference/objects/page-create.mdx)

Creates a new page.
