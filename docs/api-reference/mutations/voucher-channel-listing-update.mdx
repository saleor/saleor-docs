---
id: voucher-channel-listing-update
title: voucherChannelListingUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Manage voucher's availability in channels.

```graphql
voucherChannelListingUpdate(
  id: ID!
  input: VoucherChannelListingInput!
): VoucherChannelListingUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a voucher to update.

#### `input` ([`VoucherChannelListingInput!`](/api-reference/inputs/voucher-channel-listing-input.mdx))

Fields required to update voucher channel listings.

### Type

#### [`VoucherChannelListingUpdate`](/api-reference/objects/voucher-channel-listing-update.mdx)

Manage voucher's availability in channels.
