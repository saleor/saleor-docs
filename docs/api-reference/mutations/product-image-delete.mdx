---
id: product-image-delete
title: productImageDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a product image.

```graphql
productImageDelete(
  id: ID!
): ProductImageDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a product image to delete.

### Type

#### [`ProductImageDelete`](/api-reference/objects/product-image-delete.mdx)

Deletes a product image.
