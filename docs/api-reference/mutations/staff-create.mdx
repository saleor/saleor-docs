---
id: staff-create
title: staffCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new staff user.

```graphql
staffCreate(
  input: StaffCreateInput!
): StaffCreate

```

### Arguments

#### `input` ([`StaffCreateInput!`](/api-reference/inputs/staff-create-input.mdx))

Fields required to create a staff user.

### Type

#### [`StaffCreate`](/api-reference/objects/staff-create.mdx)

Creates a new staff user.
