---
id: product-bulk-delete
title: productBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes products.

```graphql
productBulkDelete(
  ids: [ID]!
): ProductBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of product IDs to delete.

### Type

#### [`ProductBulkDelete`](/api-reference/objects/product-bulk-delete.mdx)

Deletes products.
