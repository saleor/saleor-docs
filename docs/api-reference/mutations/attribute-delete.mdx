---
id: attribute-delete
title: attributeDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes an attribute.

```graphql
attributeDelete(
  id: ID!
): AttributeDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an attribute to delete.

### Type

#### [`AttributeDelete`](/api-reference/objects/attribute-delete.mdx)

Deletes an attribute.
