---
id: checkout-customer-detach
title: checkoutCustomerDetach
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Removes the user assigned as the owner of the checkout.

```graphql
checkoutCustomerDetach(
  checkoutId: ID!
): CheckoutCustomerDetach

```

### Arguments

#### `checkoutId` ([`ID!`](/api-reference/scalars/id.mdx))

Checkout ID.

### Type

#### [`CheckoutCustomerDetach`](/api-reference/objects/checkout-customer-detach.mdx)

Removes the user assigned as the owner of the checkout.
