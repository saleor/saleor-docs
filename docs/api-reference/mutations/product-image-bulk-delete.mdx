---
id: product-image-bulk-delete
title: productImageBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes product images.

```graphql
productImageBulkDelete(
  ids: [ID]!
): ProductImageBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of product image IDs to delete.

### Type

#### [`ProductImageBulkDelete`](/api-reference/objects/product-image-bulk-delete.mdx)

Deletes product images.
