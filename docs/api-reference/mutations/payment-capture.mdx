---
id: payment-capture
title: paymentCapture
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Captures the authorized payment amount.

```graphql
paymentCapture(
  amount: PositiveDecimal
  paymentId: ID!
): PaymentCapture

```

### Arguments

#### `amount` ([`PositiveDecimal`](/api-reference/scalars/positive-decimal.mdx))

Transaction amount.

#### `paymentId` ([`ID!`](/api-reference/scalars/id.mdx))

Payment ID.

### Type

#### [`PaymentCapture`](/api-reference/objects/payment-capture.mdx)

Captures the authorized payment amount.
