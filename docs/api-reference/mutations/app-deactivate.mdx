---
id: app-deactivate
title: appDeactivate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deactivate the app.

```graphql
appDeactivate(
  id: ID!
): AppDeactivate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of app to deactivate.

### Type

#### [`AppDeactivate`](/api-reference/objects/app-deactivate.mdx)

Deactivate the app.
