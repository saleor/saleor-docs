---
id: attribute-update
title: attributeUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates attribute.

```graphql
attributeUpdate(
  id: ID!
  input: AttributeUpdateInput!
): AttributeUpdate

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an attribute to update.

#### `input` ([`AttributeUpdateInput!`](/api-reference/inputs/attribute-update-input.mdx))

Fields required to update an attribute.

### Type

#### [`AttributeUpdate`](/api-reference/objects/attribute-update.mdx)

Updates attribute.
