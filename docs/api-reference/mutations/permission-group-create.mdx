---
id: permission-group-create
title: permissionGroupCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Create new permission group.

```graphql
permissionGroupCreate(
  input: PermissionGroupCreateInput!
): PermissionGroupCreate

```

### Arguments

#### `input` ([`PermissionGroupCreateInput!`](/api-reference/inputs/permission-group-create-input.mdx))

Input fields to create permission group.

### Type

#### [`PermissionGroupCreate`](/api-reference/objects/permission-group-create.mdx)

Create new permission group.
