---
id: product-type-create
title: productTypeCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new product type.

```graphql
productTypeCreate(
  input: ProductTypeInput!
): ProductTypeCreate

```

### Arguments

#### `input` ([`ProductTypeInput!`](/api-reference/inputs/product-type-input.mdx))

Fields required to create a product type.

### Type

#### [`ProductTypeCreate`](/api-reference/objects/product-type-create.mdx)

Creates a new product type.
