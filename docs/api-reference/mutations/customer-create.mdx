---
id: customer-create
title: customerCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates a new customer.

```graphql
customerCreate(
  input: UserCreateInput!
): CustomerCreate

```

### Arguments

#### `input` ([`UserCreateInput!`](/api-reference/inputs/user-create-input.mdx))

Fields required to create a customer.

### Type

#### [`CustomerCreate`](/api-reference/objects/customer-create.mdx)

Creates a new customer.
