---
id: checkout-billing-address-update
title: checkoutBillingAddressUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Update billing address in the existing checkout.

```graphql
checkoutBillingAddressUpdate(
  billingAddress: AddressInput!
  checkoutId: ID!
): CheckoutBillingAddressUpdate

```

### Arguments

#### `billingAddress` ([`AddressInput!`](/api-reference/inputs/address-input.mdx))

The billing address of the checkout.

#### `checkoutId` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the checkout.

### Type

#### [`CheckoutBillingAddressUpdate`](/api-reference/objects/checkout-billing-address-update.mdx)

Update billing address in the existing checkout.
