---
id: address-delete
title: addressDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes an address.

```graphql
addressDelete(
  id: ID!
): AddressDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of the address to delete.

### Type

#### [`AddressDelete`](/api-reference/objects/address-delete.mdx)

Deletes an address.
