---
id: app-delete-failed-installation
title: appDeleteFailedInstallation
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Delete failed installation.

```graphql
appDeleteFailedInstallation(
  id: ID!
): AppDeleteFailedInstallation

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of failed installation to delete.

### Type

#### [`AppDeleteFailedInstallation`](/api-reference/objects/app-delete-failed-installation.mdx)

Delete failed installation.
