---
id: confirm-account
title: confirmAccount
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Confirm user account with token sent by email during registration.

```graphql
confirmAccount(
  email: String!
  token: String!
): ConfirmAccount
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>confirmAccount.<b>email</b></code>](#)<Bullet />[`String!`](../../api-reference/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> E-mail of the user performing account confirmation.

#### [<code style={{ fontWeight: 'normal' }}>confirmAccount.<b>token</b></code>](#)<Bullet />[`String!`](../../api-reference/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> A one-time token required to confirm the account.

### Type

#### [`ConfirmAccount`](../../api-reference/objects/confirm-account) <Badge class="secondary" text="object"/>

> Confirm user account with token sent by email during registration.
