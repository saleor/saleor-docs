---
id: shop-settings-update
title: shopSettingsUpdate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Updates shop settings.

```graphql
shopSettingsUpdate(
  input: ShopSettingsInput!
): ShopSettingsUpdate

```

### Arguments

#### `input` ([`ShopSettingsInput!`](/api-reference/inputs/shop-settings-input.mdx))

Fields required to update shop settings.

### Type

#### [`ShopSettingsUpdate`](/api-reference/objects/shop-settings-update.mdx)

Updates shop settings.
