---
id: checkout-line-delete
title: checkoutLineDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a CheckoutLine.

```graphql
checkoutLineDelete(
  checkoutId: ID!
  lineId: ID = 0
): CheckoutLineDelete

```

### Arguments

#### `checkoutId` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the checkout.

#### `lineId` ([`ID`](/api-reference/scalars/id.mdx))

ID of the checkout line to delete.

### Type

#### [`CheckoutLineDelete`](/api-reference/objects/checkout-line-delete.mdx)

Deletes a CheckoutLine.
