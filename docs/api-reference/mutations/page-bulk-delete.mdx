---
id: page-bulk-delete
title: pageBulkDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes pages.

```graphql
pageBulkDelete(
  ids: [ID]!
): PageBulkDelete

```

### Arguments

#### `ids` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of page IDs to delete.

### Type

#### [`PageBulkDelete`](/api-reference/objects/page-bulk-delete.mdx)

Deletes pages.
