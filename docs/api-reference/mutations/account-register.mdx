---
id: account-register
title: accountRegister
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Register a new user.

```graphql
accountRegister(
  input: AccountRegisterInput!
): AccountRegister

```

### Arguments

#### `input` ([`AccountRegisterInput!`](/api-reference/inputs/account-register-input.mdx))

Fields required to create a user.

### Type

#### [`AccountRegister`](/api-reference/objects/account-register.mdx)

Register a new user.
