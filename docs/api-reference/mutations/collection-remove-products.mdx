---
id: collection-remove-products
title: collectionRemoveProducts
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Remove products from a collection.

```graphql
collectionRemoveProducts(
  collectionId: ID!
  products: [ID]!
): CollectionRemoveProducts

```

### Arguments

#### `collectionId` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a collection.

#### `products` ([`[ID]!`](/api-reference/scalars/id.mdx))

List of product IDs.

### Type

#### [`CollectionRemoveProducts`](/api-reference/objects/collection-remove-products.mdx)

Remove products from a collection.
