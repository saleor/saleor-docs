---
id: sale-delete
title: saleDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a sale.

```graphql
saleDelete(
  id: ID!
): SaleDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a sale to delete.

### Type

#### [`SaleDelete`](/api-reference/objects/sale-delete.mdx)

Deletes a sale.
