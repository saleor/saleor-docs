---
id: product-variant-delete
title: productVariantDelete
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Deletes a product variant.

```graphql
productVariantDelete(
  id: ID!
): ProductVariantDelete

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of a product variant to delete.

### Type

#### [`ProductVariantDelete`](/api-reference/objects/product-variant-delete.mdx)

Deletes a product variant.
