---
api_reference: true
id: page-type-update
title: PageTypeUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates page type.

<Permissions
  permissions={"MANAGE_PAGE_TYPES_AND_ATTRIBUTES"}
  text={"Requires one of the following permissions"}
/>

```graphql
type PageTypeUpdate {
  pageErrors: [PageError!]! @deprecated
  errors: [PageError!]!
  pageType: PageType
}
```

### Fields

#### [<code>errors</code>](#)<Bullet />[<code>[PageError!]!</code>](/api-reference/pages/objects/page-error.mdx)

#### [<code>pageType</code>](#)<Bullet />[<code>PageType</code>](/api-reference/pages/objects/page-type.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>pageErrors</code>](#)<Bullet />[<code>[PageError!]!</code>](/api-reference/pages/objects/page-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`pageTypeUpdate`](/api-reference/pages/mutations/page-type-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
