---
api_reference: true
id: page-update
title: pageUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates an existing page.

<Permissions
  permissions={"MANAGE_PAGES"}
  text={"Requires one of the following permissions"}
/>

```graphql
pageUpdate(
  id: ID!
  input: PageInput!
): PageUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type PageUpdate {
  pageErrors: [PageError!]! @deprecated
  errors: [PageError!]!
  page: Page
}
```

</Details>

### Arguments

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

ID of a page to update.

#### [<code>input</code>](#input)<Bullet />[<code>PageInput!</code>](/api-reference/pages/inputs/page-input.mdx) \{#input\}

Fields required to update a page.

### Type

#### [<code>PageUpdate</code>](/api-reference/pages/objects/page-update.mdx)

Updates an existing page.

<Permissions
  permissions={"MANAGE_PAGES"}
  text={"Requires one of the following permissions"}
/>
