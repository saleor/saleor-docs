---
api_reference: true
id: transaction-process-session
title: TransactionProcessSession
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Event sent when user has additional payment action to process.

```graphql
type TransactionProcessSession implements Event {
  issuedAt: DateTime
  version: String
  issuingPrincipal: IssuingPrincipal
  recipient: App
  transaction: TransactionItem!
  sourceObject: OrderOrCheckout!
  data: JSON
  merchantReference: String!
  customerIpAddress: String
  action: TransactionProcessAction!
}
```

### Fields

#### [<code>issuedAt</code>](#issued-at)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx) \{#issued-at\}

Time of the event.

#### [<code>version</code>](#version)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#version\}

Saleor version that triggered the event.

#### [<code>issuingPrincipal</code>](#issuing-principal)<Bullet />[<code>IssuingPrincipal</code>](/api-reference/miscellaneous/unions/issuing-principal.mdx) \{#issuing-principal\}

The user or application that triggered the event.

#### [<code>recipient</code>](#recipient)<Bullet />[<code>App</code>](/api-reference/apps/objects/app.mdx) \{#recipient\}

The application receiving the webhook.

#### [<code>transaction</code>](#transaction)<Bullet />[<code>TransactionItem!</code>](/api-reference/payments/objects/transaction-item.mdx) \{#transaction\}

Look up a transaction.

#### [<code>sourceObject</code>](#source-object)<Bullet />[<code>OrderOrCheckout!</code>](/api-reference/miscellaneous/unions/order-or-checkout.mdx) \{#source-object\}

Checkout or order

#### [<code>data</code>](#data)<Bullet />[<code>JSON</code>](/api-reference/miscellaneous/scalars/json.mdx) \{#data\}

Payment gateway data in JSON format, received from storefront.

#### [<code>merchantReference</code>](#merchant-reference)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#merchant-reference\}

Merchant reference assigned to this payment.

#### [<code>customerIpAddress</code>](#customer-ip-address)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#customer-ip-address\}

The customer's IP address. If not provided as a parameter in the mutation, Saleor will try to determine the customer's IP address on its own.

#### [<code>action</code>](#action)<Bullet />[<code>TransactionProcessAction!</code>](/api-reference/payments/objects/transaction-process-action.mdx) \{#action\}

Action to proceed for the transaction

### Interfaces

#### [<code>Event</code>](/api-reference/miscellaneous/interfaces/event.mdx)
