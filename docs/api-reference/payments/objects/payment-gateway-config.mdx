---
id: payment-gateway-config
title: PaymentGatewayConfig
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
type PaymentGatewayConfig {
  id: String!
  data: JSON
  errors: [PaymentGatewayConfigError!]
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>PaymentGatewayConfig.<b>id</b></code>](#)<Bullet />[`String!`](../../../api-reference/miscellaneous/scalars/string) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/>

> The app identifier.

#### [<code style={{ fontWeight: 'normal' }}>PaymentGatewayConfig.<b>data</b></code>](#)<Bullet />[`JSON`](../../../api-reference/miscellaneous/scalars/json) <Badge class="badge badge--secondary" text="scalar"/>

> The JSON data required to initialize the payment gateway.

#### [<code style={{ fontWeight: 'normal' }}>PaymentGatewayConfig.<b>errors</b></code>](#)<Bullet />[`[PaymentGatewayConfigError!]`](../../../api-reference/payments/objects/payment-gateway-config-error) <Badge class="badge badge--secondary" text="list"/> <Badge class="badge badge--secondary" text="object"/>

>

### Member of

[`PaymentGatewayInitialize`](../../../api-reference/payments/objects/payment-gateway-initialize) <Badge class="badge badge--secondary" text="object"/>
