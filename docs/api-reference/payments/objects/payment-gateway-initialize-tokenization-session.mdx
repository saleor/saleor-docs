---
api_reference: true
id: payment-gateway-initialize-tokenization-session
title: PaymentGatewayInitializeTokenizationSession
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Event sent to initialize a new session in payment gateway to store the payment method.

```graphql
type PaymentGatewayInitializeTokenizationSession implements Event {
  issuedAt: DateTime
  version: String
  issuingPrincipal: IssuingPrincipal
  recipient: App
  user: User!
  channel: Channel!
  data: JSON
}
```

### Fields

#### [<code>issuedAt</code>](#issued-at)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx) \{#issued-at\}

Time of the event.

#### [<code>version</code>](#version)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#version\}

Saleor version that triggered the event.

#### [<code>issuingPrincipal</code>](#issuing-principal)<Bullet />[<code>IssuingPrincipal</code>](/api-reference/miscellaneous/unions/issuing-principal.mdx) \{#issuing-principal\}

The user or application that triggered the event.

#### [<code>recipient</code>](#recipient)<Bullet />[<code>App</code>](/api-reference/apps/objects/app.mdx) \{#recipient\}

The application receiving the webhook.

#### [<code>user</code>](#user)<Bullet />[<code>User!</code>](/api-reference/users/objects/user.mdx) \{#user\}

The user related to the requested action.

#### [<code>channel</code>](#channel)<Bullet />[<code>Channel!</code>](/api-reference/channels/objects/channel.mdx) \{#channel\}

Channel related to the requested action.

#### [<code>data</code>](#data)<Bullet />[<code>JSON</code>](/api-reference/miscellaneous/scalars/json.mdx) \{#data\}

Payment gateway data in JSON format, received from storefront.

### Interfaces

#### [<code>Event</code>](/api-reference/miscellaneous/interfaces/event.mdx)
