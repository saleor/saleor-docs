---
api_reference: true
id: transaction
title: Transaction
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

An object representing a single payment.

```graphql
type Transaction implements Node {
  id: ID!
  created: DateTime!
  payment: Payment!
  token: String!
  kind: TransactionKind!
  isSuccess: Boolean!
  error: String
  gatewayResponse: JSONString! @deprecated
  amount: Money
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the transaction.

#### [<code>created</code>](#)<Bullet />[<code>DateTime!</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

Date and time at which transaction was created.

#### [<code>payment</code>](#)<Bullet />[<code>Payment!</code>](/api-reference/payments/objects/payment.mdx)

Determines the payment associated with a transaction.

#### [<code>token</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Unique token associated with a transaction.

#### [<code>kind</code>](#)<Bullet />[<code>TransactionKind!</code>](/api-reference/payments/enums/transaction-kind.mdx)

Determines the type of transaction.

#### [<code>isSuccess</code>](#)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Determines if the transaction was successful.

#### [<code>error</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Error associated with transaction, if any.

#### [<code>amount</code>](#)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx)

Total amount of the transaction.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>gatewayResponse</code>](#)<Bullet />[<code>JSONString!</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx)

:::warning[DEPRECATED]

This field is a part of a legacy Payments API. Please use apps instead.

:::

Response returned by payment gateway.

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`Payment`](/api-reference/payments/objects/payment.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
