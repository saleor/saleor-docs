---
api_reference: true
id: payment-method-initialize-tokenization-session
title: PaymentMethodInitializeTokenizationSession
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

Event sent when user requests a tokenization of payment method.

```graphql
type PaymentMethodInitializeTokenizationSession implements Event {
  issuedAt: DateTime
  version: String
  issuingPrincipal: IssuingPrincipal
  recipient: App
  user: User!
  channel: Channel!
  data: JSON
  paymentFlowToSupport: TokenizedPaymentFlowEnum!
}
```

### Fields

#### [`issuedAt`](#)<Bullet />[`DateTime`](/api-reference/miscellaneous/scalars/date-time.mdx)

Time of the event.

#### [`version`](#)<Bullet />[`String`](/api-reference/miscellaneous/scalars/string.mdx)

Saleor version that triggered the event.

#### [`issuingPrincipal`](#)<Bullet />[`IssuingPrincipal`](/api-reference/miscellaneous/unions/issuing-principal.mdx)

The user or application that triggered the event.

#### [`recipient`](#)<Bullet />[`App`](/api-reference/apps/objects/app.mdx)

The application receiving the webhook.

#### [`user`](#)<Bullet />[`User!`](/api-reference/users/objects/user.mdx)

The user related to the requested action.

#### [`channel`](#)<Bullet />[`Channel!`](/api-reference/channels/objects/channel.mdx)

Channel related to the requested action.

#### [`data`](#)<Bullet />[`JSON`](/api-reference/miscellaneous/scalars/json.mdx)

Payment gateway data in JSON format, received from storefront.

#### [`paymentFlowToSupport`](#)<Bullet />[`TokenizedPaymentFlowEnum!`](/api-reference/payments/enums/tokenized-payment-flow-enum.mdx)

The payment flow that the tokenized payment method should support.

### Interfaces

#### [`Event`](/api-reference/miscellaneous/interfaces/event.mdx)
