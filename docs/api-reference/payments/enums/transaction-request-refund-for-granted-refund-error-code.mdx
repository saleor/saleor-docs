---
api_reference: true
id: transaction-request-refund-for-granted-refund-error-code
title: TransactionRequestRefundForGrantedRefundErrorCode
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>INVALID</code>](#)

#### [<code>GRAPHQL_ERROR</code>](#)

#### [<code>NOT_FOUND</code>](#)

#### [<code>AMOUNT_GREATER_THAN_AVAILABLE</code>](#)

#### [<code>MISSING_TRANSACTION_ACTION_REQUEST_WEBHOOK</code>](#)

#### [<code>REFUND_ALREADY_PROCESSED</code>](#)

#### [<code>REFUND_IS_PENDING</code>](#)

### Member Of

[`TransactionRequestRefundForGrantedRefundError`](/api-reference/payments/objects/transaction-request-refund-for-granted-refund-error.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
