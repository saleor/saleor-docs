---
api_reference: true
id: payment-method-type-enum
title: PaymentMethodTypeEnum
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

Represents possible payment method types.

    The following types are possible:
    CARD - represents a card payment method.
    OTHER - represents any payment method that is not a card payment.

```graphql
enum PaymentMethodTypeEnum {
  CARD
  OTHER
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>PaymentMethodTypeEnum.<b>CARD</b></code>](#)

#### [<code style={{ fontWeight: 'normal' }}>PaymentMethodTypeEnum.<b>OTHER</b></code>](#)

### Member Of

[`PaymentMethodTypeEnumFilterInput`](../../../api-reference/miscellaneous/inputs/payment-method-type-enum-filter-input.mdx) <Badge class="badge badge--secondary" text="input"/>
