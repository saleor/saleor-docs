---
id: transaction-action-enum
title: TransactionActionEnum
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Represents possible actions on payment transaction.

    The following actions are possible:
    CHARGE - Represents the charge action.
    REFUND - Represents a refund action.
    VOID - Represents a void action. This field will be removed
    in Saleor 3.14 (Preview Feature). Use `CANCEL` instead.
    CANCEL - Represents a cancel action.

<Badge text="Added in Saleor 3.12" class="secondary margin-bottom--sm" />

```graphql
enum TransactionActionEnum {
  CHARGE
  REFUND
  VOID
  CANCEL
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>TransactionActionEnum.<b>CHARGE</b></code>](#)

>

#### [<code style={{ fontWeight: 'normal' }}>TransactionActionEnum.<b>REFUND</b></code>](#)

>

#### [<code style={{ fontWeight: 'normal' }}>TransactionActionEnum.<b>VOID</b></code>](#)

>

#### [<code style={{ fontWeight: 'normal' }}>TransactionActionEnum.<b>CANCEL</b></code>](#)

>

### Member of

[`TransactionAction`](../../../api-reference/payments/objects/transaction-action) <Badge class="secondary" text="object"/><Bullet />[`TransactionCreateInput`](../../../api-reference/payments/inputs/transaction-create-input) <Badge class="secondary" text="input"/><Bullet />[`transactionEventReport`](../../../api-reference/payments/mutations/transaction-event-report) <Badge class="secondary" text="mutation"/><Bullet />[`TransactionItem`](../../../api-reference/payments/objects/transaction-item) <Badge class="secondary" text="object"/><Bullet />[`transactionRequestAction`](../../../api-reference/payments/mutations/transaction-request-action) <Badge class="secondary" text="mutation"/><Bullet />[`TransactionUpdateInput`](../../../api-reference/payments/inputs/transaction-update-input) <Badge class="secondary" text="input"/>
