---
id: transaction-event-input
title: TransactionEventInput
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
input TransactionEventInput {
  status: TransactionStatus
  reference: String
  pspReference: String
  name: String
  message: String
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>TransactionEventInput.<b>status</b></code>](#)<Bullet />[`TransactionStatus`](../../../api-reference/payments/enums/transaction-status) <Badge class="secondary" text="enum"/>

> Current status of the payment transaction.
>
> DEPRECATED: this field will be removed in Saleor 3.15 (Preview Feature). Status will be calculated by Saleor.

#### [<code style={{ fontWeight: 'normal' }}>TransactionEventInput.<b>reference</b></code>](#)<Bullet />[`String`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> Reference of the transaction.
>
> DEPRECATED: this field will be removed in Saleor 3.15 (Preview Feature). Use `pspReference` instead.

#### [<code style={{ fontWeight: 'normal' }}>TransactionEventInput.<b>pspReference</b></code>](#)<Bullet />[`String`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> PSP Reference related to this action.
>
> <Badge text="Added in Saleor 3.13" class="secondary margin-bottom--sm" />

#### [<code style={{ fontWeight: 'normal' }}>TransactionEventInput.<b>name</b></code>](#)<Bullet />[`String`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> Name of the transaction.
>
> DEPRECATED: this field will be removed in Saleor 3.15 (Preview Feature). Use `message` instead. `name` field will be added to `message`.

#### [<code style={{ fontWeight: 'normal' }}>TransactionEventInput.<b>message</b></code>](#)<Bullet />[`String`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

> The message related to the event.
>
> <Badge text="Added in Saleor 3.13" class="secondary margin-bottom--sm" />

### Member of

[`transactionCreate`](../../../api-reference/payments/mutations/transaction-create) <Badge class="secondary" text="mutation"/><Bullet />[`transactionUpdate`](../../../api-reference/payments/mutations/transaction-update) <Badge class="secondary" text="mutation"/>
