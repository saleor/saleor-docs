---
api_reference: true
id: transaction-request-refund-for-granted-refund
title: transactionRequestRefundForGrantedRefund
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Request a refund for payment transaction based on granted refund.

<Permissions
  permissions={"HANDLE_PAYMENTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
transactionRequestRefundForGrantedRefund(
  grantedRefundId: ID!
  id: ID
  token: UUID
): TransactionRequestRefundForGrantedRefund
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type TransactionRequestRefundForGrantedRefund {
  transaction: TransactionItem
  errors: [TransactionRequestRefundForGrantedRefundError!]!
}
```

</Details>

### Arguments

#### [<code>grantedRefundId</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the granted refund.

#### [<code>id</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the transaction. One of field id or token is required, if `transactionItem` is not already assigned to the `orderGrantedRefund`. If `transactionItem` is already assigned to the grantedRefund the field will be ignored.

#### [<code>token</code>](#)<Bullet />[<code>UUID</code>](/api-reference/miscellaneous/scalars/uuid.mdx)

The token of the transaction. One of field id or token is required, if `transactionItem` is not already assigned to the `orderGrantedRefund`. If `transactionItem` is already assigned to the grantedRefund the field will be ignored.

### Type

#### [<code>TransactionRequestRefundForGrantedRefund</code>](/api-reference/payments/objects/transaction-request-refund-for-granted-refund.mdx)

Request a refund for payment transaction based on granted refund.

<Permissions
  permissions={"HANDLE_PAYMENTS"}
  text={"Requires one of the following permissions"}
/>
