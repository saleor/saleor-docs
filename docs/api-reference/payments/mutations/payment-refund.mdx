---
api_reference: true
id: payment-refund
title: paymentRefund
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Refunds the captured payment amount.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
paymentRefund(
  amount: PositiveDecimal
  paymentId: ID!
): PaymentRefund
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type PaymentRefund {
  payment: Payment
  paymentErrors: [PaymentError!]! @deprecated
  errors: [PaymentError!]!
}
```

</Details>

### Arguments

#### [<code>amount</code>](#amount)<Bullet />[<code>PositiveDecimal</code>](/api-reference/miscellaneous/scalars/positive-decimal.mdx) \{#amount\}

Transaction amount.

#### [<code>paymentId</code>](#payment-id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#payment-id\}

Payment ID.

### Type

#### [<code>PaymentRefund</code>](/api-reference/payments/objects/payment-refund.mdx)

Refunds the captured payment amount.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>
