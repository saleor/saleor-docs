---
id: customers
title: customers
---

List of the shop's customers.

```graphql
customers(
  filter: CustomerFilterInput
  sortBy: UserSortingInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): UserCountableConnection

```

### Arguments

#### `filter` ([`CustomerFilterInput`](/api-reference/inputs/customer-filter-input.mdx))

Filtering options for customers.

#### `sortBy` ([`UserSortingInput`](/api-reference/inputs/user-sorting-input.mdx))

Sort customers.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`UserCountableConnection`](/api-reference/objects/user-countable-connection.mdx)
