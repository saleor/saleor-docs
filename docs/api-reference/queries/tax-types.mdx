---
id: tax-types
title: taxTypes
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

List of all tax rates available from tax gateway.

```graphql
taxTypes: [TaxType]

```

### Type

#### [`TaxType`](/api-reference/objects/tax-type.mdx)

Representation of tax types fetched from tax gateway.
