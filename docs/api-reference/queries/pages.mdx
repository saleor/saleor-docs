---
id: pages
title: pages
---

List of the shop's pages.

```graphql
pages(
  sortBy: PageSortingInput
  filter: PageFilterInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): PageCountableConnection

```

### Arguments

#### `sortBy` ([`PageSortingInput`](/api-reference/inputs/page-sorting-input.mdx))

Sort pages.

#### `filter` ([`PageFilterInput`](/api-reference/inputs/page-filter-input.mdx))

Filtering options for pages.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`PageCountableConnection`](/api-reference/objects/page-countable-connection.mdx)
