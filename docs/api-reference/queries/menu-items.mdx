---
id: menu-items
title: menuItems
---

List of the storefronts's menu items.

```graphql
menuItems(
  channel: String
  sortBy: MenuItemSortingInput
  filter: MenuItemFilterInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): MenuItemCountableConnection

```

### Arguments

#### `channel` ([`String`](/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `sortBy` ([`MenuItemSortingInput`](/api-reference/inputs/menu-item-sorting-input.mdx))

Sort menus items.

#### `filter` ([`MenuItemFilterInput`](/api-reference/inputs/menu-item-filter-input.mdx))

Filtering options for menu items.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`MenuItemCountableConnection`](/api-reference/objects/menu-item-countable-connection.mdx)
