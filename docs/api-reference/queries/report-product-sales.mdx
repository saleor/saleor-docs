---
id: report-product-sales
title: reportProductSales
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

List of top selling products.

```graphql
reportProductSales(
  period: ReportingPeriod!
  channel: String!
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): ProductVariantCountableConnection

```

### Arguments

#### `period` ([`ReportingPeriod!`](/api-reference/enums/reporting-period.mdx))

Span of time.

#### `channel` ([`String!`](/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`ProductVariantCountableConnection`](/api-reference/objects/product-variant-countable-connection.mdx)
