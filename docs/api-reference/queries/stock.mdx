---
id: stock
title: stock
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Look up a stock by ID

```graphql
stock(
  id: ID!
): Stock

```

### Arguments

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

ID of an warehouse

### Type

#### [`Stock`](/api-reference/objects/stock.mdx)

Represents stock.
