---
id: translations
title: translations
---

Returns a list of all translatable items of a given kind.

```graphql
translations(
  kind: TranslatableKinds!
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): TranslatableItemConnection

```

### Arguments

#### `kind` ([`TranslatableKinds!`](/api-reference/enums/translatable-kinds.mdx))

Kind of objects to retrieve.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`TranslatableItemConnection`](/api-reference/objects/translatable-item-connection.mdx)
