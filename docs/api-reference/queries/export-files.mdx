---
id: export-files
title: exportFiles
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

List of export files.

```graphql
exportFiles(
  filter: ExportFileFilterInput
  sortBy: ExportFileSortingInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): ExportFileCountableConnection

```

### Arguments

#### `filter` ([`ExportFileFilterInput`](/api-reference/inputs/export-file-filter-input.mdx))

Filtering options for export files.

#### `sortBy` ([`ExportFileSortingInput`](/api-reference/inputs/export-file-sorting-input.mdx))

Sort export files.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`ExportFileCountableConnection`](/api-reference/objects/export-file-countable-connection.mdx)
