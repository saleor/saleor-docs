---
id: order-settings
title: orderSettings
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Order related settings from site settings.

```graphql
orderSettings: OrderSettings

```

### Type

#### [`OrderSettings`](/api-reference/objects/order-settings.mdx)

Order related settings from site settings.
