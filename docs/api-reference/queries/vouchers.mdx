---
id: vouchers
title: vouchers
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

List of the shop's vouchers.

```graphql
vouchers(
  filter: VoucherFilterInput
  sortBy: VoucherSortingInput
  query: String
  channel: String
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): VoucherCountableConnection

```

### Arguments

#### `filter` ([`VoucherFilterInput`](/api-reference/inputs/voucher-filter-input.mdx))

Filtering options for vouchers.

#### `sortBy` ([`VoucherSortingInput`](/api-reference/inputs/voucher-sorting-input.mdx))

Sort voucher.

#### `query` ([`String`](/api-reference/scalars/string.mdx))

Search vouchers by name or code.

#### `channel` ([`String`](/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`VoucherCountableConnection`](/api-reference/objects/voucher-countable-connection.mdx)
