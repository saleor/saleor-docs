---
id: warehouses
title: warehouses
---

List of warehouses.

```graphql
warehouses(
  filter: WarehouseFilterInput
  sortBy: WarehouseSortingInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): WarehouseCountableConnection

```

### Arguments

#### `filter` ([`WarehouseFilterInput`](/api-reference/inputs/warehouse-filter-input.mdx))

#### `sortBy` ([`WarehouseSortingInput`](/api-reference/inputs/warehouse-sorting-input.mdx))

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`WarehouseCountableConnection`](/api-reference/objects/warehouse-countable-connection.mdx)
