---
id: payments
title: payments
---

List of payments.

```graphql
payments(
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): PaymentCountableConnection

```

### Arguments

#### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`PaymentCountableConnection`](/api-reference/objects/payment-countable-connection.mdx)
