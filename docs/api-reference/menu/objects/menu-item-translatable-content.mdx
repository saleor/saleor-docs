---
api_reference: true
id: menu-item-translatable-content
title: MenuItemTranslatableContent
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents menu item's original translatable fields and related translations.

```graphql
type MenuItemTranslatableContent implements Node {
  id: ID!
  menuItemId: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): MenuItemTranslation
  menuItem: MenuItem @deprecated
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the menu item translatable content.

#### [<code>menuItemId</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the menu item to translate.

#### [<code>name</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of the menu item to translate.

#### [<code>translation</code>](#)<Bullet />[<code>MenuItemTranslation</code>](/api-reference/menu/objects/menu-item-translation.mdx)

Returns translated menu item fields for the given language code.

##### [<code>languageCode</code>](#)<Bullet />[<code>LanguageCodeEnum!</code>](/api-reference/miscellaneous/enums/language-code-enum.mdx)

A language code to return the translation for menu item.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>menuItem</code>](#)<Bullet />[<code>MenuItem</code>](/api-reference/menu/objects/menu-item.mdx)

:::warning[DEPRECATED]

Get model fields from the root level queries.

:::

Represents a single item of the related menu. Can store categories, collection or pages.

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`MenuItemTranslation`](/api-reference/menu/objects/menu-item-translation.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>

### Implemented By

[`TranslatableItem`](/api-reference/miscellaneous/unions/translatable-item.mdx) <Badge class="badge badge--secondary badge--relation" text="union"/>
