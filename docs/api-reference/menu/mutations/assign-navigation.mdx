---
api_reference: true
id: assign-navigation
title: assignNavigation
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Assigns storefront's navigation menus.

<Permissions
  permissions={"MANAGE_MENUS,MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>

```graphql
assignNavigation(
  menu: ID
  navigationType: NavigationType!
): AssignNavigation
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type AssignNavigation {
  menu: Menu
  menuErrors: [MenuError!]! @deprecated
  errors: [MenuError!]!
}
```

</Details>

### Arguments

#### [<code>menu</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the menu.

#### [<code>navigationType</code>](#)<Bullet />[<code>NavigationType!</code>](/api-reference/miscellaneous/enums/navigation-type.mdx)

Type of the navigation bar to assign the menu to.

### Type

#### [<code>AssignNavigation</code>](/api-reference/menu/objects/assign-navigation.mdx)

Assigns storefront's navigation menus.

<Permissions
  permissions={"MANAGE_MENUS,MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>
