---
id: file-upload
title: FileUpload
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Upload a file. This mutation must be sent as a `multipart` request. More detailed specs of the upload format can be found here: https://github.com/jaydenseric/graphql-multipart-request-spec

<Permissions
  permissions={"AUTHENTICATED_APP,AUTHENTICATED_STAFF_USER"}
  text={"Requires one of the following permissions"}
/>

```graphql
type FileUpload {
  uploadedFile: File
  uploadErrors: [UploadError!]! @deprecated
  errors: [UploadError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>FileUpload.<b>uploadedFile</b></code>](#)<Bullet />[`File`](../../../api-reference/miscellaneous/objects/file) <Badge class="secondary" text="object"/>

>

#### [<code style={{ fontWeight: 'normal' }}>FileUpload.<b>uploadErrors</b></code>](#)<Bullet />[`[UploadError!]!`](../../../api-reference/miscellaneous/objects/upload-error) <Badge class="secondary" text="deprecated"/> <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

> <Badge
>   class="warning"
>   text="DEPRECATED: This field will be removed in Saleor 4.0. Use `errors` field instead."
> />

#### [<code style={{ fontWeight: 'normal' }}>FileUpload.<b>errors</b></code>](#)<Bullet />[`[UploadError!]!`](../../../api-reference/miscellaneous/objects/upload-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

>

### Returned by

[`fileUpload`](../../../api-reference/miscellaneous/mutations/file-upload) <Badge class="secondary" text="mutation"/>

import Permissions from "@site/components/Permissions";
