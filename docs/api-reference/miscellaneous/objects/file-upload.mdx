---
api_reference: true
id: file-upload
title: FileUpload
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Upload a file. This mutation must be sent as a `multipart` request. More detailed specs of the upload format can be found here: https://github.com/jaydenseric/graphql-multipart-request-spec

<Permissions
  permissions={"AUTHENTICATED_APP,AUTHENTICATED_STAFF_USER"}
  text={"Requires one of the following permissions"}
/>

```graphql
type FileUpload {
  uploadedFile: File
  uploadErrors: [UploadError!]! @deprecated
  errors: [UploadError!]!
}
```

### Fields

#### [<code>uploadedFile</code>](#)<Bullet />[<code>File</code>](/api-reference/miscellaneous/objects/file.mdx)

#### [<code>errors</code>](#)<Bullet />[<code>[UploadError!]!</code>](/api-reference/miscellaneous/objects/upload-error.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>uploadErrors</code>](#)<Bullet />[<code>[UploadError!]!</code>](/api-reference/miscellaneous/objects/upload-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`fileUpload`](/api-reference/miscellaneous/mutations/file-upload.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
