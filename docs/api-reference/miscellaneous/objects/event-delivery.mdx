---
api_reference: true
id: event-delivery
title: EventDelivery
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Event delivery.

```graphql
type EventDelivery implements Node {
  id: ID!
  createdAt: DateTime!
  status: EventDeliveryStatusEnum!
  eventType: WebhookEventTypeEnum!
  attempts(
    sortBy: EventDeliveryAttemptSortingInput
    before: String
    after: String
    first: Int
    last: Int
  ): EventDeliveryAttemptCountableConnection
  payload: String
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of an event delivery.

#### [<code>createdAt</code>](#)<Bullet />[<code>DateTime!</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

Creation time of an event delivery.

#### [<code>status</code>](#)<Bullet />[<code>EventDeliveryStatusEnum!</code>](/api-reference/webhooks/enums/event-delivery-status-enum.mdx)

Event delivery status.

#### [<code>eventType</code>](#)<Bullet />[<code>WebhookEventTypeEnum!</code>](/api-reference/webhooks/enums/webhook-event-type-enum.mdx)

Webhook event type.

#### [<code>attempts</code>](#)<Bullet />[<code>EventDeliveryAttemptCountableConnection</code>](/api-reference/miscellaneous/objects/event-delivery-attempt-countable-connection.mdx)

Event delivery attempts.

##### [<code>sortBy</code>](#)<Bullet />[<code>EventDeliveryAttemptSortingInput</code>](/api-reference/webhooks/inputs/event-delivery-attempt-sorting-input.mdx)

Event delivery sorter

##### [<code>before</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come before the specified cursor.

##### [<code>after</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come after the specified cursor.

##### [<code>first</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

##### [<code>last</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>payload</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Event payload.

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`EventDeliveryCountableEdge`](/api-reference/miscellaneous/objects/event-delivery-countable-edge.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`EventDeliveryRetry`](/api-reference/webhooks/objects/event-delivery-retry.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`WebhookTrigger`](/api-reference/webhooks/objects/webhook-trigger.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
