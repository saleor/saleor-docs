---
api_reference: true
id: promotion-rule-update-error
title: PromotionRuleUpdateError
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
type PromotionRuleUpdateError {
  field: String
  message: String
  code: PromotionRuleUpdateErrorCode!
  channels: [ID!]
  giftsLimit: Int
  giftsLimitExceedBy: Int
}
```

### Fields

#### [<code>field</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of a field that caused the error. A value of `null` indicates that the error isn't associated with a particular field.

#### [<code>message</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The error message.

#### [<code>code</code>](#)<Bullet />[<code>PromotionRuleUpdateErrorCode!</code>](/api-reference/miscellaneous/enums/promotion-rule-update-error-code.mdx)

The error code.

#### [<code>channels</code>](#)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx)

List of channel IDs which causes the error.

#### [<code>giftsLimit</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Limit of gifts assigned to promotion rule.

#### [<code>giftsLimitExceedBy</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Number of gifts defined for this promotion rule exceeding the limit.

### Member Of

[`PromotionRuleUpdate`](/api-reference/discounts/objects/promotion-rule-update.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
