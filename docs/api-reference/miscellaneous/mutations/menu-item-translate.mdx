---
id: menu-item-translate
title: menuItemTranslate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Creates/updates translations for a menu item.

<Permissions
  permissions={"MANAGE_TRANSLATIONS"}
  text={"Requires one of the following permissions"}
/>

```graphql
menuItemTranslate(
  id: ID!
  input: NameTranslationInput!
  languageCode: LanguageCodeEnum!
): MenuItemTranslate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>menuItemTranslate.<b>id</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> MenuItem ID or MenuItemTranslatableContent ID.

#### [<code style={{ fontWeight: 'normal' }}>menuItemTranslate.<b>input</b></code>](#)<Bullet />[`NameTranslationInput!`](../../../api-reference/miscellaneous/inputs/name-translation-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/>

>

#### [<code style={{ fontWeight: 'normal' }}>menuItemTranslate.<b>languageCode</b></code>](#)<Bullet />[`LanguageCodeEnum!`](../../../api-reference/miscellaneous/enums/language-code-enum) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="enum"/>

> Translation language code.

### Type

#### [`MenuItemTranslate`](../../../api-reference/miscellaneous/objects/menu-item-translate) <Badge class="secondary" text="object"/>

> Creates/updates translations for a menu item.

<Permissions
  permissions={"MANAGE_TRANSLATIONS"}
  text={"Requires one of the following permissions"}
/>

import Permissions from "@site/components/Permissions";
