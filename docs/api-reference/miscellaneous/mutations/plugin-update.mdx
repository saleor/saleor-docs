---
api_reference: true
id: plugin-update
title: pluginUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Update plugin configuration.

<Permissions
  permissions={"MANAGE_PLUGINS"}
  text={"Requires one of the following permissions"}
/>

```graphql
pluginUpdate(
  channelId: ID
  id: ID!
  input: PluginUpdateInput!
): PluginUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type PluginUpdate {
  plugin: Plugin
  pluginsErrors: [PluginError!]! @deprecated
  errors: [PluginError!]!
}
```

</Details>

### Arguments

#### [<code>channelId</code>](#channel-id)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#channel-id\}

ID of a channel for which the data should be modified.

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

ID of plugin to update.

#### [<code>input</code>](#input)<Bullet />[<code>PluginUpdateInput!</code>](/api-reference/miscellaneous/inputs/plugin-update-input.mdx) \{#input\}

Fields required to update a plugin configuration.

### Type

#### [<code>PluginUpdate</code>](/api-reference/miscellaneous/objects/plugin-update.mdx)

Update plugin configuration.

<Permissions
  permissions={"MANAGE_PLUGINS"}
  text={"Requires one of the following permissions"}
/>
