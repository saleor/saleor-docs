---
api_reference: true
id: card-payment-method-details-input
title: CardPaymentMethodDetailsInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input CardPaymentMethodDetailsInput {
  name: String!
  brand: String
  firstDigits: String
  lastDigits: String
  expMonth: Int
  expYear: Int
}
```

### Fields

#### [<code>name</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of the payment method used for the transaction. Max length is 256 characters.

#### [<code>brand</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Brand of the payment method used for the transaction. Max length is 40 characters.

#### [<code>firstDigits</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

First digits of the card used for the transaction. Max length is 4 characters.

#### [<code>lastDigits</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Last digits of the card used for the transaction. Max length is 4 characters.

#### [<code>expMonth</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Expiration month of the card used for the transaction. Value must be between 1 and 12.

#### [<code>expYear</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Expiration year of the card used for the transaction. Value must be between 2000 and 9999.

### Member Of

[`PaymentMethodDetailsInput`](/api-reference/miscellaneous/inputs/payment-method-details-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
