---
api_reference: true
id: webhook-events-info
title: webhookEventsInfo
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Webhook events triggered by a specific location.

```graphql
directive @webhookEventsInfo(
  asyncEvents: [WebhookEventTypeAsyncEnum!]!
  syncEvents: [WebhookEventTypeSyncEnum!]!
) on FIELD | FIELD_DEFINITION | INPUT_OBJECT | OBJECT
```

### Arguments

#### [<code>asyncEvents</code>](#async-events)<Bullet />[<code>[WebhookEventTypeAsyncEnum!]!</code>](/api-reference/webhooks/enums/webhook-event-type-async-enum.mdx) \{#async-events\}

List of asynchronous webhook events triggered by a specific location.

#### [<code>syncEvents</code>](#sync-events)<Bullet />[<code>[WebhookEventTypeSyncEnum!]!</code>](/api-reference/webhooks/enums/webhook-event-type-sync-enum.mdx) \{#sync-events\}

List of synchronous webhook events triggered by a specific location.
