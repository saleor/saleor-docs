---
api_reference: true
id: export-files
title: exportFiles
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

List of export files.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
exportFiles(
  filter: ExportFileFilterInput
  sortBy: ExportFileSortingInput
  before: String
  after: String
  first: Int
  last: Int
): ExportFileCountableConnection
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ExportFileCountableConnection {
  pageInfo: PageInfo!
  edges: [ExportFileCountableEdge!]!
  totalCount: Int
}
```

</Details>

### Arguments

#### [<code>filter</code>](#)<Bullet />[<code>ExportFileFilterInput</code>](/api-reference/miscellaneous/inputs/export-file-filter-input.mdx)

Filtering options for export files.

#### [<code>sortBy</code>](#)<Bullet />[<code>ExportFileSortingInput</code>](/api-reference/miscellaneous/inputs/export-file-sorting-input.mdx)

Sort export files.

#### [<code>before</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come before the specified cursor.

#### [<code>after</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Return the elements in the list that come after the specified cursor.

#### [<code>first</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>last</code>](#)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx)

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

### Type

#### [<code>ExportFileCountableConnection</code>](/api-reference/miscellaneous/objects/export-file-countable-connection.mdx)
