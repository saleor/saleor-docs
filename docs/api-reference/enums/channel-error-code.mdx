---
id: channel-error-code
title: ChannelErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum ChannelErrorCode {
  ALREADY_EXISTS
  GRAPHQL_ERROR
  INVALID
  NOT_FOUND
  REQUIRED
  UNIQUE
  CHANNEL_TARGET_ID_MUST_BE_DIFFERENT
  CHANNELS_CURRENCY_MUST_BE_THE_SAME
  CHANNEL_WITH_ORDERS
}
```

### Values

#### `ALREADY_EXISTS`

#### `GRAPHQL_ERROR`

#### `INVALID`

#### `NOT_FOUND`

#### `REQUIRED`

#### `UNIQUE`

#### `CHANNEL_TARGET_ID_MUST_BE_DIFFERENT`

#### `CHANNELS_CURRENCY_MUST_BE_THE_SAME`

#### `CHANNEL_WITH_ORDERS`
