---
id: reporting-period
title: ReportingPeriod
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum ReportingPeriod {
  TODAY
  THIS_MONTH
}
```

### Values

#### `TODAY`

#### `THIS_MONTH`
