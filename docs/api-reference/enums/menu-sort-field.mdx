---
id: menu-sort-field
title: MenuSortField
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum MenuSortField {
  NAME
  ITEMS_COUNT
}
```

### Values

#### `NAME`

Sort menus by name.

#### `ITEMS_COUNT`

Sort menus by items count.
