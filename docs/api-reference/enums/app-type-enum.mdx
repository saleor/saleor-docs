---
id: app-type-enum
title: AppTypeEnum
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum AppTypeEnum {
  LOCAL
  THIRDPARTY
}
```

### Values

#### `LOCAL`

#### `THIRDPARTY`
