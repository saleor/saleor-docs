---
id: invoice-error-code
title: InvoiceErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum InvoiceErrorCode {
  REQUIRED
  NOT_READY
  URL_NOT_SET
  EMAIL_NOT_SET
  NUMBER_NOT_SET
  NOT_FOUND
  INVALID_STATUS
}
```

### Values

#### `REQUIRED`

#### `NOT_READY`

#### `URL_NOT_SET`

#### `EMAIL_NOT_SET`

#### `NUMBER_NOT_SET`

#### `NOT_FOUND`

#### `INVALID_STATUS`
