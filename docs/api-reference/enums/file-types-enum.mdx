---
id: file-types-enum
title: FileTypesEnum
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum FileTypesEnum {
  CSV
  XLSX
}
```

### Values

#### `CSV`

#### `XLSX`
