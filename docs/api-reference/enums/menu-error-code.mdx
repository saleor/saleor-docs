---
id: menu-error-code
title: MenuErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum MenuErrorCode {
  CANNOT_ASSIGN_NODE
  GRAPHQL_ERROR
  INVALID
  INVALID_MENU_ITEM
  NO_MENU_ITEM_PROVIDED
  NOT_FOUND
  REQUIRED
  TOO_MANY_MENU_ITEMS
  UNIQUE
}
```

### Values

#### `CANNOT_ASSIGN_NODE`

#### `GRAPHQL_ERROR`

#### `INVALID`

#### `INVALID_MENU_ITEM`

#### `NO_MENU_ITEM_PROVIDED`

#### `NOT_FOUND`

#### `REQUIRED`

#### `TOO_MANY_MENU_ITEMS`

#### `UNIQUE`
