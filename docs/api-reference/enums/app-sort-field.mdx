---
id: app-sort-field
title: AppSortField
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum AppSortField {
  NAME
  CREATION_DATE
}
```

### Values

#### `NAME`

Sort apps by name.

#### `CREATION_DATE`

Sort apps by creation date.
