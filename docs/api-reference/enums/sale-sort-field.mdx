---
id: sale-sort-field
title: SaleSortField
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
enum SaleSortField {
  NAME
  START_DATE
  END_DATE
  VALUE
  TYPE
}
```

### Values

#### `NAME`

Sort sales by name.

#### `START_DATE`

Sort sales by start date.

#### `END_DATE`

Sort sales by end date.

#### `VALUE`

Sort sales by value.

#### `TYPE`

Sort sales by type.
