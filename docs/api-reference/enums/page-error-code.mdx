---
id: page-error-code
title: PageErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum PageErrorCode {
  GRAPHQL_ERROR
  INVALID
  NOT_FOUND
  REQUIRED
  UNIQUE
  DUPLICATED_INPUT_ITEM
  ATTRIBUTE_ALREADY_ASSIGNED
}
```

### Values

#### `GRAPHQL_ERROR`

#### `INVALID`

#### `NOT_FOUND`

#### `REQUIRED`

#### `UNIQUE`

#### `DUPLICATED_INPUT_ITEM`

#### `ATTRIBUTE_ALREADY_ASSIGNED`
