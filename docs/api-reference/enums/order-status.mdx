---
id: order-status
title: OrderStatus
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum OrderStatus {
  DRAFT
  UNCONFIRMED
  UNFULFILLED
  PARTIALLY_FULFILLED
  FULFILLED
  CANCELED
}
```

### Values

#### `DRAFT`

Draft

#### `UNCONFIRMED`

Unconfirmed

#### `UNFULFILLED`

Unfulfilled

#### `PARTIALLY_FULFILLED`

Partially fulfilled

#### `FULFILLED`

Fulfilled

#### `CANCELED`

Canceled
