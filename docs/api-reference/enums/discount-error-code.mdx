---
id: discount-error-code
title: DiscountErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum DiscountErrorCode {
  ALREADY_EXISTS
  GRAPHQL_ERROR
  INVALID
  NOT_FOUND
  REQUIRED
  UNIQUE
  CANNOT_MANAGE_PRODUCT_WITHOUT_VARIANT
  DUPLICATED_INPUT_ITEM
}
```

### Values

#### `ALREADY_EXISTS`

#### `GRAPHQL_ERROR`

#### `INVALID`

#### `NOT_FOUND`

#### `REQUIRED`

#### `UNIQUE`

#### `CANNOT_MANAGE_PRODUCT_WITHOUT_VARIANT`

#### `DUPLICATED_INPUT_ITEM`
