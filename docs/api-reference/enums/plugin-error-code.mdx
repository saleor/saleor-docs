---
id: plugin-error-code
title: PluginErrorCode
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

An enumeration.

```graphql
enum PluginErrorCode {
  GRAPHQL_ERROR
  INVALID
  PLUGIN_MISCONFIGURED
  NOT_FOUND
  REQUIRED
  UNIQUE
}
```

### Values

#### `GRAPHQL_ERROR`

#### `INVALID`

#### `PLUGIN_MISCONFIGURED`

#### `NOT_FOUND`

#### `REQUIRED`

#### `UNIQUE`
