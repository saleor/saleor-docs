---
api_reference: true
id: return-settings
title: returnSettings
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Returns related settings. Returns `ReturnSettings` configuration, global for the entire shop.

```graphql
returnSettings: ReturnSettings!
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ReturnSettings {
  reasonReferenceType: PageType
}
```

</Details>

### Type

#### [<code>ReturnSettings</code>](/api-reference/orders/objects/return-settings.mdx)

Return related settings from site settings.

<Badge
  text="Added in Saleor 3.23"
  class="badge badge--secondary margin-bottom--sm"
/>
