---
id: account-address-update
title: accountAddressUpdate
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

Updates an address of the logged-in user. 
<Permissions permissions={"MANAGE_USERS,IS_OWNER"} text={"Requires one of the following permissions"} />


Triggers the following webhook events:

- ADDRESS_UPDATED (async): An address was updated.

```graphql
accountAddressUpdate(
  id: ID!
  input: AddressInput!
): AccountAddressUpdate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>accountAddressUpdate.<b>id</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id.mdx) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/> <Badge class="badge badge--secondary" text="miscellaneous"/>

ID of the address to update.

#### [<code style={{ fontWeight: 'normal' }}>accountAddressUpdate.<b>input</b></code>](#)<Bullet />[`AddressInput!`](../../../api-reference/miscellaneous/inputs/address-input.mdx) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/> <Badge class="badge badge--secondary" text="miscellaneous"/>

Fields required to update the address.

### Type

#### [`AccountAddressUpdate`](../../../api-reference/users/objects/account-address-update.mdx) <Badge class="badge badge--secondary" text="object"/> <Badge class="badge badge--secondary" text="users"/>

Updates an address of the logged-in user. 
<Permissions permissions={"MANAGE_USERS,IS_OWNER"} text={"Requires one of the following permissions"} />


Triggers the following webhook events:

- ADDRESS_UPDATED (async): An address was updated.

import Permissions from "@site/components/Permissions";
