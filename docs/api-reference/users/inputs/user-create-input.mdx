---
api_reference: true
id: user-create-input
title: UserCreateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input UserCreateInput {
  defaultBillingAddress: AddressInput
  defaultShippingAddress: AddressInput
  firstName: String
  lastName: String
  email: String
  isActive: Boolean
  note: String
  metadata: [MetadataInput!]
  privateMetadata: [MetadataInput!]
  languageCode: LanguageCodeEnum
  externalReference: String
  isConfirmed: Boolean @deprecated
  redirectUrl: String
  channel: String
}
```

### Fields

#### [<code>defaultBillingAddress</code>](#default-billing-address)<Bullet />[<code>AddressInput</code>](/api-reference/miscellaneous/inputs/address-input.mdx) \{#default-billing-address\}

Billing address of the customer.

#### [<code>defaultShippingAddress</code>](#default-shipping-address)<Bullet />[<code>AddressInput</code>](/api-reference/miscellaneous/inputs/address-input.mdx) \{#default-shipping-address\}

Shipping address of the customer.

#### [<code>firstName</code>](#first-name)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#first-name\}

Given name.

#### [<code>lastName</code>](#last-name)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#last-name\}

Family name.

#### [<code>email</code>](#email)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#email\}

The unique email address of the user.

#### [<code>isActive</code>](#is-active)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-active\}

User account is active.

#### [<code>note</code>](#note)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#note\}

A note about the user.

#### [<code>metadata</code>](#metadata)<Bullet />[<code>[MetadataInput!]</code>](/api-reference/miscellaneous/inputs/metadata-input.mdx) \{#metadata\}

Fields required to update the user metadata. Can be read by any API client authorized to read the object it's attached to.

Warning: never store sensitive information, including financial data such as credit card details.

#### [<code>privateMetadata</code>](#private-metadata)<Bullet />[<code>[MetadataInput!]</code>](/api-reference/miscellaneous/inputs/metadata-input.mdx) \{#private-metadata\}

Fields required to update the user private metadata. Requires permissions to modify and to read the metadata of the object it's attached to.

Warning: never store sensitive information, including financial data such as credit card details.

#### [<code>languageCode</code>](#language-code)<Bullet />[<code>LanguageCodeEnum</code>](/api-reference/miscellaneous/enums/language-code-enum.mdx) \{#language-code\}

User language code.

#### [<code>externalReference</code>](#external-reference)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#external-reference\}

External ID of the customer.

#### [<code>redirectUrl</code>](#redirect-url)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#redirect-url\}

URL of a view where users should be redirected to set the password. URL in RFC 1808 format.

#### [<code>channel</code>](#channel)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#channel\}

Slug of a channel which will be used for notify user. Optional when only one channel exists.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>isConfirmed</code>](#is-confirmed)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-confirmed\}

:::warning[DEPRECATED]

The user will be always set as unconfirmed. The confirmation will take place when the user sets the password.

:::

User account is confirmed.

</Details>

### Member Of

[`customerCreate`](/api-reference/users/mutations/customer-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
