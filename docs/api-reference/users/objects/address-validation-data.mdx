---
api_reference: true
id: address-validation-data
title: AddressValidationData
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents address validation rules for a country.

```graphql
type AddressValidationData {
  countryCode: String!
  countryName: String!
  addressFormat: String!
  addressLatinFormat: String!
  allowedFields: [String!]!
  requiredFields: [String!]!
  upperFields: [String!]!
  countryAreaType: String!
  countryAreaChoices: [ChoiceValue!]!
  cityType: String!
  cityChoices: [ChoiceValue!]!
  cityAreaType: String!
  cityAreaChoices: [ChoiceValue!]!
  postalCodeType: String!
  postalCodeMatchers: [String!]!
  postalCodeExamples: [String!]!
  postalCodePrefix: String!
}
```

### Fields

#### [<code>countryCode</code>](#country-code)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#country-code\}

The country code of the address validation rule.

#### [<code>countryName</code>](#country-name)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#country-name\}

The country name of the address validation rule.

#### [<code>addressFormat</code>](#address-format)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#address-format\}

The address format of the address validation rule.

Many fields in the JSON refer to address fields by one-letter abbreviations. These are defined as follows:

- `N`: Name
- `O`: Organization
- `A`: Street Address Line(s)
- `D`: Dependent locality (may be an inner-city district or a suburb)
- `C`: City or Locality
- `S`: Administrative area such as a state, province, island etc
- `Z`: Zip or postal code
- `X`: Sorting code

[Click here for more information.](https://github.com/google/libaddressinput/wiki/AddressValidationMetadata)

#### [<code>addressLatinFormat</code>](#address-latin-format)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#address-latin-format\}

The latin address format of the address validation rule.

Many fields in the JSON refer to address fields by one-letter abbreviations. These are defined as follows:

- `N`: Name
- `O`: Organization
- `A`: Street Address Line(s)
- `D`: Dependent locality (may be an inner-city district or a suburb)
- `C`: City or Locality
- `S`: Administrative area such as a state, province, island etc
- `Z`: Zip or postal code
- `X`: Sorting code

[Click here for more information.](https://github.com/google/libaddressinput/wiki/AddressValidationMetadata)

#### [<code>allowedFields</code>](#allowed-fields)<Bullet />[<code>[String!]!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#allowed-fields\}

The allowed fields to use in address.

#### [<code>requiredFields</code>](#required-fields)<Bullet />[<code>[String!]!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#required-fields\}

The required fields to create a valid address.

#### [<code>upperFields</code>](#upper-fields)<Bullet />[<code>[String!]!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#upper-fields\}

The list of fields that should be in upper case for address validation rule.

#### [<code>countryAreaType</code>](#country-area-type)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#country-area-type\}

The formal name of the county area of the address validation rule.

#### [<code>countryAreaChoices</code>](#country-area-choices)<Bullet />[<code>[ChoiceValue!]!</code>](/api-reference/miscellaneous/objects/choice-value.mdx) \{#country-area-choices\}

The available choices for the country area of the address validation rule.

#### [<code>cityType</code>](#city-type)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#city-type\}

The formal name of the city of the address validation rule.

#### [<code>cityChoices</code>](#city-choices)<Bullet />[<code>[ChoiceValue!]!</code>](/api-reference/miscellaneous/objects/choice-value.mdx) \{#city-choices\}

The available choices for the city of the address validation rule.

#### [<code>cityAreaType</code>](#city-area-type)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#city-area-type\}

The formal name of the city area of the address validation rule.

#### [<code>cityAreaChoices</code>](#city-area-choices)<Bullet />[<code>[ChoiceValue!]!</code>](/api-reference/miscellaneous/objects/choice-value.mdx) \{#city-area-choices\}

The available choices for the city area of the address validation rule.

#### [<code>postalCodeType</code>](#postal-code-type)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#postal-code-type\}

The formal name of the postal code of the address validation rule.

#### [<code>postalCodeMatchers</code>](#postal-code-matchers)<Bullet />[<code>[String!]!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#postal-code-matchers\}

The regular expression for postal code validation.

#### [<code>postalCodeExamples</code>](#postal-code-examples)<Bullet />[<code>[String!]!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#postal-code-examples\}

The example postal code of the address validation rule.

#### [<code>postalCodePrefix</code>](#postal-code-prefix)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#postal-code-prefix\}

The postal code prefix of the address validation rule.

### Returned By

[`addressValidationRules`](/api-reference/users/queries/address-validation-rules.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
