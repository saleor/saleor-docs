---
api_reference: true
id: account-request-deletion
title: AccountRequestDeletion
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Sends an email with the account removal link for the logged-in user.

<Permissions
  permissions={"AUTHENTICATED_USER"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- NOTIFY_USER (async): A notification for account delete request.
- ACCOUNT_DELETE_REQUESTED (async): An account delete requested.

```graphql
type AccountRequestDeletion {
  accountErrors: [AccountError!]! @deprecated
  errors: [AccountError!]!
}
```

### Fields

#### [<code>errors</code>](#)<Bullet />[<code>[AccountError!]!</code>](/api-reference/users/objects/account-error.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>accountErrors</code>](#)<Bullet />[<code>[AccountError!]!</code>](/api-reference/users/objects/account-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`accountRequestDeletion`](/api-reference/users/mutations/account-request-deletion.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
