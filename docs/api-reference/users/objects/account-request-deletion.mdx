---
id: account-request-deletion
title: AccountRequestDeletion
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Sends an email with the account removal link for the logged-in user.

<Permissions
  permissions={"AUTHENTICATED_USER"}
  text={"Requires one of the following permissions"}
/>

```graphql
type AccountRequestDeletion {
  accountErrors: [AccountError!]! @deprecated
  errors: [AccountError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>AccountRequestDeletion.<b>accountErrors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-reference/users/objects/account-error) <Badge class="secondary" text="deprecated"/> <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

> <Badge
>   class="warning"
>   text="DEPRECATED: This field will be removed in Saleor 4.0. Use `errors` field instead."
> />

#### [<code style={{ fontWeight: 'normal' }}>AccountRequestDeletion.<b>errors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-reference/users/objects/account-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

>

### Returned by

[`accountRequestDeletion`](../../../api-reference/users/mutations/account-request-deletion) <Badge class="secondary" text="mutation"/>

import Permissions from "@site/components/Permissions";
