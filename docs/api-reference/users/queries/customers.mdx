---
api_reference: true
id: customers
title: customers
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

List of the shop's customers. This list includes all users who registered through the accountRegister mutation. Additionally, staff users who have placed an order using their account will also appear in this list.

<Permissions
  permissions={"MANAGE_ORDERS,MANAGE_USERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
customers(
  filter: CustomerFilterInput
  where: CustomerWhereInput
  sortBy: UserSortingInput
  search: String
  before: String
  after: String
  first: Int
  last: Int
): UserCountableConnection
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type UserCountableConnection {
  pageInfo: PageInfo!
  edges: [UserCountableEdge!]!
  totalCount: Int
}
```

</Details>

### Arguments

#### [<code>where</code>](#where)<Bullet />[<code>CustomerWhereInput</code>](/api-reference/users/inputs/customer-where-input.mdx) \{#where\}

Where filtering options for customers.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>sortBy</code>](#sort-by)<Bullet />[<code>UserSortingInput</code>](/api-reference/users/inputs/user-sorting-input.mdx) \{#sort-by\}

Sort customers.

#### [<code>search</code>](#search)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#search\}

Search customers.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>before</code>](#before)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#before\}

Return the elements in the list that come before the specified cursor.

#### [<code>after</code>](#after)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#after\}

Return the elements in the list that come after the specified cursor.

#### [<code>first</code>](#first)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#first\}

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>last</code>](#last)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#last\}

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

<details class="graphql-markdown-details">
<summary>
<span class="graphql-markdown-details-label-closed">Show deprecated</span>
<span class="graphql-markdown-details-label-open">Hide deprecated</span>
</summary>

#### [<code>filter</code>](#filter)<Bullet />[<code>CustomerFilterInput</code>](/api-reference/users/inputs/customer-filter-input.mdx) \{#filter\}

:::warning[DEPRECATED]

Use `where` filter instead.

:::

Filtering options for customers.

</details>

### Type

#### [<code>UserCountableConnection</code>](/api-reference/users/objects/user-countable-connection.mdx)
