---
api_reference: true
id: attribute-value-where-input
title: AttributeValueWhereInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Where filtering options for attribute values.

```graphql
input AttributeValueWhereInput {
  ids: [ID!]
  name: StringFilterInput
  slug: StringFilterInput
  AND: [AttributeValueWhereInput!]
  OR: [AttributeValueWhereInput!]
}
```

### Fields

#### [<code>ids</code>](#)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx)

#### [<code>name</code>](#)<Bullet />[<code>StringFilterInput</code>](/api-reference/miscellaneous/inputs/string-filter-input.mdx)

#### [<code>slug</code>](#)<Bullet />[<code>StringFilterInput</code>](/api-reference/miscellaneous/inputs/string-filter-input.mdx)

#### [<code>AND</code>](#)<Bullet />[<code>[AttributeValueWhereInput!]</code>](/api-reference/attributes/inputs/attribute-value-where-input.mdx)

List of conditions that must be met.

#### [<code>OR</code>](#)<Bullet />[<code>[AttributeValueWhereInput!]</code>](/api-reference/attributes/inputs/attribute-value-where-input.mdx)

A list of conditions of which at least one must be met.

### Member Of

[`AttributeValueWhereInput`](/api-reference/attributes/inputs/attribute-value-where-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
