---
api_reference: true
id: attribute-value-create-input
title: AttributeValueCreateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input AttributeValueCreateInput {
  value: String
  richText: JSONString @deprecated
  plainText: String @deprecated
  fileUrl: String
  contentType: String
  externalReference: String
  name: String!
}
```

### Fields

#### [<code>value</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Represent value of the attribute value (e.g. color values for swatch attributes).

#### [<code>fileUrl</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

URL of the file attribute. Every time, a new value is created.

#### [<code>contentType</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

File content type.

#### [<code>externalReference</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

External ID of this attribute value.

#### [<code>name</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of a value displayed in the interface.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>richText</code>](#)<Bullet />[<code>JSONString</code>](/api-reference/miscellaneous/scalars/jsonstring.mdx)

:::warning[DEPRECATED]

The rich text attribute hasn't got predefined value, so can be specified only from instance that supports the given attribute.

:::

Represents the text of the attribute value, includes formatting.

Rich text format. For reference see https://editorjs.io/

#### [<code>plainText</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

:::warning[DEPRECATED]

The plain text attribute hasn't got predefined value, so can be specified only from instance that supports the given attribute.

:::

Represents the text of the attribute value, plain text without formatting.

</Details>

### Member Of

[`AttributeCreateInput`](/api-reference/attributes/inputs/attribute-create-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/><Bullet />[`attributeValueCreate`](/api-reference/attributes/mutations/attribute-value-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
