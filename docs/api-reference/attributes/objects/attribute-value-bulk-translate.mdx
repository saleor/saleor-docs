---
api_reference: true
id: attribute-value-bulk-translate
title: AttributeValueBulkTranslate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Creates/updates translations for attribute values.

<Permissions
  permissions={"MANAGE_TRANSLATIONS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type AttributeValueBulkTranslate {
  count: Int!
  results: [AttributeValueBulkTranslateResult!]!
  errors: [AttributeValueBulkTranslateError!]!
}
```

### Fields

#### [<code>count</code>](#)<Bullet />[<code>Int!</code>](/api-reference/miscellaneous/scalars/int.mdx)

Returns how many translations were created/updated.

#### [<code>results</code>](#)<Bullet />[<code>[AttributeValueBulkTranslateResult!]!</code>](/api-reference/attributes/objects/attribute-value-bulk-translate-result.mdx)

List of the translations.

#### [<code>errors</code>](#)<Bullet />[<code>[AttributeValueBulkTranslateError!]!</code>](/api-reference/miscellaneous/objects/attribute-value-bulk-translate-error.mdx)

### Returned By

[`attributeValueBulkTranslate`](/api-reference/attributes/mutations/attribute-value-bulk-translate.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
