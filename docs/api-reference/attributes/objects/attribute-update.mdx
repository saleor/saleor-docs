---
api_reference: true
id: attribute-update
title: AttributeUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates attribute.

<Permissions
  permissions={"MANAGE_PRODUCT_TYPES_AND_ATTRIBUTES"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- ATTRIBUTE_UPDATED (async): An attribute was updated.

```graphql
type AttributeUpdate {
  attribute: Attribute
  attributeErrors: [AttributeError!]! @deprecated
  errors: [AttributeError!]!
}
```

### Fields

#### [<code>attribute</code>](#)<Bullet />[<code>Attribute</code>](/api-reference/attributes/objects/attribute.mdx)

The updated attribute.

#### [<code>errors</code>](#)<Bullet />[<code>[AttributeError!]!</code>](/api-reference/attributes/objects/attribute-error.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>attributeErrors</code>](#)<Bullet />[<code>[AttributeError!]!</code>](/api-reference/attributes/objects/attribute-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`attributeUpdate`](/api-reference/attributes/mutations/attribute-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
