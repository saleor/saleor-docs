---
id: digital-content-countable-edge
title: DigitalContentCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DigitalContentCountableEdge {
  node: DigitalContent!
  cursor: String!
}
```

### Fields

#### `node` ([`DigitalContent!`](/api-reference/objects/digital-content.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
