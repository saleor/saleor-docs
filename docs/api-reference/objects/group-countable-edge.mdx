---
id: group-countable-edge
title: GroupCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type GroupCountableEdge {
  node: Group!
  cursor: String!
}
```

### Fields

#### `node` ([`Group!`](/api-reference/objects/group.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
