---
id: money
title: Money
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents amount of money in specific currency.

```graphql
type Money {
  currency: String!
  amount: Float!
  localized: String!
}
```

### Fields

#### `currency` ([`String!`](/api-reference/scalars/string.mdx))

Currency code.

#### `amount` ([`Float!`](/api-reference/scalars/float.mdx))

Amount of money.

#### `localized` ([`String!`](/api-reference/scalars/string.mdx))

<sub><sup><Tag color="#ffba00">DEPRECATED</Tag> Price formatting according to the current locale should be handled by the frontend client. This field will be removed after 2020-07-31.</sup></sub>

Money formatted according to the current locale.
