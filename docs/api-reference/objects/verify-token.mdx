---
id: verify-token
title: VerifyToken
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Verify JWT token.

```graphql
type VerifyToken {
  errors: [Error!]!
  user: User
  isValid: Boolean!
  payload: GenericScalar
  accountErrors: [AccountError!]!
}
```

### Fields

#### `errors` ([`[Error!]!`](/api-reference/objects/error.mdx))

<sub><sup><Tag color="#ffba00">DEPRECATED</Tag> Use typed errors with error codes. This field will be removed after 2020-07-31.</sup></sub>

List of errors that occurred executing the mutation.

#### `user` ([`User`](/api-reference/objects/user.mdx))

User assigned to token.

#### `isValid` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

Determine if token is valid or not.

#### `payload` ([`GenericScalar`](/api-reference/scalars/generic-scalar.mdx))

JWT payload.

#### `accountErrors` ([`[AccountError!]!`](/api-reference/objects/account-error.mdx))
