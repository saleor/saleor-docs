---
id: shipping-zone-countable-edge
title: ShippingZoneCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ShippingZoneCountableEdge {
  node: ShippingZone!
  cursor: String!
}
```

### Fields

#### `node` ([`ShippingZone!`](/api-reference/objects/shipping-zone.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
