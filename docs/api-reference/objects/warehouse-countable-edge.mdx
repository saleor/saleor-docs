---
id: warehouse-countable-edge
title: WarehouseCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type WarehouseCountableEdge {
  node: Warehouse!
  cursor: String!
}
```

### Fields

#### `node` ([`Warehouse!`](/api-reference/objects/warehouse.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
