---
id: checkout-countable-edge
title: CheckoutCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CheckoutCountableEdge {
  node: Checkout!
  cursor: String!
}
```

### Fields

#### `node` ([`Checkout!`](/api-reference/objects/checkout.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
