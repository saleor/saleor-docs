---
id: export-file-countable-edge
title: ExportFileCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ExportFileCountableEdge {
  node: ExportFile!
  cursor: String!
}
```

### Fields

#### `node` ([`ExportFile!`](/api-reference/objects/export-file.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
