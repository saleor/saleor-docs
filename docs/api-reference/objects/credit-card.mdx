---
id: credit-card
title: CreditCard
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CreditCard {
  brand: String!
  firstDigits: String
  lastDigits: String!
  expMonth: Int
  expYear: Int
}
```

### Fields

#### `brand` ([`String!`](/api-reference/scalars/string.mdx))

Card brand.

#### `firstDigits` ([`String`](/api-reference/scalars/string.mdx))

First 4 digits of the card number.

#### `lastDigits` ([`String!`](/api-reference/scalars/string.mdx))

Last 4 digits of the card number.

#### `expMonth` ([`Int`](/api-reference/scalars/int.mdx))

Two-digit number representing the card’s expiration month.

#### `expYear` ([`Int`](/api-reference/scalars/int.mdx))

Four-digit number representing the card’s expiration year.
