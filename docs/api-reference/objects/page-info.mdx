---
id: page-info
title: PageInfo
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

The Relay compliant `PageInfo` type, containing data necessary to paginate this connection.

```graphql
type PageInfo {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  startCursor: String
  endCursor: String
}
```

### Fields

#### `hasNextPage` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

When paginating forwards, are there more items?

#### `hasPreviousPage` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

When paginating backwards, are there more items?

#### `startCursor` ([`String`](/api-reference/scalars/string.mdx))

When paginating backwards, the cursor to continue.

#### `endCursor` ([`String`](/api-reference/scalars/string.mdx))

When paginating forwards, the cursor to continue.
