---
id: language-display
title: LanguageDisplay
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type LanguageDisplay {
  code: LanguageCodeEnum!
  language: String!
}
```

### Fields

#### `code` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

ISO 639 representation of the language name.

#### `language` ([`String!`](/api-reference/scalars/string.mdx))

Full name of the language.
