---
id: discount-error
title: DiscountError
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type DiscountError {
  field: String
  message: String
  products: [ID!]
  code: DiscountErrorCode!
  channels: [ID!]
}
```

### Fields

#### `field` ([`String`](/api-reference/scalars/string.mdx))

Name of a field that caused the error. A value of `null` indicates that the error isn't associated with a particular field.

#### `message` ([`String`](/api-reference/scalars/string.mdx))

The error message.

#### `products` ([`[ID!]`](/api-reference/scalars/id.mdx))

List of products IDs which causes the error.

#### `code` ([`DiscountErrorCode!`](/api-reference/enums/discount-error-code.mdx))

The error code.

#### `channels` ([`[ID!]`](/api-reference/scalars/id.mdx))

List of channels IDs which causes the error.
