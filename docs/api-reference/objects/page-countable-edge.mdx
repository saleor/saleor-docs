---
id: page-countable-edge
title: PageCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PageCountableEdge {
  node: Page!
  cursor: String!
}
```

### Fields

#### `node` ([`Page!`](/api-reference/objects/page.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
