---
id: choice-value
title: ChoiceValue
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ChoiceValue {
  raw: String
  verbose: String
}
```

### Fields

#### `raw` ([`String`](/api-reference/scalars/string.mdx))

#### `verbose` ([`String`](/api-reference/scalars/string.mdx))
