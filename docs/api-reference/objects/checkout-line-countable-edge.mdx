---
id: checkout-line-countable-edge
title: CheckoutLineCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CheckoutLineCountableEdge {
  node: CheckoutLine!
  cursor: String!
}
```

### Fields

#### `node` ([`CheckoutLine!`](/api-reference/objects/checkout-line.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
