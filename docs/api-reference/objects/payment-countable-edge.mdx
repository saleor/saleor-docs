---
id: payment-countable-edge
title: PaymentCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PaymentCountableEdge {
  node: Payment!
  cursor: String!
}
```

### Fields

#### `node` ([`Payment!`](/api-reference/objects/payment.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
