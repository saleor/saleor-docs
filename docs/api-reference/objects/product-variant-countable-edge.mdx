---
id: product-variant-countable-edge
title: ProductVariantCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProductVariantCountableEdge {
  node: ProductVariant!
  cursor: String!
}
```

### Fields

#### `node` ([`ProductVariant!`](/api-reference/objects/product-variant.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
