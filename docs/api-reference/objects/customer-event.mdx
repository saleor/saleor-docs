---
id: customer-event
title: CustomerEvent
---

History log of the customer.

```graphql
type CustomerEvent implements Node {
  id: ID!
  date: DateTime
  type: CustomerEventsEnum
  user: User
  message: String
  count: Int
  order: Order
  orderLine: OrderLine
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `date` ([`DateTime`](/api-reference/scalars/date-time.mdx))

Date when event happened at in ISO 8601 format.

#### `type` ([`CustomerEventsEnum`](/api-reference/enums/customer-events-enum.mdx))

Customer event type.

#### `user` ([`User`](/api-reference/objects/user.mdx))

User who performed the action.

#### `message` ([`String`](/api-reference/scalars/string.mdx))

Content of the event.

#### `count` ([`Int`](/api-reference/scalars/int.mdx))

Number of objects concerned by the event.

#### `order` ([`Order`](/api-reference/objects/order.mdx))

The concerned order.

#### `orderLine` ([`OrderLine`](/api-reference/objects/order-line.mdx))

The concerned order line.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
