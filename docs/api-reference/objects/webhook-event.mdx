---
id: webhook-event
title: WebhookEvent
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Webhook event.

```graphql
type WebhookEvent {
  eventType: WebhookEventTypeEnum!
  name: String!
}
```

### Fields

#### `eventType` ([`WebhookEventTypeEnum!`](/api-reference/enums/webhook-event-type-enum.mdx))

Internal name of the event type.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

Display name of the event.
