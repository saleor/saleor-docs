---
id: service
title: _Service
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type _Service {
  sdl: String
}
```

### Fields

#### `sdl` ([`String`](/api-reference/scalars/string.mdx))
