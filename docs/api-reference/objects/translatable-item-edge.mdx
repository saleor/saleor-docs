---
id: translatable-item-edge
title: TranslatableItemEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type TranslatableItemEdge {
  node: TranslatableItem!
  cursor: String!
}
```

### Fields

#### `node` ([`TranslatableItem!`](/api-reference/unions/translatable-item.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
