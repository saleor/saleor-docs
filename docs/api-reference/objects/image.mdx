---
id: image
title: Image
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents an image.

```graphql
type Image {
  url: String!
  alt: String
}
```

### Fields

#### `url` ([`String!`](/api-reference/scalars/string.mdx))

The URL of the image.

#### `alt` ([`String`](/api-reference/scalars/string.mdx))

Alt text for an image.
