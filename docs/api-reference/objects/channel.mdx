---
id: channel
title: Channel
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents channel.

```graphql
type Channel implements Node {
  id: ID!
  name: String!
  isActive: Boolean!
  slug: String!
  currencyCode: String!
  hasOrders: Boolean!
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `isActive` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

#### `slug` ([`String!`](/api-reference/scalars/string.mdx))

#### `currencyCode` ([`String!`](/api-reference/scalars/string.mdx))

#### `hasOrders` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

Whether a channel has associated orders.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
