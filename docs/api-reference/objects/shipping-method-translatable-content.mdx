---
id: shipping-method-translatable-content
title: ShippingMethodTranslatableContent
---

No description

```graphql
type ShippingMethodTranslatableContent implements Node {
  id: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): ShippingMethodTranslation
  shippingMethod: ShippingMethod
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `translation` ([`ShippingMethodTranslation`](/api-reference/objects/shipping-method-translation.mdx))

Returns translated shipping method fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

A language code to return the translation for shipping method.

#### `shippingMethod` ([`ShippingMethod`](/api-reference/objects/shipping-method.mdx))

Shipping method are the methods you'll use to get customer's orders to them. They are directly exposed to the customers.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
