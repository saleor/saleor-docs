---
id: product-image
title: ProductImage
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents a product image.

```graphql
type ProductImage implements Node {
  id: ID!
  sortOrder: Int
  alt: String!
  url(size: Int = 0): String!
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `sortOrder` ([`Int`](/api-reference/scalars/int.mdx))

#### `alt` ([`String!`](/api-reference/scalars/string.mdx))

#### `url` ([`String!`](/api-reference/scalars/string.mdx))

The URL of the image.

- ##### `size` ([`Int`](/api-reference/scalars/int.mdx))

Size of the image.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
