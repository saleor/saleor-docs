---
id: product-variant-bulk-create
title: ProductVariantBulkCreate
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Creates product variants for a given product.

```graphql
type ProductVariantBulkCreate {
  errors: [Error!]!
  count: Int!
  productVariants: [ProductVariant!]!
  bulkProductErrors: [BulkProductError!]!
}
```

### Fields

#### `errors` ([`[Error!]!`](/api-reference/objects/error.mdx))

<sub><sup><Tag color="#ffba00">DEPRECATED</Tag> Use typed errors with error codes. This field will be removed after 2020-07-31.</sup></sub>

List of errors that occurred executing the mutation.

#### `count` ([`Int!`](/api-reference/scalars/int.mdx))

Returns how many objects were created.

#### `productVariants` ([`[ProductVariant!]!`](/api-reference/objects/product-variant.mdx))

List of the created variants.

#### `bulkProductErrors` ([`[BulkProductError!]!`](/api-reference/objects/bulk-product-error.mdx))
