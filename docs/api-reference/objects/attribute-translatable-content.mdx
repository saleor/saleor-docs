---
id: attribute-translatable-content
title: AttributeTranslatableContent
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type AttributeTranslatableContent implements Node {
  id: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): AttributeTranslation
  attribute: Attribute
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `translation` ([`AttributeTranslation`](/api-reference/objects/attribute-translation.mdx))

Returns translated attribute fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

A language code to return the translation for attribute.

#### `attribute` ([`Attribute`](/api-reference/objects/attribute.mdx))

Custom attribute of a product.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
