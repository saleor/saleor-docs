---
id: category-countable-edge
title: CategoryCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type CategoryCountableEdge {
  node: Category!
  cursor: String!
}
```

### Fields

#### `node` ([`Category!`](/api-reference/objects/category.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
