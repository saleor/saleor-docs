---
id: plugin-countable-edge
title: PluginCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type PluginCountableEdge {
  node: Plugin!
  cursor: String!
}
```

### Fields

#### `node` ([`Plugin!`](/api-reference/objects/plugin.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
