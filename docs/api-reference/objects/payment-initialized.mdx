---
id: payment-initialized
title: PaymentInitialized
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Server-side data generated by a payment gateway. Optional step when the payment provider requires an additional action to initialize payment session.

```graphql
type PaymentInitialized {
  gateway: String!
  name: String!
  data: JSONString
}
```

### Fields

#### `gateway` ([`String!`](/api-reference/scalars/string.mdx))

ID of a payment gateway.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

Payment gateway name.

#### `data` ([`JSONString`](/api-reference/scalars/json-string.mdx))

Initialized data by gateway.
