---
id: domain
title: Domain
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents shop's domain.

```graphql
type Domain {
  host: String!
  sslEnabled: Boolean!
  url: String!
}
```

### Fields

#### `host` ([`String!`](/api-reference/scalars/string.mdx))

The host name of the domain.

#### `sslEnabled` ([`Boolean!`](/api-reference/scalars/boolean.mdx))

Inform if SSL is enabled.

#### `url` ([`String!`](/api-reference/scalars/string.mdx))

Shop's absolute URL.
