---
id: product-type-countable-edge
title: ProductTypeCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type ProductTypeCountableEdge {
  node: ProductType!
  cursor: String!
}
```

### Fields

#### `node` ([`ProductType!`](/api-reference/objects/product-type.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
