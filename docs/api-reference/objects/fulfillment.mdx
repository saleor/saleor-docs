---
id: fulfillment
title: Fulfillment
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents order fulfillment.

```graphql
type Fulfillment implements Node, ObjectWithMetadata {
  id: ID!
  fulfillmentOrder: Int!
  status: FulfillmentStatus!
  trackingNumber: String!
  created: DateTime!
  privateMetadata: [MetadataItem]!
  metadata: [MetadataItem]!
  lines: [FulfillmentLine]
  statusDisplay: String
  warehouse: Warehouse
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `fulfillmentOrder` ([`Int!`](/api-reference/scalars/int.mdx))

#### `status` ([`FulfillmentStatus!`](/api-reference/enums/fulfillment-status.mdx))

#### `trackingNumber` ([`String!`](/api-reference/scalars/string.mdx))

#### `created` ([`DateTime!`](/api-reference/scalars/date-time.mdx))

#### `privateMetadata` ([`[MetadataItem]!`](/api-reference/objects/metadata-item.mdx))

List of private metadata items.Requires proper staff permissions to access.

#### `metadata` ([`[MetadataItem]!`](/api-reference/objects/metadata-item.mdx))

List of public metadata items. Can be accessed without permissions.

#### `lines` ([`[FulfillmentLine]`](/api-reference/objects/fulfillment-line.mdx))

List of lines for the fulfillment.

#### `statusDisplay` ([`String`](/api-reference/scalars/string.mdx))

User-friendly fulfillment status.

#### `warehouse` ([`Warehouse`](/api-reference/objects/warehouse.mdx))

Warehouse from fulfillment was fulfilled.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID

#### [`ObjectWithMetadata`](/api-reference/interfaces/object-with-metadata.mdx)
