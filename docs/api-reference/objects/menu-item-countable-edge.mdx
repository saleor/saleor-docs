---
id: menu-item-countable-edge
title: MenuItemCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MenuItemCountableEdge {
  node: MenuItem!
  cursor: String!
}
```

### Fields

#### `node` ([`MenuItem!`](/api-reference/objects/menu-item.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
