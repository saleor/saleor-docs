---
id: shipping-method
title: ShippingMethod
---

Shipping method are the methods you'll use to get customer's orders to them. They are directly exposed to the customers.

```graphql
type ShippingMethod implements Node, ObjectWithMetadata {
  id: ID!
  name: String!
  minimumOrderWeight: Weight
  maximumOrderWeight: Weight
  maximumDeliveryDays: Int
  minimumDeliveryDays: Int
  privateMetadata: [MetadataItem]!
  metadata: [MetadataItem]!
  type: ShippingMethodTypeEnum
  translation(languageCode: LanguageCodeEnum!): ShippingMethodTranslation
  channelListings: [ShippingMethodChannelListing!]
  price: Money
  maximumOrderPrice: Money
  minimumOrderPrice: Money
  zipCodeRules: [ShippingMethodZipCodeRule]
  excludedProducts(
    before: String
    after: String
    first: Int = 0
    last: Int = 0
  ): ProductCountableConnection
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `minimumOrderWeight` ([`Weight`](/api-reference/objects/weight.mdx))

#### `maximumOrderWeight` ([`Weight`](/api-reference/objects/weight.mdx))

#### `maximumDeliveryDays` ([`Int`](/api-reference/scalars/int.mdx))

#### `minimumDeliveryDays` ([`Int`](/api-reference/scalars/int.mdx))

#### `privateMetadata` ([`[MetadataItem]!`](/api-reference/objects/metadata-item.mdx))

List of private metadata items.Requires proper staff permissions to access.

#### `metadata` ([`[MetadataItem]!`](/api-reference/objects/metadata-item.mdx))

List of public metadata items. Can be accessed without permissions.

#### `type` ([`ShippingMethodTypeEnum`](/api-reference/enums/shipping-method-type-enum.mdx))

Type of the shipping method.

#### `translation` ([`ShippingMethodTranslation`](/api-reference/objects/shipping-method-translation.mdx))

Returns translated shipping method fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

A language code to return the translation for shipping method.

#### `channelListings` ([`[ShippingMethodChannelListing!]`](/api-reference/objects/shipping-method-channel-listing.mdx))

List of channels available for the method.

#### `price` ([`Money`](/api-reference/objects/money.mdx))

The price of the cheapest variant (including discounts).

#### `maximumOrderPrice` ([`Money`](/api-reference/objects/money.mdx))

The price of the cheapest variant (including discounts).

#### `minimumOrderPrice` ([`Money`](/api-reference/objects/money.mdx))

The price of the cheapest variant (including discounts).

#### `zipCodeRules` ([`[ShippingMethodZipCodeRule]`](/api-reference/objects/shipping-method-zip-code-rule.mdx))

Zip code exclude range of the shipping method.

#### `excludedProducts` ([`ProductCountableConnection`](/api-reference/objects/product-countable-connection.mdx))

List of excluded products for the shipping method.

- ##### `before` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

- ##### `after` ([`String`](/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

- ##### `first` ([`Int`](/api-reference/scalars/int.mdx))

Return the first n elements from the list.

- ##### `last` ([`Int`](/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID

#### [`ObjectWithMetadata`](/api-reference/interfaces/object-with-metadata.mdx)
