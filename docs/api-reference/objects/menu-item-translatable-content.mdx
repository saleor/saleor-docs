---
id: menu-item-translatable-content
title: MenuItemTranslatableContent
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type MenuItemTranslatableContent implements Node {
  id: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): MenuItemTranslation
  menuItem: MenuItem
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `translation` ([`MenuItemTranslation`](/api-reference/objects/menu-item-translation.mdx))

Returns translated menu item fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

A language code to return the translation for menu item.

#### `menuItem` ([`MenuItem`](/api-reference/objects/menu-item.mdx))

Represents a single item of the related menu. Can store categories, collection or pages.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
