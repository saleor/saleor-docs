---
id: order-event-countable-edge
title: OrderEventCountableEdge
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type OrderEventCountableEdge {
  node: OrderEvent!
  cursor: String!
}
```

### Fields

#### `node` ([`OrderEvent!`](/api-reference/objects/order-event.mdx))

The item at the end of the edge.

#### `cursor` ([`String!`](/api-reference/scalars/string.mdx))

A cursor for use in pagination.
