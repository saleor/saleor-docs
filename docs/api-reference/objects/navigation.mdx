---
id: navigation
title: Navigation
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Represents shop's navigation menus.

```graphql
type Navigation {
  main: Menu
  secondary: Menu
}
```

### Fields

#### `main` ([`Menu`](/api-reference/objects/menu.mdx))

Main navigation bar.

#### `secondary` ([`Menu`](/api-reference/objects/menu.mdx))

Secondary navigation bar.
