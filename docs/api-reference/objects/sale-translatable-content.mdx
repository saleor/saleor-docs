---
id: sale-translatable-content
title: SaleTranslatableContent
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

No description

```graphql
type SaleTranslatableContent implements Node {
  id: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): SaleTranslation
  sale: Sale
}
```

### Fields

#### `id` ([`ID!`](/api-reference/scalars/id.mdx))

The ID of the object.

#### `name` ([`String!`](/api-reference/scalars/string.mdx))

#### `translation` ([`SaleTranslation`](/api-reference/objects/sale-translation.mdx))

Returns translated sale fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/api-reference/enums/language-code-enum.mdx))

A language code to return the translation for sale.

#### `sale` ([`Sale`](/api-reference/objects/sale.mdx))

Sales allow creating discounts for categories, collections or products and are visible to all the customers.

### Interfaces

#### [`Node`](/api-reference/interfaces/node.mdx)

An object with an ID
