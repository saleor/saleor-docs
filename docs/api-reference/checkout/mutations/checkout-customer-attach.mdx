---
id: checkout-customer-attach
title: checkoutCustomerAttach
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Sets the customer as the owner of the checkout.

<Permissions
  permissions={"AUTHENTICATED_APP,AUTHENTICATED_USER"}
  text={"Requires one of the following permissions"}
/>

```graphql
checkoutCustomerAttach(
  checkoutId: ID
  customerId: ID
  id: ID
  token: UUID
): CheckoutCustomerAttach
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>checkoutCustomerAttach.<b>checkoutId</b></code>](#)<Bullet />[`ID`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The ID of the checkout.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

#### [<code style={{ fontWeight: 'normal' }}>checkoutCustomerAttach.<b>customerId</b></code>](#)<Bullet />[`ID`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> ID of customer to attach to checkout. Requires IMPERSONATE_USER permission when customerId is different than the logged-in user.

#### [<code style={{ fontWeight: 'normal' }}>checkoutCustomerAttach.<b>id</b></code>](#)<Bullet />[`ID`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="scalar"/>

> The checkout's ID.
>
> <Badge text="Added in Saleor 3.4" class="secondary margin-bottom--sm" />

#### [<code style={{ fontWeight: 'normal' }}>checkoutCustomerAttach.<b>token</b></code>](#)<Bullet />[`UUID`](../../../api-reference/miscellaneous/scalars/uuid) <Badge class="badge badge--secondary" text="scalar"/>

> Checkout token.
>
> DEPRECATED: this field will be removed in Saleor 4.0. Use `id` instead.

### Type

#### [`CheckoutCustomerAttach`](../../../api-reference/checkout/objects/checkout-customer-attach) <Badge class="badge badge--secondary" text="object"/>

> Sets the customer as the owner of the checkout.

<Permissions
  permissions={"AUTHENTICATED_APP,AUTHENTICATED_USER"}
  text={"Requires one of the following permissions"}
/>

import Permissions from "@site/components/Permissions";
