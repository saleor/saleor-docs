---
api_reference: true
id: checkout-lines-update
title: checkoutLinesUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates checkout line in the existing checkout.

Triggers the following webhook events:

- CHECKOUT_UPDATED (async): A checkout was updated.

```graphql
checkoutLinesUpdate(
  checkoutId: ID
  id: ID
  lines: [CheckoutLineUpdateInput!]!
  token: UUID
): CheckoutLinesUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type CheckoutLinesUpdate {
  checkout: Checkout
  checkoutErrors: [CheckoutError!]! @deprecated
  errors: [CheckoutError!]!
}
```

</Details>

### Arguments

#### [<code>id</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The checkout's ID.

#### [<code>lines</code>](#)<Bullet />[<code>[CheckoutLineUpdateInput!]!</code>](/api-reference/checkout/inputs/checkout-line-update-input.mdx)

A list of checkout lines, each containing information about an item in the checkout.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>checkoutId</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

:::warning[DEPRECATED]

Use `id` instead.

:::

The ID of the checkout.

#### [<code>token</code>](#)<Bullet />[<code>UUID</code>](/api-reference/miscellaneous/scalars/uuid.mdx)

:::warning[DEPRECATED]

Use `id` instead.

:::

Checkout token.

</Details>

### Type

#### [<code>CheckoutLinesUpdate</code>](/api-reference/checkout/objects/checkout-lines-update.mdx)

Updates checkout line in the existing checkout.

Triggers the following webhook events:

- CHECKOUT_UPDATED (async): A checkout was updated.
