---
api_reference: true
id: checkout-line-problem-insufficient-stock
title: CheckoutLineProblemInsufficientStock
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

Indicates insufficient stock for a given checkout line.Placing the order will not be possible until solving this problem.

```graphql
type CheckoutLineProblemInsufficientStock {
  availableQuantity: Int
  line: CheckoutLine!
  variant: ProductVariant!
}
```

### Fields

#### [`availableQuantity`](#)<Bullet />[`Int`](/api-reference/miscellaneous/scalars/int.mdx)

Available quantity of a variant.

#### [`line`](#)<Bullet />[`CheckoutLine!`](/api-reference/checkout/objects/checkout-line.mdx)

The line that has variant with insufficient stock.

#### [`variant`](#)<Bullet />[`ProductVariant!`](/api-reference/products/objects/product-variant.mdx)

The variant with insufficient stock.

### Implemented By

[`CheckoutLineProblem`](/api-reference/miscellaneous/unions/checkout-line-problem.mdx) <Badge class="badge badge--secondary" text="union"/><Bullet />[`CheckoutProblem`](/api-reference/miscellaneous/unions/checkout-problem.mdx) <Badge class="badge badge--secondary" text="union"/>
