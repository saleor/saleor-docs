---
api_reference: true
id: order-grant-refund-update-input
title: OrderGrantRefundUpdateInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input OrderGrantRefundUpdateInput {
  amount: Decimal
  reason: String
  reasonReference: ID
  addLines: [OrderGrantRefundUpdateLineAddInput!]
  removeLines: [ID!]
  grantRefundForShipping: Boolean
  transactionId: ID
}
```

### Fields

#### [<code>amount</code>](#amount)<Bullet />[<code>Decimal</code>](/api-reference/miscellaneous/scalars/decimal.mdx) \{#amount\}

Amount of the granted refund. if not provided and `addLines` or `removeLines` or `grantRefundForShipping` is provided, amount will be calculated automatically.

#### [<code>reason</code>](#reason)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#reason\}

Reason of the granted refund.

#### [<code>reasonReference</code>](#reason-reference)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#reason-reference\}

ID of a `Page` (Model) to reference in reason.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>addLines</code>](#add-lines)<Bullet />[<code>[OrderGrantRefundUpdateLineAddInput!]</code>](/api-reference/orders/inputs/order-grant-refund-update-line-add-input.mdx) \{#add-lines\}

Lines to assign to granted refund.

#### [<code>removeLines</code>](#remove-lines)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#remove-lines\}

Lines to remove from granted refund.

#### [<code>grantRefundForShipping</code>](#grant-refund-for-shipping)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#grant-refund-for-shipping\}

Determine if granted refund should include shipping costs.

#### [<code>transactionId</code>](#transaction-id)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#transaction-id\}

The ID of the transaction item related to the granted refund. If `amount` provided in the input, the transaction.chargedAmount needs to be equal or greater than provided `amount`.If `amount` is not provided in the input and calculated automatically by Saleor, the `min(calculatedAmount, transaction.chargedAmount)` will be used.Field will be required starting from Saleor 3.21.

<Badge
  text="Added in Saleor 3.20"
  class="badge badge--secondary margin-bottom--sm"
/>

<FeaturePreview />

### Member Of

[`orderGrantRefundUpdate`](/api-reference/orders/mutations/order-grant-refund-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
