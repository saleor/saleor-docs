---
api_reference: true
id: order-bulk-create-delivery-method-input
title: OrderBulkCreateDeliveryMethodInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input OrderBulkCreateDeliveryMethodInput {
  warehouseId: ID
  warehouseName: String
  shippingMethodId: ID
  shippingMethodName: String
  shippingPrice: TaxedMoneyInput
  shippingTaxRate: PositiveDecimal
  shippingTaxClassId: ID
  shippingTaxClassName: String
  shippingTaxClassMetadata: [MetadataInput!]
  shippingTaxClassPrivateMetadata: [MetadataInput!]
}
```

### Fields

#### [<code>warehouseId</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the warehouse.

#### [<code>warehouseName</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of the warehouse.

#### [<code>shippingMethodId</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the shipping method.

#### [<code>shippingMethodName</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of the shipping method.

#### [<code>shippingPrice</code>](#)<Bullet />[<code>TaxedMoneyInput</code>](/api-reference/orders/inputs/taxed-money-input.mdx)

The price of the shipping.

#### [<code>shippingTaxRate</code>](#)<Bullet />[<code>PositiveDecimal</code>](/api-reference/miscellaneous/scalars/positive-decimal.mdx)

Tax rate of the shipping.

#### [<code>shippingTaxClassId</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the tax class.

#### [<code>shippingTaxClassName</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of the tax class.

#### [<code>shippingTaxClassMetadata</code>](#)<Bullet />[<code>[MetadataInput!]</code>](/api-reference/miscellaneous/inputs/metadata-input.mdx)

Metadata of the tax class. Can be read by any API client authorized to read the object it's attached to.

Warning: never store sensitive information, including financial data such as credit card details.

#### [<code>shippingTaxClassPrivateMetadata</code>](#)<Bullet />[<code>[MetadataInput!]</code>](/api-reference/miscellaneous/inputs/metadata-input.mdx)

Private metadata of the tax class. Requires permissions to modify and to read the metadata of the object it's attached to.

Warning: never store sensitive information, including financial data such as credit card details.

### Member Of

[`OrderBulkCreateInput`](/api-reference/orders/inputs/order-bulk-create-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
