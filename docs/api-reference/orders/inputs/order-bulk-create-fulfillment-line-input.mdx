---
api_reference: true
id: order-bulk-create-fulfillment-line-input
title: OrderBulkCreateFulfillmentLineInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input OrderBulkCreateFulfillmentLineInput {
  variantId: ID
  variantSku: String
  variantExternalReference: String
  quantity: Int!
  warehouse: ID!
  orderLineIndex: Int!
}
```

### Fields

#### [<code>variantId</code>](#variant-id)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#variant-id\}

The ID of the product variant.

#### [<code>variantSku</code>](#variant-sku)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#variant-sku\}

The SKU of the product variant.

#### [<code>variantExternalReference</code>](#variant-external-reference)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#variant-external-reference\}

The external ID of the product variant.

#### [<code>quantity</code>](#quantity)<Bullet />[<code>Int!</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#quantity\}

The number of line items to be fulfilled from given warehouse.

#### [<code>warehouse</code>](#warehouse)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#warehouse\}

ID of the warehouse from which the item will be fulfilled.

#### [<code>orderLineIndex</code>](#order-line-index)<Bullet />[<code>Int!</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#order-line-index\}

0-based index of order line, which the fulfillment line refers to.

### Member Of

[`OrderBulkCreateFulfillmentInput`](/api-reference/orders/inputs/order-bulk-create-fulfillment-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
