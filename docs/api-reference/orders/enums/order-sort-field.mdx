---
api_reference: true
id: order-sort-field
title: OrderSortField
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>NUMBER</code>](#)

Sort orders by number.

#### [<code>RANK</code>](#)

Sort orders by rank. Note: This option is available only with the `search` filter.

#### [<code>CREATED_AT</code>](#)

Sort orders by creation date.

#### [<code>LAST_MODIFIED_AT</code>](#)

Sort orders by last modified date.

#### [<code>CUSTOMER</code>](#)

Sort orders by customer.

#### [<code>PAYMENT</code>](#)

Sort orders by payment status.

#### [<code>STATUS</code>](#)

Sort orders by order status.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>CREATION_DATE</code>](#)

:::warning[DEPRECATED]

Use `CREATED_AT` instead.

:::

Sort orders by creation date

#### [<code>FULFILLMENT_STATUS</code>](#)

:::warning[DEPRECATED]

Use `STATUS` instead.

:::

Sort orders by fulfillment status.

</Details>

### Member Of

[`OrderSortingInput`](/api-reference/orders/inputs/order-sorting-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
