---
api_reference: true
id: order-line-update
title: orderLineUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates an order line of an order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
orderLineUpdate(
  id: ID!
  input: OrderLineInput!
): OrderLineUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type OrderLineUpdate {
  order: Order
  orderErrors: [OrderError!]! @deprecated
  errors: [OrderError!]!
  orderLine: OrderLine
}
```

</Details>

### Arguments

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

ID of the order line to update.

#### [<code>input</code>](#input)<Bullet />[<code>OrderLineInput!</code>](/api-reference/orders/inputs/order-line-input.mdx) \{#input\}

Fields required to update an order line.

### Type

#### [<code>OrderLineUpdate</code>](/api-reference/orders/objects/order-line-update.mdx)

Updates an order line of an order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>
