---
api_reference: true
id: order-bulk-create
title: orderBulkCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Creates multiple orders.

<Permissions
  permissions={"MANAGE_ORDERS_IMPORT"}
  text={"Requires one of the following permissions"}
/>

```graphql
orderBulkCreate(
  errorPolicy: ErrorPolicyEnum
  orders: [OrderBulkCreateInput!]!
  stockUpdatePolicy: StockUpdatePolicyEnum
): OrderBulkCreate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type OrderBulkCreate {
  count: Int!
  results: [OrderBulkCreateResult!]!
  errors: [OrderBulkCreateError!]!
}
```

</Details>

### Arguments

#### [<code>errorPolicy</code>](#)<Bullet />[<code>ErrorPolicyEnum</code>](/api-reference/miscellaneous/enums/error-policy-enum.mdx)

Policies of error handling. DEFAULT: REJECT_EVERYTHING

#### [<code>orders</code>](#)<Bullet />[<code>[OrderBulkCreateInput!]!</code>](/api-reference/orders/inputs/order-bulk-create-input.mdx)

Input list of orders to create. Orders limit: 50.

#### [<code>stockUpdatePolicy</code>](#)<Bullet />[<code>StockUpdatePolicyEnum</code>](/api-reference/miscellaneous/enums/stock-update-policy-enum.mdx)

Determine how stock should be updated, while processing the order. DEFAULT: UPDATE - Only do update, if there is enough stocks.

### Type

#### [<code>OrderBulkCreate</code>](/api-reference/orders/objects/order-bulk-create.mdx)

Creates multiple orders.

<Permissions
  permissions={"MANAGE_ORDERS_IMPORT"}
  text={"Requires one of the following permissions"}
/>
