---
api_reference: true
id: order-fulfillment-update-tracking
title: orderFulfillmentUpdateTracking
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates a fulfillment for an order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- FULFILLMENT_TRACKING_NUMBER_UPDATED (async): Fulfillment tracking number is updated.

```graphql
orderFulfillmentUpdateTracking(
  id: ID!
  input: FulfillmentUpdateTrackingInput!
): FulfillmentUpdateTracking
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type FulfillmentUpdateTracking {
  fulfillment: Fulfillment
  order: Order
  orderErrors: [OrderError!]! @deprecated
  errors: [OrderError!]!
}
```

</Details>

### Arguments

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of a fulfillment to update.

#### [<code>input</code>](#)<Bullet />[<code>FulfillmentUpdateTrackingInput!</code>](/api-reference/orders/inputs/fulfillment-update-tracking-input.mdx)

Fields required to update a fulfillment.

### Type

#### [<code>FulfillmentUpdateTracking</code>](/api-reference/orders/objects/fulfillment-update-tracking.mdx)

Updates a fulfillment for an order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- FULFILLMENT_TRACKING_NUMBER_UPDATED (async): Fulfillment tracking number is updated.
