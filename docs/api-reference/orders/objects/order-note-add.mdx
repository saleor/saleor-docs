---
api_reference: true
id: order-note-add
title: OrderNoteAdd
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Adds note to the order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type OrderNoteAdd {
  order: Order
  event: OrderEvent
  errors: [OrderNoteAddError!]!
}
```

### Fields

#### [<code>order</code>](#)<Bullet />[<code>Order</code>](/api-reference/orders/objects/order.mdx)

Order with the note added.

#### [<code>event</code>](#)<Bullet />[<code>OrderEvent</code>](/api-reference/orders/objects/order-event.mdx)

Order note created.

#### [<code>errors</code>](#)<Bullet />[<code>[OrderNoteAddError!]!</code>](/api-reference/orders/objects/order-note-add-error.mdx)

### Returned By

[`orderNoteAdd`](/api-reference/orders/mutations/order-note-add.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
