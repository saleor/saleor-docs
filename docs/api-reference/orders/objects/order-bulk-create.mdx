---
id: order-bulk-create
title: OrderBulkCreate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Creates multiple orders.

<Badge text="Added in Saleor 3.14" class="secondary margin-bottom--sm" />

<FeaturePreview />

<Permissions
  permissions={"MANAGE_ORDERS_IMPORT"}
  text={"Requires one of the following permissions"}
/>

```graphql
type OrderBulkCreate {
  count: Int!
  results: [OrderBulkCreateResult!]!
  errors: [OrderBulkCreateError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>OrderBulkCreate.<b>count</b></code>](#)<Bullet />[`Int!`](../../../api-reference/miscellaneous/scalars/int) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> Returns how many objects were created.

#### [<code style={{ fontWeight: 'normal' }}>OrderBulkCreate.<b>results</b></code>](#)<Bullet />[`[OrderBulkCreateResult!]!`](../../../api-reference/orders/objects/order-bulk-create-result) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

> List of the created orders.

#### [<code style={{ fontWeight: 'normal' }}>OrderBulkCreate.<b>errors</b></code>](#)<Bullet />[`[OrderBulkCreateError!]!`](../../../api-reference/orders/objects/order-bulk-create-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

>

### Returned by

[`orderBulkCreate`](../../../api-reference/orders/mutations/order-bulk-create) <Badge class="secondary" text="mutation"/>

import FeaturePreview from "@site/components/FeaturePreview";

import Permissions from "@site/components/Permissions";
