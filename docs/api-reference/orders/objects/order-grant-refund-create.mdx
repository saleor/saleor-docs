---
api_reference: true
id: order-grant-refund-create
title: OrderGrantRefundCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Adds granted refund to the order.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type OrderGrantRefundCreate {
  order: Order
  grantedRefund: OrderGrantedRefund
  errors: [OrderGrantRefundCreateError!]!
}
```

### Fields

#### [<code>order</code>](#order)<Bullet />[<code>Order</code>](/api-reference/orders/objects/order.mdx) \{#order\}

Order which has assigned new grant refund.

#### [<code>grantedRefund</code>](#granted-refund)<Bullet />[<code>OrderGrantedRefund</code>](/api-reference/orders/objects/order-granted-refund.mdx) \{#granted-refund\}

Created granted refund.

#### [<code>errors</code>](#errors)<Bullet />[<code>[OrderGrantRefundCreateError!]!</code>](/api-reference/orders/objects/order-grant-refund-create-error.mdx) \{#errors\}

### Returned By

[`orderGrantRefundCreate`](/api-reference/orders/mutations/order-grant-refund-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
