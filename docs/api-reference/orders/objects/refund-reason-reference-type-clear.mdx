---
api_reference: true
id: refund-reason-reference-type-clear
title: RefundReasonReferenceTypeClear
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates RefundSettings. The `Page` (Model) Type will be cleared from `reasonReferenceType`. When it's cleared, passing reason reference to refund mutations is no longer accepted and will raise error.

Added in Saleor 3.22.

<Permissions
  permissions={"MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type RefundReasonReferenceTypeClear {
  refundSettings: RefundSettings!
  refundSettingsErrors: [RefundReasonReferenceTypeClearError!]! @deprecated
  errors: [RefundReasonReferenceTypeClearError!]!
}
```

### Fields

#### [<code>refundSettings</code>](#)<Bullet />[<code>RefundSettings!</code>](/api-reference/orders/objects/refund-settings.mdx)

Refund settings.

#### [<code>errors</code>](#)<Bullet />[<code>[RefundReasonReferenceTypeClearError!]!</code>](/api-reference/shop/objects/refund-reason-reference-type-clear-error.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>refundSettingsErrors</code>](#)<Bullet />[<code>[RefundReasonReferenceTypeClearError!]!</code>](/api-reference/shop/objects/refund-reason-reference-type-clear-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`refundReasonReferenceClear`](/api-reference/orders/mutations/refund-reason-reference-clear.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
