---
api_reference: true
id: order-grant-refund-update-error
title: OrderGrantRefundUpdateError
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
type OrderGrantRefundUpdateError {
  field: String
  message: String
  code: OrderGrantRefundUpdateErrorCode!
  addLines: [OrderGrantRefundUpdateLineError!]
  removeLines: [OrderGrantRefundUpdateLineError!]
}
```

### Fields

#### [<code>field</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of a field that caused the error. A value of `null` indicates that the error isn't associated with a particular field.

#### [<code>message</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The error message.

#### [<code>code</code>](#)<Bullet />[<code>OrderGrantRefundUpdateErrorCode!</code>](/api-reference/orders/enums/order-grant-refund-update-error-code.mdx)

The error code.

#### [<code>addLines</code>](#)<Bullet />[<code>[OrderGrantRefundUpdateLineError!]</code>](/api-reference/miscellaneous/objects/order-grant-refund-update-line-error.mdx)

List of lines to add which cause the error.

#### [<code>removeLines</code>](#)<Bullet />[<code>[OrderGrantRefundUpdateLineError!]</code>](/api-reference/miscellaneous/objects/order-grant-refund-update-line-error.mdx)

List of lines to remove which cause the error.

### Member Of

[`OrderGrantRefundUpdate`](/api-reference/orders/objects/order-grant-refund-update.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
