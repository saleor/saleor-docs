---
api_reference: true
id: promotion-rule-update
title: promotionRuleUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates an existing promotion rule.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- PROMOTION_RULE_UPDATED (async): A promotion rule was updated.

```graphql
promotionRuleUpdate(
  id: ID!
  input: PromotionRuleUpdateInput!
): PromotionRuleUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type PromotionRuleUpdate {
  errors: [PromotionRuleUpdateError!]!
  promotionRule: PromotionRule
}
```

</Details>

### Arguments

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the promotion rule to update.

#### [<code>input</code>](#)<Bullet />[<code>PromotionRuleUpdateInput!</code>](/api-reference/miscellaneous/inputs/promotion-rule-update-input.mdx)

Fields required to create a promotion rule.

### Type

#### [<code>PromotionRuleUpdate</code>](/api-reference/discounts/objects/promotion-rule-update.mdx)

Updates an existing promotion rule.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- PROMOTION_RULE_UPDATED (async): A promotion rule was updated.
