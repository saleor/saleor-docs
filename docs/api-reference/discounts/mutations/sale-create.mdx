---
api_reference: true
id: sale-create
title: saleCreate
---

export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="\_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
const [open, setOpen] = useState(startOpen);
return (
<details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
<summary
onClick={(e) => {
e.preventDefault();
setOpen((open) => !open);
}}
style={{ listStyle:'none' }} >
{open ? dataOpen : dataClose}
</summary>
{open && children}
</details>
);
};

:::warning[DEPRECATED]

Use `promotionCreate` mutation instead.

:::

Creates a new sale.


<Permissions permissions={"MANAGE_DISCOUNTS"} text={"Requires one of the following permissions"} />


Triggers the following webhook events:

- SALE_CREATED (async): A sale was created.

```graphql
saleCreate(
  input: SaleInput!
): SaleCreate @deprecated
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>saleCreate.<b>input</b></code>](#)<Bullet />[`SaleInput!`](../../../api-reference/discounts/inputs/sale-input.mdx) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/> <Badge class="badge badge--secondary" text="discounts"/>

Fields required to create a sale.

### Type

#### [`SaleCreate`](../../../api-reference/discounts/objects/sale-create.mdx) <Badge class="badge badge--secondary" text="object"/> <Badge class="badge badge--secondary" text="discounts"/>

Creates a new sale.


<Permissions permissions={"MANAGE_DISCOUNTS"} text={"Requires one of the following permissions"} />


Triggers the following webhook events:

- SALE_CREATED (async): A sale was created.

import Permissions from "@site/components/Permissions";
