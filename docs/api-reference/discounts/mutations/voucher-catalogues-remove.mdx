---
api_reference: true
id: voucher-catalogues-remove
title: voucherCataloguesRemove
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Removes products, categories, collections from a voucher.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- VOUCHER_UPDATED (async): A voucher was updated.

```graphql
voucherCataloguesRemove(
  id: ID!
  input: CatalogueInput!
): VoucherRemoveCatalogues
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type VoucherRemoveCatalogues {
  voucher: Voucher
  discountErrors: [DiscountError!]! @deprecated
  errors: [DiscountError!]!
}
```

</Details>

### Arguments

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

ID of a voucher.

#### [<code>input</code>](#input)<Bullet />[<code>CatalogueInput!</code>](/api-reference/discounts/inputs/catalogue-input.mdx) \{#input\}

Fields required to modify catalogue IDs of voucher.

### Type

#### [<code>VoucherRemoveCatalogues</code>](/api-reference/discounts/objects/voucher-remove-catalogues.mdx)

Removes products, categories, collections from a voucher.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- VOUCHER_UPDATED (async): A voucher was updated.
