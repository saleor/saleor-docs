---
id: voucher-translatable-content
title: VoucherTranslatableContent
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
type VoucherTranslatableContent implements Node {
  id: ID!
  name: String
  translation(languageCode: LanguageCodeEnum!): VoucherTranslation
  voucher: Voucher
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>VoucherTranslatableContent.<b>id</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

>

#### [<code style={{ fontWeight: 'normal' }}>VoucherTranslatableContent.<b>name</b></code>](#)<Bullet />[`String`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="scalar"/>

>

#### [<code style={{ fontWeight: 'normal' }}>VoucherTranslatableContent.<b>translation</b></code>](#)<Bullet />[`VoucherTranslation`](../../../api-reference/miscellaneous/objects/voucher-translation) <Badge class="secondary" text="object"/>

> Returns translated voucher fields for the given language code.
>
> ##### [<code style={{ fontWeight: 'normal' }}>VoucherTranslatableContent.translation.<b>languageCode</b></code>](#)<Bullet />[`LanguageCodeEnum!`](../../../api-reference/miscellaneous/enums/language-code-enum) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="enum"/>
>
> A language code to return the translation for voucher.

#### [<code style={{ fontWeight: 'normal' }}>VoucherTranslatableContent.<b>voucher</b></code>](#)<Bullet />[`Voucher`](../../../api-reference/discounts/objects/voucher) <Badge class="secondary" text="deprecated"/> <Badge class="secondary" text="object"/>

> <Badge
>   class="warning"
>   text="DEPRECATED: This field will be removed in Saleor 4.0. Get model fields from the root level queries."
> />
>
> Vouchers allow giving discounts to particular customers on categories, collections or specific products. They can be used during checkout by providing valid voucher codes.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

### Interfaces

#### [`Node`](../../../api-reference/miscellaneous/interfaces/node) <Badge class="secondary" text="interface"/>

> An object with an ID

### Implemented by

[`TranslatableItem`](../../../api-reference/miscellaneous/unions/translatable-item) <Badge class="secondary" text="union"/>

import Permissions from "@site/components/Permissions";
