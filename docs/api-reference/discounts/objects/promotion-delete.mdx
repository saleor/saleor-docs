---
api_reference: true
id: promotion-delete
title: PromotionDelete
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Deletes a promotion.

<Permissions
  permissions={"MANAGE_DISCOUNTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- PROMOTION_DELETED (async): A promotion was deleted.

```graphql
type PromotionDelete {
  errors: [PromotionDeleteError!]!
  promotion: Promotion
}
```

### Fields

#### [<code>errors</code>](#)<Bullet />[<code>[PromotionDeleteError!]!</code>](/api-reference/miscellaneous/objects/promotion-delete-error.mdx)

#### [<code>promotion</code>](#)<Bullet />[<code>Promotion</code>](/api-reference/discounts/objects/promotion.mdx)

### Returned By

[`promotionDelete`](/api-reference/discounts/mutations/promotion-delete.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
