---
api_reference: true
id: export-voucher-codes-input
title: ExportVoucherCodesInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input ExportVoucherCodesInput {
  voucherId: ID
  ids: [ID!]
  fileType: FileTypesEnum!
}
```

### Fields

#### [<code>voucherId</code>](#)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the voucher. If provided, exports all codes belonging to the voucher.

#### [<code>ids</code>](#)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx)

List of voucher code IDs to export.

#### [<code>fileType</code>](#)<Bullet />[<code>FileTypesEnum!</code>](/api-reference/miscellaneous/enums/file-types-enum.mdx)

Type of exported file.

### Member Of

[`exportVoucherCodes`](/api-reference/deprecated/discounts/mutations/export-voucher-codes.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
