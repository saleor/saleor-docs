---
api_reference: true
id: channel-update
title: ChannelUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Update a channel.

<Permissions
  permissions={"MANAGE_CHANNELS"}
  text={"Requires one of the following permissions"}
/>

<Permissions
  permissions={"`MANAGE_CHANNELS`,`MANAGE_ORDERS`"}
  text={
    "Requires one of the following permissions when updating only `orderSettings` field"
  }
/>

<Permissions
  permissions={"`MANAGE_CHANNELS`,`MANAGE_CHECKOUTS`"}
  text={
    "Requires one of the following permissions when updating only `checkoutSettings` field"
  }
/>

<Permissions
  permissions={"`MANAGE_CHANNELS`,`HANDLE_PAYMENTS`"}
  text={
    "Requires one of the following permissions when updating only `paymentSettings` field"
  }
/>

Triggers the following webhook events:

- CHANNEL_UPDATED (async): A channel was updated.
- CHANNEL_METADATA_UPDATED (async): Optionally triggered when public or private metadata is updated.

```graphql
type ChannelUpdate {
  channelErrors: [ChannelError!]! @deprecated
  errors: [ChannelError!]!
  channel: Channel
}
```

### Fields

#### [<code>errors</code>](#)<Bullet />[<code>[ChannelError!]!</code>](/api-reference/channels/objects/channel-error.mdx)

#### [<code>channel</code>](#)<Bullet />[<code>Channel</code>](/api-reference/channels/objects/channel.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>channelErrors</code>](#)<Bullet />[<code>[ChannelError!]!</code>](/api-reference/channels/objects/channel-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`channelUpdate`](/api-reference/channels/mutations/channel-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
