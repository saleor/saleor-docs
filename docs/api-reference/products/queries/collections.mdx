---
api_reference: true
id: collections
title: collections
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

List of the shop's collections.

<Permissions
  permissions={"MANAGE_ORDERS,MANAGE_DISCOUNTS,MANAGE_PRODUCTS"}
  text={
    "Requires one of the following permissions to include the unpublished items"
  }
/>

```graphql
collections(
  filter: CollectionFilterInput
  where: CollectionWhereInput
  sortBy: CollectionSortingInput
  channel: String
  before: String
  after: String
  first: Int
  last: Int
): CollectionCountableConnection
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type CollectionCountableConnection {
  pageInfo: PageInfo!
  edges: [CollectionCountableEdge!]!
  totalCount: Int
}
```

</Details>

### Arguments

#### [<code>filter</code>](#filter)<Bullet />[<code>CollectionFilterInput</code>](/api-reference/products/inputs/collection-filter-input.mdx) \{#filter\}

Filtering options for collections.

#### [<code>where</code>](#where)<Bullet />[<code>CollectionWhereInput</code>](/api-reference/products/inputs/collection-where-input.mdx) \{#where\}

Where filtering options for collections.

#### [<code>sortBy</code>](#sort-by)<Bullet />[<code>CollectionSortingInput</code>](/api-reference/products/inputs/collection-sorting-input.mdx) \{#sort-by\}

Sort collections.

#### [<code>channel</code>](#channel)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#channel\}

Slug of a channel for which the data should be returned.

#### [<code>before</code>](#before)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#before\}

Return the elements in the list that come before the specified cursor.

#### [<code>after</code>](#after)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#after\}

Return the elements in the list that come after the specified cursor.

#### [<code>first</code>](#first)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#first\}

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>last</code>](#last)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#last\}

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

### Type

#### [<code>CollectionCountableConnection</code>](/api-reference/products/objects/collection-countable-connection.mdx)

Represents a connection to a list of collections.
