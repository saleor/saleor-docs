---
api_reference: true
id: categories
title: categories
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

List of the shop's categories.

```graphql
categories(
  filter: CategoryFilterInput
  where: CategoryWhereInput
  sortBy: CategorySortingInput
  level: Int
  before: String
  after: String
  first: Int
  last: Int
): CategoryCountableConnection
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type CategoryCountableConnection {
  pageInfo: PageInfo!
  edges: [CategoryCountableEdge!]!
  totalCount: Int
}
```

</Details>

### Arguments

#### [<code>filter</code>](#filter)<Bullet />[<code>CategoryFilterInput</code>](/api-reference/products/inputs/category-filter-input.mdx) \{#filter\}

Filtering options for categories.

#### [<code>where</code>](#where)<Bullet />[<code>CategoryWhereInput</code>](/api-reference/products/inputs/category-where-input.mdx) \{#where\}

Where filtering options for categories.

#### [<code>sortBy</code>](#sort-by)<Bullet />[<code>CategorySortingInput</code>](/api-reference/products/inputs/category-sorting-input.mdx) \{#sort-by\}

Sort categories.

#### [<code>level</code>](#level)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#level\}

Filter categories by the nesting level in the category tree.

#### [<code>before</code>](#before)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#before\}

Return the elements in the list that come before the specified cursor.

#### [<code>after</code>](#after)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#after\}

Return the elements in the list that come after the specified cursor.

#### [<code>first</code>](#first)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#first\}

Retrieve the first n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

#### [<code>last</code>](#last)<Bullet />[<code>Int</code>](/api-reference/miscellaneous/scalars/int.mdx) \{#last\}

Retrieve the last n elements from the list. Note that the system only allows fetching a maximum of 100 objects in a single query.

### Type

#### [<code>CategoryCountableConnection</code>](/api-reference/products/objects/category-countable-connection.mdx)
