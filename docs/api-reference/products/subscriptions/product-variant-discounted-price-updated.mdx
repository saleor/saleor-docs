---
api_reference: true
id: product-variant-discounted-price-updated
title: productVariantDiscountedPriceUpdated
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Event sent when product variant discounted price is recalculated.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

<FeaturePreview />

```graphql
productVariantDiscountedPriceUpdated(
  channels: [String!]
): ProductVariantDiscountedPriceUpdated
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ProductVariantDiscountedPriceUpdated implements Event {
  issuedAt: DateTime
  version: String
  issuingPrincipal: IssuingPrincipal
  recipient: App
  productVariant: ProductVariant!
  channel: Channel!
  previousPrice: Money!
  newPrice: Money!
}
```

</Details>

### Arguments

#### [<code>channels</code>](#channels)<Bullet />[<code>[String!]</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#channels\}

List of channel slugs. The event will be sent only if the object belongs to one of the provided channels. If the channel slug list is empty, objects that belong to any channel will be sent. Maximally 500 items.

### Type

#### [<code>ProductVariantDiscountedPriceUpdated</code>](/api-reference/products/objects/product-variant-discounted-price-updated.mdx)

Event sent when product variant discounted price is recalculated.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>
