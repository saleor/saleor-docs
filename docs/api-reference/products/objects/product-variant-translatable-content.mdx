---
api_reference: true
id: product-variant-translatable-content
title: ProductVariantTranslatableContent
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents product variant's original translatable fields and related translations.

```graphql
type ProductVariantTranslatableContent implements Node {
  id: ID!
  productVariantId: ID!
  name: String!
  translation(languageCode: LanguageCodeEnum!): ProductVariantTranslation
  productVariant: ProductVariant @deprecated
  attributeValues: [AttributeValueTranslatableContent!]!
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the product variant translatable content.

#### [<code>productVariantId</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the product variant to translate.

#### [<code>name</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Name of the product variant to translate.

#### [<code>translation</code>](#)<Bullet />[<code>ProductVariantTranslation</code>](/api-reference/products/objects/product-variant-translation.mdx)

Returns translated product variant fields for the given language code.

##### [<code>languageCode</code>](#)<Bullet />[<code>LanguageCodeEnum!</code>](/api-reference/miscellaneous/enums/language-code-enum.mdx)

A language code to return the translation for product variant.

#### [<code>attributeValues</code>](#)<Bullet />[<code>[AttributeValueTranslatableContent!]!</code>](/api-reference/attributes/objects/attribute-value-translatable-content.mdx)

List of product variant attribute values that can be translated.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>productVariant</code>](#)<Bullet />[<code>ProductVariant</code>](/api-reference/products/objects/product-variant.mdx)

:::warning[DEPRECATED]

Get model fields from the root level queries.

:::

Represents a version of a product such as different size or color.

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`ProductVariantTranslation`](/api-reference/products/objects/product-variant-translation.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>

### Implemented By

[`TranslatableItem`](/api-reference/miscellaneous/unions/translatable-item.mdx) <Badge class="badge badge--secondary badge--relation" text="union"/>
