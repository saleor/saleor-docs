---
api_reference: true
id: variant-pricing-info
title: VariantPricingInfo
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents availability of a variant in the storefront.

```graphql
type VariantPricingInfo {
  onSale: Boolean
  discount: TaxedMoney
  discountPrior: TaxedMoney
  discountLocalCurrency: TaxedMoney @deprecated
  price: TaxedMoney
  priceUndiscounted: TaxedMoney
  pricePrior: TaxedMoney
  priceLocalCurrency: TaxedMoney @deprecated
}
```

### Fields

#### [<code>onSale</code>](#)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Whether it is in sale or not.

#### [<code>discount</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

The discount amount if in sale (null otherwise).

#### [<code>discountPrior</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

The discount amount compared to prior price. Null if product is not on sale or prior price was not provided in VariantChannelListing

<Badge
  text="Added in Saleor 3.21"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>price</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

The price, with any discount subtracted.

#### [<code>priceUndiscounted</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

The price without any discount.

#### [<code>pricePrior</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

The price prior to discount.

<Badge
  text="Added in Saleor 3.21"
  class="badge badge--secondary margin-bottom--sm"
/>

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>discountLocalCurrency</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

:::warning[DEPRECATED]

Always returns `null`.

:::

The discount amount in the local currency.

#### [<code>priceLocalCurrency</code>](#)<Bullet />[<code>TaxedMoney</code>](/api-reference/miscellaneous/objects/taxed-money.mdx)

:::warning[DEPRECATED]

Always returns `null`.

:::

The discounted price in the local currency.

</Details>

### Member Of

[`ProductVariant`](/api-reference/products/objects/product-variant.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
