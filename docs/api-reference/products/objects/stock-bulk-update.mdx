---
id: stock-bulk-update
title: StockBulkUpdate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Updates stocks for a given variant and warehouse.

<Badge text="Added in Saleor 3.13" class="secondary margin-bottom--sm" />

<FeaturePreview />

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type StockBulkUpdate {
  count: Int!
  results: [StockBulkResult!]!
  errors: [StockBulkUpdateError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>StockBulkUpdate.<b>count</b></code>](#)<Bullet />[`Int!`](../../../api-reference/miscellaneous/scalars/int) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> Returns how many objects were updated.

#### [<code style={{ fontWeight: 'normal' }}>StockBulkUpdate.<b>results</b></code>](#)<Bullet />[`[StockBulkResult!]!`](../../../api-reference/products/objects/stock-bulk-result) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

> List of the updated stocks.

#### [<code style={{ fontWeight: 'normal' }}>StockBulkUpdate.<b>errors</b></code>](#)<Bullet />[`[StockBulkUpdateError!]!`](../../../api-reference/products/objects/stock-bulk-update-error) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="object"/>

>

### Returned by

[`stockBulkUpdate`](../../../api-reference/products/mutations/stock-bulk-update) <Badge class="secondary" text="mutation"/>

import FeaturePreview from "@site/components/FeaturePreview";

import Permissions from "@site/components/Permissions";
