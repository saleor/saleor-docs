---
api_reference: true
id: product-channel-listing
title: ProductChannelListing
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents product channel listing.

```graphql
type ProductChannelListing implements Node {
  id: ID!
  publicationDate: Date @deprecated
  publishedAt: DateTime
  isPublished: Boolean!
  channel: Channel!
  visibleInListings: Boolean!
  availableForPurchase: Date @deprecated
  availableForPurchaseAt: DateTime
  discountedPrice: Money
  purchaseCost: MoneyRange
  margin: Margin
  isAvailableForPurchase: Boolean
  pricing(address: AddressInput): ProductPricingInfo
}
```

### Fields

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

The ID of the product channel listing.

#### [<code>publishedAt</code>](#published-at)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx) \{#published-at\}

The product publication date time.

#### [<code>isPublished</code>](#is-published)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-published\}

Indicates if the product is published in the channel.

#### [<code>channel</code>](#channel)<Bullet />[<code>Channel!</code>](/api-reference/channels/objects/channel.mdx) \{#channel\}

The channel in which the product is listed.

#### [<code>visibleInListings</code>](#visible-in-listings)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#visible-in-listings\}

Indicates product visibility in the channel listings.

#### [<code>availableForPurchaseAt</code>](#available-for-purchase-at)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx) \{#available-for-purchase-at\}

The product available for purchase date time.

#### [<code>discountedPrice</code>](#discounted-price)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx) \{#discounted-price\}

The price of the cheapest variant (including discounts).

#### [<code>purchaseCost</code>](#purchase-cost)<Bullet />[<code>MoneyRange</code>](/api-reference/miscellaneous/objects/money-range.mdx) \{#purchase-cost\}

Purchase cost of product.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

#### [<code>margin</code>](#margin)<Bullet />[<code>Margin</code>](/api-reference/products/objects/margin.mdx) \{#margin\}

Range of margin percentage value.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

#### [<code>isAvailableForPurchase</code>](#is-available-for-purchase)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-available-for-purchase\}

Refers to a state that can be set by admins to control whether a product is available for purchase in storefronts in this channel. This does not guarantee the availability of stock. When set to `False`, this product is still visible to customers, but it cannot be purchased.

#### [<code>pricing</code>](#pricing)<Bullet />[<code>ProductPricingInfo</code>](/api-reference/products/objects/product-pricing-info.mdx) \{#pricing\}

Lists the storefront product's pricing, the current price and discounts, only meant for displaying.

##### [<code>address</code>](#product-channel-listing-pricing-address)<Bullet />[<code>AddressInput</code>](/api-reference/miscellaneous/inputs/address-input.mdx) \{#product-channel-listing-pricing-address\}

Destination address used to find warehouses where stock availability for this product is checked. If address is empty, uses `Shop.companyAddress` or fallbacks to server's `settings.DEFAULT_COUNTRY` configuration.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>publicationDate</code>](#publication-date)<Bullet />[<code>Date</code>](/api-reference/miscellaneous/scalars/date.mdx) \{#publication-date\}

:::warning[DEPRECATED]

Use the `publishedAt` field to fetch the publication date.

:::

#### [<code>availableForPurchase</code>](#available-for-purchase)<Bullet />[<code>Date</code>](/api-reference/miscellaneous/scalars/date.mdx) \{#available-for-purchase\}

:::warning[DEPRECATED]

Use the `availableForPurchaseAt` field to fetch the available for purchase date.

:::

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`Product`](/api-reference/products/objects/product.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
