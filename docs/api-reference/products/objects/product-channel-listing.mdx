---
api_reference: true
id: product-channel-listing
title: ProductChannelListing
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents product channel listing.

```graphql
type ProductChannelListing implements Node {
  id: ID!
  publicationDate: Date @deprecated
  publishedAt: DateTime
  isPublished: Boolean!
  channel: Channel!
  visibleInListings: Boolean!
  availableForPurchase: Date @deprecated
  availableForPurchaseAt: DateTime
  discountedPrice: Money
  purchaseCost: MoneyRange
  margin: Margin
  isAvailableForPurchase: Boolean
  pricing(address: AddressInput): ProductPricingInfo
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the product channel listing.

#### [<code>publishedAt</code>](#)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

The product publication date time.

#### [<code>isPublished</code>](#)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Indicates if the product is published in the channel.

#### [<code>channel</code>](#)<Bullet />[<code>Channel!</code>](/api-reference/channels/objects/channel.mdx)

The channel in which the product is listed.

#### [<code>visibleInListings</code>](#)<Bullet />[<code>Boolean!</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Indicates product visibility in the channel listings.

#### [<code>availableForPurchaseAt</code>](#)<Bullet />[<code>DateTime</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

The product available for purchase date time.

#### [<code>discountedPrice</code>](#)<Bullet />[<code>Money</code>](/api-reference/miscellaneous/objects/money.mdx)

The price of the cheapest variant (including discounts).

#### [<code>purchaseCost</code>](#)<Bullet />[<code>MoneyRange</code>](/api-reference/miscellaneous/objects/money-range.mdx)

Purchase cost of product.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

#### [<code>margin</code>](#)<Bullet />[<code>Margin</code>](/api-reference/products/objects/margin.mdx)

Range of margin percentage value.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

#### [<code>isAvailableForPurchase</code>](#)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx)

Refers to a state that can be set by admins to control whether a product is available for purchase in storefronts in this channel. This does not guarantee the availability of stock. When set to `False`, this product is still visible to customers, but it cannot be purchased.

#### [<code>pricing</code>](#)<Bullet />[<code>ProductPricingInfo</code>](/api-reference/products/objects/product-pricing-info.mdx)

Lists the storefront product's pricing, the current price and discounts, only meant for displaying.

##### [<code>address</code>](#)<Bullet />[<code>AddressInput</code>](/api-reference/miscellaneous/inputs/address-input.mdx)

Destination address used to find warehouses where stock availability for this product is checked. If address is empty, uses `Shop.companyAddress` or fallbacks to server's `settings.DEFAULT_COUNTRY` configuration.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>publicationDate</code>](#)<Bullet />[<code>Date</code>](/api-reference/miscellaneous/scalars/date.mdx)

:::warning[DEPRECATED]

Use the `publishedAt` field to fetch the publication date.

:::

#### [<code>availableForPurchase</code>](#)<Bullet />[<code>Date</code>](/api-reference/miscellaneous/scalars/date.mdx)

:::warning[DEPRECATED]

Use the `availableForPurchaseAt` field to fetch the available for purchase date.

:::

</Details>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`Product`](/api-reference/products/objects/product.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
