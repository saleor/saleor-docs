---
api_reference: true
id: export-products
title: ExportProducts
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Export products to csv file.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- NOTIFY_USER (async): A notification for the exported file.
- PRODUCT_EXPORT_COMPLETED (async): A notification for the exported file.

```graphql
type ExportProducts {
  exportFile: ExportFile
  exportErrors: [ExportError!]! @deprecated
  errors: [ExportError!]!
}
```

### Fields

#### [<code>exportFile</code>](#)<Bullet />[<code>ExportFile</code>](/api-reference/miscellaneous/objects/export-file.mdx)

The newly created export file job which is responsible for export data.

#### [<code>errors</code>](#)<Bullet />[<code>[ExportError!]!</code>](/api-reference/miscellaneous/objects/export-error.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>exportErrors</code>](#)<Bullet />[<code>[ExportError!]!</code>](/api-reference/miscellaneous/objects/export-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`exportProducts`](/api-reference/deprecated/products/mutations/export-products.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
