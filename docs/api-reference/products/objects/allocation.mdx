---
api_reference: true
id: allocation
title: Allocation
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents allocation.

```graphql
type Allocation implements Node {
  id: ID!
  quantity: Int!
  warehouse: Warehouse!
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of allocation.

#### [<code>quantity</code>](#)<Bullet />[<code>Int!</code>](/api-reference/miscellaneous/scalars/int.mdx)

Quantity allocated for orders.

<Permissions
  permissions={"MANAGE_PRODUCTS,MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

#### [<code>warehouse</code>](#)<Bullet />[<code>Warehouse!</code>](/api-reference/products/objects/warehouse.mdx)

The warehouse were items were allocated.

<Permissions
  permissions={"MANAGE_PRODUCTS,MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Member Of

[`OrderLine`](/api-reference/orders/objects/order-line.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
