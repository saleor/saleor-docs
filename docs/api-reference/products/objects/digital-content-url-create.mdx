---
api_reference: true
id: digital-content-url-create
title: DigitalContentUrlCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Generate new URL to digital content.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type DigitalContentUrlCreate {
  productErrors: [ProductError!]! @deprecated
  errors: [ProductError!]!
  digitalContentUrl: DigitalContentUrl
}
```

### Fields

#### [<code>errors</code>](#)<Bullet />[<code>[ProductError!]!</code>](/api-reference/products/objects/product-error.mdx)

#### [<code>digitalContentUrl</code>](#)<Bullet />[<code>DigitalContentUrl</code>](/api-reference/products/objects/digital-content-url.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>productErrors</code>](#)<Bullet />[<code>[ProductError!]!</code>](/api-reference/products/objects/product-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`digitalContentUrlCreate`](/api-reference/deprecated/products/mutations/digital-content-url-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
