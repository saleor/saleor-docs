---
api_reference: true
id: product-variant-bulk-create
title: productVariantBulkCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Creates product variants for a given product.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
productVariantBulkCreate(
  errorPolicy: ErrorPolicyEnum
  product: ID!
  variants: [ProductVariantBulkCreateInput!]!
): ProductVariantBulkCreate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ProductVariantBulkCreate {
  count: Int!
  productVariants: [ProductVariant!]!
  results: [ProductVariantBulkResult!]!
  bulkProductErrors: [BulkProductError!]! @deprecated
  errors: [BulkProductError!]!
}
```

</Details>

### Arguments

#### [<code>errorPolicy</code>](#)<Bullet />[<code>ErrorPolicyEnum</code>](/api-reference/miscellaneous/enums/error-policy-enum.mdx)

Policies of error handling. DEFAULT: REJECT_EVERYTHING

#### [<code>product</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the product to create the variants for.

#### [<code>variants</code>](#)<Bullet />[<code>[ProductVariantBulkCreateInput!]!</code>](/api-reference/products/inputs/product-variant-bulk-create-input.mdx)

Input list of product variants to create.

### Type

#### [<code>ProductVariantBulkCreate</code>](/api-reference/products/objects/product-variant-bulk-create.mdx)

Creates product variants for a given product.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>
