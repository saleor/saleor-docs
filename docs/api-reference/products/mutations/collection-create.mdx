---
id: collection-create
title: collectionCreate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Creates a new collection.

Requires one of the following permissions: MANAGE_PRODUCTS.

```graphql
collectionCreate(
  input: CollectionCreateInput!
): CollectionCreate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>collectionCreate.<b>input</b></code>](#)<Bullet />[`CollectionCreateInput!`](../../../api-reference/products/inputs/collection-create-input) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="input"/> <Badge class="secondary" text="products"/>

> Fields required to create a collection.

### Type

#### [`CollectionCreate`](../../../api-reference/products/objects/collection-create) <Badge class="secondary" text="object"/> <Badge class="secondary" text="products"/>

> Creates a new collection.

Requires one of the following permissions: MANAGE_PRODUCTS.

>
