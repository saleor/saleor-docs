---
api_reference: true
id: product-bulk-create
title: productBulkCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Creates products.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
productBulkCreate(
  errorPolicy: ErrorPolicyEnum
  products: [ProductBulkCreateInput!]!
): ProductBulkCreate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ProductBulkCreate {
  count: Int!
  results: [ProductBulkResult!]!
  errors: [ProductBulkCreateError!]!
}
```

</Details>

### Arguments

#### [<code>errorPolicy</code>](#)<Bullet />[<code>ErrorPolicyEnum</code>](/api-reference/miscellaneous/enums/error-policy-enum.mdx)

Policies of error handling. DEFAULT: REJECT_EVERYTHING

#### [<code>products</code>](#)<Bullet />[<code>[ProductBulkCreateInput!]!</code>](/api-reference/products/inputs/product-bulk-create-input.mdx)

Input list of products to create.

### Type

#### [<code>ProductBulkCreate</code>](/api-reference/products/objects/product-bulk-create.mdx)

Creates products.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>
