---
api_reference: true
id: product-variant-bulk-delete
title: productVariantBulkDelete
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Deletes product variants.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
productVariantBulkDelete(
  ids: [ID!]
  skus: [String!]
): ProductVariantBulkDelete
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ProductVariantBulkDelete {
  count: Int!
  productErrors: [ProductError!]! @deprecated
  errors: [ProductError!]!
}
```

</Details>

### Arguments

#### [<code>ids</code>](#)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx)

List of product variant IDs to delete.

#### [<code>skus</code>](#)<Bullet />[<code>[String!]</code>](/api-reference/miscellaneous/scalars/string.mdx)

List of product variant SKUs to delete.

### Type

#### [<code>ProductVariantBulkDelete</code>](/api-reference/products/objects/product-variant-bulk-delete.mdx)

Deletes product variants.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>
