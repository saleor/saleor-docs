---
api_reference: true
id: collection-reorder-products
title: collectionReorderProducts
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Reorder the products of a collection.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
collectionReorderProducts(
  collectionId: ID!
  moves: [MoveProductInput!]!
): CollectionReorderProducts
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type CollectionReorderProducts {
  collection: Collection
  collectionErrors: [CollectionError!]! @deprecated
  errors: [CollectionError!]!
}
```

</Details>

### Arguments

#### [<code>collectionId</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of a collection.

#### [<code>moves</code>](#)<Bullet />[<code>[MoveProductInput!]!</code>](/api-reference/products/inputs/move-product-input.mdx)

The collection products position operations.

### Type

#### [<code>CollectionReorderProducts</code>](/api-reference/products/objects/collection-reorder-products.mdx)

Reorder the products of a collection.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>
