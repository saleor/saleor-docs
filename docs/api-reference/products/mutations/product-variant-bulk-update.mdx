---
api_reference: true
id: product-variant-bulk-update
title: productVariantBulkUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates multiple product variants.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>

```graphql
productVariantBulkUpdate(
  errorPolicy: ErrorPolicyEnum
  product: ID!
  variants: [ProductVariantBulkUpdateInput!]!
): ProductVariantBulkUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type ProductVariantBulkUpdate {
  count: Int!
  results: [ProductVariantBulkResult!]!
  errors: [ProductVariantBulkError!]!
}
```

</Details>

### Arguments

#### [<code>errorPolicy</code>](#)<Bullet />[<code>ErrorPolicyEnum</code>](/api-reference/miscellaneous/enums/error-policy-enum.mdx)

Policies of error handling. DEFAULT: REJECT_EVERYTHING

#### [<code>product</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of the product to update the variants for.

#### [<code>variants</code>](#)<Bullet />[<code>[ProductVariantBulkUpdateInput!]!</code>](/api-reference/products/inputs/product-variant-bulk-update-input.mdx)

Input list of product variants to update.

### Type

#### [<code>ProductVariantBulkUpdate</code>](/api-reference/products/objects/product-variant-bulk-update.mdx)

Updates multiple product variants.

<Permissions
  permissions={"MANAGE_PRODUCTS"}
  text={"Requires one of the following permissions"}
/>
