---
api_reference: true
id: product-type-input
title: ProductTypeInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
input ProductTypeInput {
  name: String
  slug: String
  kind: ProductTypeKindEnum
  hasVariants: Boolean @deprecated
  productAttributes: [ID!]
  variantAttributes: [ID!]
  isShippingRequired: Boolean
  isDigital: Boolean
  weight: WeightScalar
  taxCode: String @deprecated
  taxClass: ID
}
```

### Fields

#### [<code>name</code>](#name)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#name\}

Name of the product type.

#### [<code>slug</code>](#slug)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#slug\}

Product type slug.

#### [<code>kind</code>](#kind)<Bullet />[<code>ProductTypeKindEnum</code>](/api-reference/products/enums/product-type-kind-enum.mdx) \{#kind\}

The product type kind.

#### [<code>productAttributes</code>](#product-attributes)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#product-attributes\}

List of attributes shared among all product variants.

#### [<code>variantAttributes</code>](#variant-attributes)<Bullet />[<code>[ID!]</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#variant-attributes\}

List of attributes used to distinguish between different variants of a product.

#### [<code>isShippingRequired</code>](#is-shipping-required)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-shipping-required\}

Determines if shipping is required for products of this variant.

#### [<code>isDigital</code>](#is-digital)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#is-digital\}

Determines if products are digital.

#### [<code>weight</code>](#weight)<Bullet />[<code>WeightScalar</code>](/api-reference/miscellaneous/scalars/weight-scalar.mdx) \{#weight\}

Weight of the ProductType items.

#### [<code>taxClass</code>](#tax-class)<Bullet />[<code>ID</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#tax-class\}

ID of a tax class to assign to this product type. All products of this product type would use this tax class, unless it's overridden in the `Product` type.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>hasVariants</code>](#has-variants)<Bullet />[<code>Boolean</code>](/api-reference/miscellaneous/scalars/boolean.mdx) \{#has-variants\}

:::warning[DEPRECATED]

The field has no effect on the API behavior. This is a leftover from the past Simple/Configurable product distinction. Products can have multiple variants regardless of this setting.

:::

Determines if product of this type has multiple variants. This option mainly simplifies product management in the dashboard. There is always at least one variant created under the hood.

#### [<code>taxCode</code>](#tax-code)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#tax-code\}

:::warning[DEPRECATED]

Use tax classes to control the tax calculation for a product type. If taxCode is provided, Saleor will try to find a tax class with given code (codes are stored in metadata) and assign it. If no tax class is found, it would be created and assigned.

:::

Tax rate for enabled tax gateway.

</Details>

### Member Of

[`productTypeCreate`](/api-reference/products/mutations/product-type-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/><Bullet />[`productTypeUpdate`](/api-reference/products/mutations/product-type-update.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
