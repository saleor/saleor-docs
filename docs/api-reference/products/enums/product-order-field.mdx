---
api_reference: true
id: product-order-field
title: ProductOrderField
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>NAME</code>](#name) \{#name\}

Sort products by name.

#### [<code>RANK</code>](#rank) \{#rank\}

Sort products by rank. Note: This option is available only with the `search` filter.

#### [<code>PRICE</code>](#price) \{#price\}

Sort products by price.

This option requires a channel filter to work as the values can vary between channels.

#### [<code>MINIMAL_PRICE</code>](#minimal-price) \{#minimal-price\}

Sort products by a minimal price of a product's variant.

This option requires a channel filter to work as the values can vary between channels.

#### [<code>TYPE</code>](#type) \{#type\}

Sort products by type.

#### [<code>PUBLISHED</code>](#published) \{#published\}

Sort products by publication status.

This option requires a channel filter to work as the values can vary between channels.

#### [<code>PUBLISHED_AT</code>](#published-at) \{#published-at\}

Sort products by publication date.

This option requires a channel filter to work as the values can vary between channels.

#### [<code>LAST_MODIFIED_AT</code>](#last-modified-at) \{#last-modified-at\}

Sort products by update date.

#### [<code>COLLECTION</code>](#collection) \{#collection\}

Sort products by collection. Note: This option is available only for the `Collection.products` query.

This option requires a channel filter to work as the values can vary between channels.

#### [<code>RATING</code>](#rating) \{#rating\}

Sort products by rating.

#### [<code>CREATED_AT</code>](#created-at) \{#created-at\}

Sort products by creation date.

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>LAST_MODIFIED</code>](#last-modified) \{#last-modified\}

:::warning[DEPRECATED]

Use `LAST_MODIFIED_AT` instead.

:::

Sort products by update date.

#### [<code>DATE</code>](#date) \{#date\}

:::warning[DEPRECATED]

Use `LAST_MODIFIED_AT` instead.

:::

Sort products by update date.

#### [<code>PUBLICATION_DATE</code>](#publication-date) \{#publication-date\}

:::warning[DEPRECATED]

Use `PUBLISHED_AT` instead.

:::

Sort products by publication date.

This option requires a channel filter to work as the values can vary between channels.

</Details>

### Member Of

[`ProductOrder`](/api-reference/products/inputs/product-order.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
