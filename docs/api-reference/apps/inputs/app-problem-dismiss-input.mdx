---
api_reference: true
id: app-problem-dismiss-input
title: AppProblemDismissInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Input for dismissing app problems. Only one can be specified.

```graphql
input AppProblemDismissInput {
  byApp: AppProblemDismissByAppInput
  byStaffWithIds: AppProblemDismissByStaffWithIdsInput
  byStaffWithKeys: AppProblemDismissByStaffWithKeysInput
}
```

### Fields

#### [<code>byApp</code>](#)<Bullet />[<code>AppProblemDismissByAppInput</code>](/api-reference/apps/inputs/app-problem-dismiss-by-app-input.mdx)

For app callers only - dismiss own problems.

#### [<code>byStaffWithIds</code>](#)<Bullet />[<code>AppProblemDismissByStaffWithIdsInput</code>](/api-reference/apps/inputs/app-problem-dismiss-by-staff-with-ids-input.mdx)

For staff callers - dismiss problems by IDs.

#### [<code>byStaffWithKeys</code>](#)<Bullet />[<code>AppProblemDismissByStaffWithKeysInput</code>](/api-reference/apps/inputs/app-problem-dismiss-by-staff-with-keys-input.mdx)

For staff callers - dismiss problems by keys for specified app.

### Member Of

[`appProblemDismiss`](/api-reference/apps/mutations/app-problem-dismiss.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
