---
api_reference: true
id: app-create
title: AppCreate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Creates a new app.

<Permissions
  permissions={"AUTHENTICATED_STAFF_USER and MANAGE_APPS"}
  text={"Requires the following permissions"}
/>

Triggers the following webhook events:

- APP_INSTALLED (async): An app was installed.

```graphql
type AppCreate {
  authToken: String
  appErrors: [AppError!]! @deprecated
  errors: [AppError!]!
  app: App
}
```

### Fields

#### [<code>authToken</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

The newly created authentication token.

#### [<code>errors</code>](#)<Bullet />[<code>[AppError!]!</code>](/api-reference/apps/objects/app-error.mdx)

#### [<code>app</code>](#)<Bullet />[<code>App</code>](/api-reference/apps/objects/app.mdx)

<Details dataOpen="Hide deprecated" dataClose="Show deprecated">

#### [<code>appErrors</code>](#)<Bullet />[<code>[AppError!]!</code>](/api-reference/apps/objects/app-error.mdx)

:::warning[DEPRECATED]

Use `errors` field instead.

:::

</Details>

### Returned By

[`appCreate`](/api-reference/apps/mutations/app-create.mdx) <Badge class="badge badge--secondary badge--relation" text="mutation"/>
