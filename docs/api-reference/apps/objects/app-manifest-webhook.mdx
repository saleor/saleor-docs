---
id: app-manifest-webhook
title: AppManifestWebhook
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={"badge badge--" + props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

No description

```graphql
type AppManifestWebhook {
  name: String!
  asyncEvents: [WebhookEventTypeAsyncEnum!]
  syncEvents: [WebhookEventTypeSyncEnum!]
  query: String!
  targetUrl: String!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>AppManifestWebhook.<b>name</b></code>](#)<Bullet />[`String!`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> The name of the webhook.

#### [<code style={{ fontWeight: 'normal' }}>AppManifestWebhook.<b>asyncEvents</b></code>](#)<Bullet />[`[WebhookEventTypeAsyncEnum!]`](../../../api-reference/webhooks/enums/webhook-event-type-async-enum) <Badge class="secondary" text="list"/> <Badge class="secondary" text="enum"/>

> The asynchronous events that webhook wants to subscribe.

#### [<code style={{ fontWeight: 'normal' }}>AppManifestWebhook.<b>syncEvents</b></code>](#)<Bullet />[`[WebhookEventTypeSyncEnum!]`](../../../api-reference/webhooks/enums/webhook-event-type-sync-enum) <Badge class="secondary" text="list"/> <Badge class="secondary" text="enum"/>

> The synchronous events that webhook wants to subscribe.

#### [<code style={{ fontWeight: 'normal' }}>AppManifestWebhook.<b>query</b></code>](#)<Bullet />[`String!`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> Subscription query of a webhook

#### [<code style={{ fontWeight: 'normal' }}>AppManifestWebhook.<b>targetUrl</b></code>](#)<Bullet />[`String!`](../../../api-reference/miscellaneous/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> The url to receive the payload.

### Member of

[`Manifest`](../../../api-reference/apps/objects/manifest) <Badge class="secondary" text="object"/>
