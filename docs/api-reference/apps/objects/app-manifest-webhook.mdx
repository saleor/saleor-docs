---
api_reference: true
id: app-manifest-webhook
title: AppManifestWebhook
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

```graphql
type AppManifestWebhook {
  name: String!
  asyncEvents: [WebhookEventTypeAsyncEnum!]
  syncEvents: [WebhookEventTypeSyncEnum!]
  query: String!
  targetUrl: String!
}
```

### Fields

#### [<code>name</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of the webhook.

#### [<code>asyncEvents</code>](#)<Bullet />[<code>[WebhookEventTypeAsyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-async-enum.mdx)

The asynchronous events that webhook wants to subscribe.

#### [<code>syncEvents</code>](#)<Bullet />[<code>[WebhookEventTypeSyncEnum!]</code>](/api-reference/webhooks/enums/webhook-event-type-sync-enum.mdx)

The synchronous events that webhook wants to subscribe.

#### [<code>query</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

Subscription query of a webhook

#### [<code>targetUrl</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

The url to receive the payload.

### Member Of

[`Manifest`](/api-reference/apps/objects/manifest.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
