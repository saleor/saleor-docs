---
api_reference: true
id: app-installation
title: AppInstallation
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents ongoing installation of app.

```graphql
type AppInstallation implements Node, Job {
  id: ID!
  status: JobStatusEnum!
  createdAt: DateTime!
  updatedAt: DateTime!
  message: String
  appName: String!
  manifestUrl: String!
  brand: AppBrand
}
```

### Fields

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

The ID of the app installation.

#### [<code>status</code>](#)<Bullet />[<code>JobStatusEnum!</code>](/api-reference/miscellaneous/enums/job-status-enum.mdx)

Job status.

#### [<code>createdAt</code>](#)<Bullet />[<code>DateTime!</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

Created date time of job in ISO 8601 format.

#### [<code>updatedAt</code>](#)<Bullet />[<code>DateTime!</code>](/api-reference/miscellaneous/scalars/date-time.mdx)

Date time of job last update in ISO 8601 format.

#### [<code>message</code>](#)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx)

Job message.

#### [<code>appName</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

The name of the app installation.

#### [<code>manifestUrl</code>](#)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx)

The URL address of manifest for the app installation.

#### [<code>brand</code>](#)<Bullet />[<code>AppBrand</code>](/api-reference/apps/objects/app-brand.mdx)

App's brand data.

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

#### [<code>Job</code>](/api-reference/miscellaneous/interfaces/job.mdx)

### Returned By

[`appsInstallations`](/api-reference/apps/queries/apps-installations.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>

### Member Of

[`AppDeleteFailedInstallation`](/api-reference/apps/objects/app-delete-failed-installation.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`AppInstall`](/api-reference/apps/objects/app-install.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`AppRetryInstall`](/api-reference/apps/objects/app-retry-install.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
