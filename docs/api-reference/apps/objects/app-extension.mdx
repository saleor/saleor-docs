---
api_reference: true
id: app-extension
title: AppExtension
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Represents app data.

```graphql
type AppExtension implements Node {
  id: ID!
  permissions: [Permission!]!
  label: String!
  url: String!
  mountName: String!
  targetName: String!
  settings: JSON!
  app: App!
  accessToken: String
}
```

### Fields

#### [<code>id</code>](#id)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx) \{#id\}

The ID of the app extension.

#### [<code>permissions</code>](#permissions)<Bullet />[<code>[Permission!]!</code>](/api-reference/authentication/objects/permission.mdx) \{#permissions\}

List of the app extension's permissions.

#### [<code>label</code>](#label)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#label\}

Label of the extension to show in the dashboard.

#### [<code>url</code>](#url)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#url\}

URL of a view where extension's iframe is placed.

#### [<code>mountName</code>](#mount-name)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#mount-name\}

Name of the extension mount point in the dashboard. Value returned in UPPERCASE.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>targetName</code>](#target-name)<Bullet />[<code>String!</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#target-name\}

Name of the extension target in the dashboard. Value returned in UPPERCASE.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>settings</code>](#settings)<Bullet />[<code>JSON!</code>](/api-reference/miscellaneous/scalars/json.mdx) \{#settings\}

App extension settings.

<Badge
  text="Added in Saleor 3.22"
  class="badge badge--secondary margin-bottom--sm"
/>

#### [<code>app</code>](#app)<Bullet />[<code>App!</code>](/api-reference/apps/objects/app.mdx) \{#app\}

The app assigned to app extension.

#### [<code>accessToken</code>](#access-token)<Bullet />[<code>String</code>](/api-reference/miscellaneous/scalars/string.mdx) \{#access-token\}

JWT token used to authenticate by third-party app extension.

### Interfaces

#### [<code>Node</code>](/api-reference/miscellaneous/interfaces/node.mdx)

An object with an ID

### Returned By

[`appExtension`](/api-reference/apps/queries/app-extension.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>

### Member Of

[`App`](/api-reference/apps/objects/app.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`AppExtensionCountableEdge`](/api-reference/apps/objects/app-extension-countable-edge.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
