---
api_reference: true
id: app-install
title: appInstall
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Install new app by using app manifest.

<Permissions
  permissions={"AUTHENTICATED_STAFF_USER and MANAGE_APPS"}
  text={"Requires the following permissions"}
/>

```graphql
appInstall(
  input: AppInstallInput!
): AppInstall
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type AppInstall {
  appErrors: [AppError!]! @deprecated
  errors: [AppError!]!
  appInstallation: AppInstallation
}
```

</Details>

### Arguments

#### [<code>input</code>](#input)<Bullet />[<code>AppInstallInput!</code>](/api-reference/apps/inputs/app-install-input.mdx) \{#input\}

Fields required to install a new app.

### Type

#### [<code>AppInstall</code>](/api-reference/apps/objects/app-install.mdx)

Install new app by using app manifest.

<Permissions
  permissions={"AUTHENTICATED_STAFF_USER and MANAGE_APPS"}
  text={"Requires the following permissions"}
/>
