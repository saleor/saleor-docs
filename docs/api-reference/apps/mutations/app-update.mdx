---
api_reference: true
id: app-update
title: appUpdate
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

Updates an existing app.

<Permissions
  permissions={"MANAGE_APPS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- APP_UPDATED (async): An app was updated.

```graphql
appUpdate(
  id: ID!
  input: AppInput!
): AppUpdate
```

<Details dataOpen="Hide return type" dataClose="Show return type">

```graphql
type AppUpdate {
  appErrors: [AppError!]! @deprecated
  errors: [AppError!]!
  app: App
}
```

</Details>

### Arguments

#### [<code>id</code>](#)<Bullet />[<code>ID!</code>](/api-reference/miscellaneous/scalars/id.mdx)

ID of an app to update.

#### [<code>input</code>](#)<Bullet />[<code>AppInput!</code>](/api-reference/apps/inputs/app-input.mdx)

Fields required to update an existing app.

### Type

#### [<code>AppUpdate</code>](/api-reference/apps/objects/app-update.mdx)

Updates an existing app.

<Permissions
  permissions={"MANAGE_APPS"}
  text={"Requires one of the following permissions"}
/>

Triggers the following webhook events:

- APP_UPDATED (async): An app was updated.
