---
api_reference: true
id: app-error-code
title: AppErrorCode
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

import FeaturePreview from "@site/components/FeaturePreview";
import Permissions from "@site/components/Permissions";

No description

### Values

#### [<code>FORBIDDEN</code>](#forbidden) \{#forbidden\}

#### [<code>GRAPHQL_ERROR</code>](#graphql-error) \{#graphql-error\}

#### [<code>INVALID</code>](#invalid) \{#invalid\}

#### [<code>INVALID_STATUS</code>](#invalid-status) \{#invalid-status\}

#### [<code>INVALID_PERMISSION</code>](#invalid-permission) \{#invalid-permission\}

#### [<code>INVALID_URL_FORMAT</code>](#invalid-url-format) \{#invalid-url-format\}

#### [<code>INVALID_MANIFEST_FORMAT</code>](#invalid-manifest-format) \{#invalid-manifest-format\}

#### [<code>INVALID_CUSTOM_HEADERS</code>](#invalid-custom-headers) \{#invalid-custom-headers\}

#### [<code>MANIFEST_URL_CANT_CONNECT</code>](#manifest-url-cant-connect) \{#manifest-url-cant-connect\}

#### [<code>NOT_FOUND</code>](#not-found) \{#not-found\}

#### [<code>REQUIRED</code>](#required) \{#required\}

#### [<code>UNIQUE</code>](#unique) \{#unique\}

#### [<code>OUT_OF_SCOPE_APP</code>](#out-of-scope-app) \{#out-of-scope-app\}

#### [<code>OUT_OF_SCOPE_PERMISSION</code>](#out-of-scope-permission) \{#out-of-scope-permission\}

#### [<code>UNSUPPORTED_SALEOR_VERSION</code>](#unsupported-saleor-version) \{#unsupported-saleor-version\}

### Member Of

[`AppError`](/api-reference/apps/objects/app-error.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
