---
id: account-update
title: accountUpdate
---

Updates the account of the logged-in user.

```graphql
accountUpdate(
  input: AccountInput!
): AccountUpdate

```

### Arguments

#### `input` ([`AccountInput!`](/developer/api-reference/inputs/account-input.mdx))

Fields required to update the account of the logged-in user.

### Type

#### [`AccountUpdate`](/developer/api-reference/objects/account-update.mdx)

Updates the account of the logged-in user.
