---
id: set-password
title: setPassword
---

Sets the user's password from the token sent by email using the RequestPasswordReset mutation.

```graphql
setPassword(
  email: String!
  password: String!
  token: String!
): SetPassword

```

### Arguments

#### `email` ([`String!`](/docs/developer/api-reference/scalars/string))

Email of a user.

#### `password` ([`String!`](/docs/developer/api-reference/scalars/string))

Password of a user.

#### `token` ([`String!`](/docs/developer/api-reference/scalars/string))

A one-time token required to set the password.

### Type

#### [`SetPassword`](/docs/developer/api-reference/objects/set-password)

Sets the user's password from the token sent by email using the RequestPasswordReset mutation.
