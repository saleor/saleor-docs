---
id: confirm-email-change
title: confirmEmailChange
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Confirm the email change of the logged-in user.

Requires one of the following permissions: AUTHENTICATED_USER.

```graphql
confirmEmailChange(
  channel: String
  token: String!
): ConfirmEmailChange
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>confirmEmailChange.<b>channel</b></code>](#)<Bullet />[`String`](../../../developer/api-reference/scalars/string) <Badge class="secondary" text="scalar"/>

> Slug of a channel which will be used to notify users. Optional when only one channel exists.

#### [<code style={{ fontWeight: 'normal' }}>confirmEmailChange.<b>token</b></code>](#)<Bullet />[`String!`](../../../developer/api-reference/scalars/string) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> A one-time token required to change the email.

### Type

#### [`ConfirmEmailChange`](../../../developer/api-reference/objects/confirm-email-change) <Badge class="secondary" text="object"/>

> Confirm the email change of the logged-in user.

Requires one of the following permissions: AUTHENTICATED_USER.

>
