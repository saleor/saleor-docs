---
id: user-avatar-update
title: userAvatarUpdate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span class={"badge badge--" + props.class}>{props.text}</span>
  </>
);

Create a user avatar. Only for staff members. This mutation must be sent as a `multipart` request. More detailed specs of the upload format can be found here: https://github.com/jaydenseric/graphql-multipart-request-spec

Requires one of the following permissions: AUTHENTICATED_STAFF_USER.

```graphql
userAvatarUpdate(
  image: Upload!
): UserAvatarUpdate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>userAvatarUpdate.<b>image</b></code>](#)<Bullet />[`Upload!`](../../../developer/api-reference/scalars/upload) <Badge class="secondary" text="non-null"/> <Badge class="secondary" text="scalar"/>

> Represents an image file in a multipart request.

### Type

#### [`UserAvatarUpdate`](../../../developer/api-reference/objects/user-avatar-update) <Badge class="secondary" text="object"/>

> Create a user avatar. Only for staff members. This mutation must be sent as a `multipart` request. More detailed specs of the upload format can be found here: https://github.com/jaydenseric/graphql-multipart-request-spec

Requires one of the following permissions: AUTHENTICATED_STAFF_USER.

>
