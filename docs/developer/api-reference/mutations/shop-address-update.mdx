---
id: shop-address-update
title: shopAddressUpdate
---

Update the shop's address. If the `null` value is passed, the currently selected address will be deleted.

```graphql
shopAddressUpdate(
  input: AddressInput
): ShopAddressUpdate

```

### Arguments

#### `input` ([`AddressInput`](/docs/developer/api-reference/inputs/address-input))

Fields required to update shop address.

### Type

#### [`ShopAddressUpdate`](/docs/developer/api-reference/objects/shop-address-update)

Update the shop's address. If the `null` value is passed, the currently selected address will be deleted.
