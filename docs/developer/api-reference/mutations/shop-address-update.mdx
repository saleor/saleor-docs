---
id: shop-address-update
title: shopAddressUpdate
---

Update the shop's address. If the `null` value is passed, the currently selected address will be deleted. Requires one of the following permissions: MANAGE_SETTINGS.

```graphql
shopAddressUpdate(
  input: AddressInput
): ShopAddressUpdate

```

### Arguments

#### `input` ([`AddressInput`](../../../developer/api-reference/inputs/address-input))

Fields required to update shop address.

### Type

#### [`ShopAddressUpdate`](../../../developer/api-reference/objects/shop-address-update)

Update the shop's address. If the `null` value is passed, the currently selected address will be deleted. Requires one of the following permissions: MANAGE_SETTINGS.
