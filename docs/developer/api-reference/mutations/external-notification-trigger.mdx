---
id: external-notification-trigger
title: externalNotificationTrigger
---

New in Saleor 3.1. Trigger sending a notification with the notify plugin method. Serializes nodes provided as ids parameter and includes this data in the notification payload.

```graphql
externalNotificationTrigger(
  channel: String!
  input: ExternalNotificationTriggerInput!
  pluginId: String
): ExternalNotificationTrigger

```

### Arguments

#### `channel` ([`String!`](../../../developer/api-reference/scalars/string))

Channel slug. Saleor will send a notification within a provided channel. Please, make sure that necessary plugins are active.

#### `input` ([`ExternalNotificationTriggerInput!`](../../../developer/api-reference/inputs/external-notification-trigger-input))

Input for External Notification Trigger.

#### `pluginId` ([`String`](../../../developer/api-reference/scalars/string))

The ID of notification plugin.

### Type

#### [`ExternalNotificationTrigger`](../../../developer/api-reference/objects/external-notification-trigger)

New in Saleor 3.1. Trigger sending a notification with the notify plugin method. Serializes nodes provided as ids parameter and includes this data in the notification payload.
