---
id: app-retry-install
title: appRetryInstall
---

Retry failed installation of new app.

```graphql
appRetryInstall(
  activateAfterInstallation: Boolean = "true"
  id: ID!
): AppRetryInstall

```

### Arguments

#### `activateAfterInstallation` ([`Boolean`](/developer/api-reference/scalars/boolean.mdx))

Determine if app will be set active or not.

#### `id` ([`ID!`](/developer/api-reference/scalars/id.mdx))

ID of failed installation.

### Type

#### [`AppRetryInstall`](/developer/api-reference/objects/app-retry-install.mdx)

Retry failed installation of new app.
