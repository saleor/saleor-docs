---
id: attribute-reorder-values
title: attributeReorderValues
---

Reorder the values of an attribute.

```graphql
attributeReorderValues(
  attributeId: ID!
  moves: [ReorderInput]!
): AttributeReorderValues

```

### Arguments

#### `attributeId` ([`ID!`](/docs/developer/api-reference/scalars/id))

ID of an attribute.

#### `moves` ([`[ReorderInput]!`](/docs/developer/api-reference/inputs/reorder-input))

The list of reordering operations for given attribute values.

### Type

#### [`AttributeReorderValues`](/docs/developer/api-reference/objects/attribute-reorder-values)

Reorder the values of an attribute.
