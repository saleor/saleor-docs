---
id: collections
title: collections
---

List of the shop's collections.

```graphql
collections(
  filter: CollectionFilterInput
  sortBy: CollectionSortingInput
  channel: String
  before: String
  after: String
  first: Int
  last: Int
): CollectionCountableConnection

```

### Arguments

#### `filter` ([`CollectionFilterInput`](/docs/developer/api-reference/inputs/collection-filter-input))

Filtering options for collections.

#### `sortBy` ([`CollectionSortingInput`](/docs/developer/api-reference/inputs/collection-sorting-input))

Sort collections.

#### `channel` ([`String`](/docs/developer/api-reference/scalars/string))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`CollectionCountableConnection`](/docs/developer/api-reference/objects/collection-countable-connection)
