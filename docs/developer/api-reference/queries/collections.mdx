---
id: collections
title: collections
---

List of the shop's collections.

```graphql
collections(
  filter: CollectionFilterInput
  sortBy: CollectionSortingInput
  channel: String
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): CollectionCountableConnection

```

### Arguments

#### `filter` ([`CollectionFilterInput`](/developer/api-reference/inputs/collection-filter-input.mdx))

Filtering options for collections.

#### `sortBy` ([`CollectionSortingInput`](/developer/api-reference/inputs/collection-sorting-input.mdx))

Sort collections.

#### `channel` ([`String`](/developer/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`CollectionCountableConnection`](/developer/api-reference/objects/collection-countable-connection.mdx)
