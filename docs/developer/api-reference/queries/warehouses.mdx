---
id: warehouses
title: warehouses
---

List of warehouses.

```graphql
warehouses(
  filter: WarehouseFilterInput
  sortBy: WarehouseSortingInput
  before: String
  after: String
  first: Int
  last: Int
): WarehouseCountableConnection

```

### Arguments

#### `filter` ([`WarehouseFilterInput`](/docs/developer/api-reference/inputs/warehouse-filter-input))

#### `sortBy` ([`WarehouseSortingInput`](/docs/developer/api-reference/inputs/warehouse-sorting-input))

#### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`WarehouseCountableConnection`](/docs/developer/api-reference/objects/warehouse-countable-connection)
