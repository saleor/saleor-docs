---
id: warehouses
title: warehouses
---

List of warehouses.

```graphql
warehouses(
  filter: WarehouseFilterInput
  sortBy: WarehouseSortingInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): WarehouseCountableConnection

```

### Arguments

#### `filter` ([`WarehouseFilterInput`](/developer/api-reference/inputs/warehouse-filter-input.mdx))

#### `sortBy` ([`WarehouseSortingInput`](/developer/api-reference/inputs/warehouse-sorting-input.mdx))

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`WarehouseCountableConnection`](/developer/api-reference/objects/warehouse-countable-connection.mdx)
