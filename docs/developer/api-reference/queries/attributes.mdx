---
id: attributes
title: attributes
---

List of the shop's attributes.

```graphql
attributes(
  filter: AttributeFilterInput
  sortBy: AttributeSortingInput
  channel: String
  before: String
  after: String
  first: Int
  last: Int
): AttributeCountableConnection

```

### Arguments

#### `filter` ([`AttributeFilterInput`](../../../developer/api-reference/inputs/attribute-filter-input))

Filtering options for attributes.

#### `sortBy` ([`AttributeSortingInput`](../../../developer/api-reference/inputs/attribute-sorting-input))

Sorting options for attributes.

#### `channel` ([`String`](../../../developer/api-reference/scalars/string))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](../../../developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](../../../developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`AttributeCountableConnection`](../../../developer/api-reference/objects/attribute-countable-connection)
