---
id: attributes
title: attributes
---

List of the shop's attributes.

```graphql
attributes(
  filter: AttributeFilterInput
  sortBy: AttributeSortingInput
  channel: String
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): AttributeCountableConnection

```

### Arguments

#### `filter` ([`AttributeFilterInput`](/developer/api-reference/inputs/attribute-filter-input.mdx))

Filtering options for attributes.

#### `sortBy` ([`AttributeSortingInput`](/developer/api-reference/inputs/attribute-sorting-input.mdx))

Sorting options for attributes.

#### `channel` ([`String`](/developer/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`AttributeCountableConnection`](/developer/api-reference/objects/attribute-countable-connection.mdx)
