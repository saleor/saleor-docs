---
id: app-extensions
title: appExtensions
---

New in Saleor 3.1. List of all extensions

```graphql
appExtensions(
  filter: AppExtensionFilterInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): AppExtensionCountableConnection

```

### Arguments

#### `filter` ([`AppExtensionFilterInput`](/developer/api-reference/inputs/app-extension-filter-input.mdx))

Filtering options for apps extensions.

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`AppExtensionCountableConnection`](/developer/api-reference/objects/app-extension-countable-connection.mdx)
