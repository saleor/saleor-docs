---
id: app-extensions
title: appExtensions
---

List of all extensions.

Added in Saleor 3.1.

Note: this API is currently in Feature Preview and can be subject to changes at later point.

Requires one of the following permissions: AUTHENTICATED_STAFF_USER, AUTHENTICATED_APP.

```graphql
appExtensions(
  filter: AppExtensionFilterInput
  before: String
  after: String
  first: Int
  last: Int
): AppExtensionCountableConnection
```

### Arguments

#### `filter` ([`AppExtensionFilterInput`](../../../developer/api-reference/inputs/app-extension-filter-input))

Filtering options for apps extensions.

#### `before` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](../../../developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](../../../developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`AppExtensionCountableConnection`](../../../developer/api-reference/objects/app-extension-countable-connection)
