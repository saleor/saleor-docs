---
id: app-extensions
title: appExtensions
---

New in Saleor 3.1. List of all extensions

```graphql
appExtensions(
  filter: AppExtensionFilterInput
  before: String
  after: String
  first: Int
  last: Int
): AppExtensionCountableConnection

```

### Arguments

#### `filter` ([`AppExtensionFilterInput`](/docs/developer/api-reference/inputs/app-extension-filter-input))

Filtering options for apps extensions.

#### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`AppExtensionCountableConnection`](/docs/developer/api-reference/objects/app-extension-countable-connection)
