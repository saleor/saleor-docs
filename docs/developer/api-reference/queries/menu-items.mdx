---
id: menu-items
title: menuItems
---

List of the storefronts's menu items.

```graphql
menuItems(
  channel: String
  sortBy: MenuItemSortingInput
  filter: MenuItemFilterInput
  before: String
  after: String
  first: Int
  last: Int
): MenuItemCountableConnection

```

### Arguments

#### `channel` ([`String`](/docs/developer/api-reference/scalars/string))

Slug of a channel for which the data should be returned.

#### `sortBy` ([`MenuItemSortingInput`](/docs/developer/api-reference/inputs/menu-item-sorting-input))

Sort menus items.

#### `filter` ([`MenuItemFilterInput`](/docs/developer/api-reference/inputs/menu-item-filter-input))

Filtering options for menu items.

#### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`MenuItemCountableConnection`](/docs/developer/api-reference/objects/menu-item-countable-connection)
