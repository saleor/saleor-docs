---
id: menu-items
title: menuItems
---

List of the storefronts's menu items.

```graphql
menuItems(
  channel: String
  sortBy: MenuItemSortingInput
  filter: MenuItemFilterInput
  before: String
  after: String
  first: Int
  last: Int
): MenuItemCountableConnection

```

### Arguments

#### `channel` ([`String`](../../../developer/api-reference/scalars/string))

Slug of a channel for which the data should be returned.

#### `sortBy` ([`MenuItemSortingInput`](../../../developer/api-reference/inputs/menu-item-sorting-input))

Sort menus items.

#### `filter` ([`MenuItemFilterInput`](../../../developer/api-reference/inputs/menu-item-filter-input))

Filtering options for menu items.

#### `before` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](../../../developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](../../../developer/api-reference/scalars/int))

Return the first n elements from the list.

#### `last` ([`Int`](../../../developer/api-reference/scalars/int))

Return the last n elements from the list.

### Type

#### [`MenuItemCountableConnection`](../../../developer/api-reference/objects/menu-item-countable-connection)
