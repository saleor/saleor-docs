---
id: product-variants
title: productVariants
---

List of product variants.

```graphql
productVariants(
  ids: [ID] = []
  channel: String
  filter: ProductVariantFilterInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): ProductVariantCountableConnection

```

### Arguments

#### `ids` ([`[ID]`](/developer/api-reference/scalars/id.mdx))

Filter product variants by given IDs.

#### `channel` ([`String`](/developer/api-reference/scalars/string.mdx))

Slug of a channel for which the data should be returned.

#### `filter` ([`ProductVariantFilterInput`](/developer/api-reference/inputs/product-variant-filter-input.mdx))

Filtering options for product variant.

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`ProductVariantCountableConnection`](/developer/api-reference/objects/product-variant-countable-connection.mdx)
