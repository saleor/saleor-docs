---
id: page-types
title: pageTypes
---

List of the page types.

```graphql
pageTypes(
  sortBy: PageTypeSortingInput
  filter: PageTypeFilterInput
  before: String
  after: String
  first: Int = 0
  last: Int = 0
): PageTypeCountableConnection

```

### Arguments

#### `sortBy` ([`PageTypeSortingInput`](/developer/api-reference/inputs/page-type-sorting-input.mdx))

Sort page types.

#### `filter` ([`PageTypeFilterInput`](/developer/api-reference/inputs/page-type-filter-input.mdx))

Filtering options for page types.

#### `before` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come before the specified cursor.

#### `after` ([`String`](/developer/api-reference/scalars/string.mdx))

Return the elements in the list that come after the specified cursor.

#### `first` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the first n elements from the list.

#### `last` ([`Int`](/developer/api-reference/scalars/int.mdx))

Return the last n elements from the list.

### Type

#### [`PageTypeCountableConnection`](/developer/api-reference/objects/page-type-countable-connection.mdx)
