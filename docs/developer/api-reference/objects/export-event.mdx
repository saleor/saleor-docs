---
id: export-event
title: ExportEvent
---

History log of export file.

```graphql
type ExportEvent implements Node {
  id: ID!
  date: DateTime!
  type: ExportEventsEnum!
  user: User
  app: App
  message: String!
}
```

### Fields

#### `id` ([`ID!`](/developer/api-reference/scalars/id.mdx))

The ID of the object.

#### `date` ([`DateTime!`](/developer/api-reference/scalars/date-time.mdx))

Date when event happened at in ISO 8601 format.

#### `type` ([`ExportEventsEnum!`](/developer/api-reference/enums/export-events-enum.mdx))

Export event type.

#### `user` ([`User`](/developer/api-reference/objects/user.mdx))

User who performed the action.

#### `app` ([`App`](/developer/api-reference/objects/app.mdx))

App which performed the action.

#### `message` ([`String!`](/developer/api-reference/scalars/string.mdx))

Content of the event.

### Interfaces

#### [`Node`](/developer/api-reference/interfaces/node.mdx)

An object with an ID
