---
id: product-pricing-info
title: ProductPricingInfo
---

Represents availability of a product in the storefront.

```graphql
type ProductPricingInfo {
  onSale: Boolean
  discount: TaxedMoney
  discountLocalCurrency: TaxedMoney
  priceRange: TaxedMoneyRange
  priceRangeUndiscounted: TaxedMoneyRange
  priceRangeLocalCurrency: TaxedMoneyRange
}
```

### Fields

#### `onSale` ([`Boolean`](/docs/developer/api-reference/scalars/boolean))

Whether it is in sale or not.

#### `discount` ([`TaxedMoney`](/docs/developer/api-reference/objects/taxed-money))

The discount amount if in sale (null otherwise).

#### `discountLocalCurrency` ([`TaxedMoney`](/docs/developer/api-reference/objects/taxed-money))

The discount amount in the local currency.

#### `priceRange` ([`TaxedMoneyRange`](/docs/developer/api-reference/objects/taxed-money-range))

The discounted price range of the product variants.

#### `priceRangeUndiscounted` ([`TaxedMoneyRange`](/docs/developer/api-reference/objects/taxed-money-range))

The undiscounted price range of the product variants.

#### `priceRangeLocalCurrency` ([`TaxedMoneyRange`](/docs/developer/api-reference/objects/taxed-money-range))

The discounted price range of the product variants in the local currency.
