---
id: credit-card
title: CreditCard
---

No description

```graphql
type CreditCard {
  brand: String!
  firstDigits: String
  lastDigits: String!
  expMonth: Int
  expYear: Int
}
```

### Fields

#### `brand` ([`String!`](/developer/api-reference/scalars/string.mdx))

Card brand.

#### `firstDigits` ([`String`](/developer/api-reference/scalars/string.mdx))

First 4 digits of the card number.

#### `lastDigits` ([`String!`](/developer/api-reference/scalars/string.mdx))

Last 4 digits of the card number.

#### `expMonth` ([`Int`](/developer/api-reference/scalars/int.mdx))

Two-digit number representing the card’s expiration month.

#### `expYear` ([`Int`](/developer/api-reference/scalars/int.mdx))

Four-digit number representing the card’s expiration year.
