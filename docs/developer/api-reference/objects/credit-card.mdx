---
id: credit-card
title: CreditCard
---

No description

```graphql
type CreditCard {
  brand: String!
  firstDigits: String
  lastDigits: String!
  expMonth: Int
  expYear: Int
}
```

### Fields

#### `brand` ([`String!`](/docs/developer/api-reference/scalars/string))

Card brand.

#### `firstDigits` ([`String`](/docs/developer/api-reference/scalars/string))

First 4 digits of the card number.

#### `lastDigits` ([`String!`](/docs/developer/api-reference/scalars/string))

Last 4 digits of the card number.

#### `expMonth` ([`Int`](/docs/developer/api-reference/scalars/int))

Two-digit number representing the card’s expiration month.

#### `expYear` ([`Int`](/docs/developer/api-reference/scalars/int))

Four-digit number representing the card’s expiration year.
