---
id: sale
title: Sale
---

Sales allow creating discounts for categories, collections or products and are visible to all the customers.

```graphql
type Sale implements Node, ObjectWithMetadata {
  id: ID!
  name: String!
  type: SaleType!
  startDate: DateTime!
  endDate: DateTime
  privateMetadata: [MetadataItem]!
  metadata: [MetadataItem]!
  categories(
    before: String
    after: String
    first: Int
    last: Int
  ): CategoryCountableConnection
  collections(
    before: String
    after: String
    first: Int
    last: Int
  ): CollectionCountableConnection
  products(
    before: String
    after: String
    first: Int
    last: Int
  ): ProductCountableConnection
  translation(languageCode: LanguageCodeEnum!): SaleTranslation
  channelListings: [SaleChannelListing!]
  discountValue: Float
  currency: String
}
```

### Fields

#### `id` ([`ID!`](/docs/developer/api-reference/scalars/id))

The ID of the object.

#### `name` ([`String!`](/docs/developer/api-reference/scalars/string))

#### `type` ([`SaleType!`](/docs/developer/api-reference/enums/sale-type))

#### `startDate` ([`DateTime!`](/docs/developer/api-reference/scalars/date-time))

#### `endDate` ([`DateTime`](/docs/developer/api-reference/scalars/date-time))

#### `privateMetadata` ([`[MetadataItem]!`](/docs/developer/api-reference/objects/metadata-item))

List of private metadata items.Requires proper staff permissions to access.

#### `metadata` ([`[MetadataItem]!`](/docs/developer/api-reference/objects/metadata-item))

List of public metadata items. Can be accessed without permissions.

#### `categories` ([`CategoryCountableConnection`](/docs/developer/api-reference/objects/category-countable-connection))

List of categories this sale applies to.

- ##### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

- ##### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

- ##### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

- ##### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

#### `collections` ([`CollectionCountableConnection`](/docs/developer/api-reference/objects/collection-countable-connection))

List of collections this sale applies to.

- ##### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

- ##### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

- ##### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

- ##### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

#### `products` ([`ProductCountableConnection`](/docs/developer/api-reference/objects/product-countable-connection))

List of products this sale applies to.

- ##### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

- ##### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

- ##### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

- ##### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

#### `translation` ([`SaleTranslation`](/docs/developer/api-reference/objects/sale-translation))

Returns translated sale fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/docs/developer/api-reference/enums/language-code-enum))

A language code to return the translation for sale.

#### `channelListings` ([`[SaleChannelListing!]`](/docs/developer/api-reference/objects/sale-channel-listing))

List of channels available for the sale.

#### `discountValue` ([`Float`](/docs/developer/api-reference/scalars/float))

Sale value.

#### `currency` ([`String`](/docs/developer/api-reference/scalars/string))

Currency code for sale.

### Interfaces

#### [`Node`](/docs/developer/api-reference/interfaces/node)

An object with an ID

#### [`ObjectWithMetadata`](/docs/developer/api-reference/interfaces/object-with-metadata)
