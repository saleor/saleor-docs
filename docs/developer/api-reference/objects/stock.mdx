---
id: stock
title: Stock
---

Represents stock.

```graphql
type Stock implements Node {
  warehouse: Warehouse!
  productVariant: ProductVariant!
  quantity: Int!
  id: ID!
  quantityAllocated: Int!
}
```

### Fields

#### `warehouse` ([`Warehouse!`](/developer/api-reference/objects/warehouse.mdx))

#### `productVariant` ([`ProductVariant!`](/developer/api-reference/objects/product-variant.mdx))

#### `quantity` ([`Int!`](/developer/api-reference/scalars/int.mdx))

Quantity of a product in the warehouse's possession, including the allocated stock that is waiting for shipment.

#### `id` ([`ID!`](/developer/api-reference/scalars/id.mdx))

The ID of the object.

#### `quantityAllocated` ([`Int!`](/developer/api-reference/scalars/int.mdx))

Quantity allocated for orders

### Interfaces

#### [`Node`](/developer/api-reference/interfaces/node.mdx)

An object with an ID
