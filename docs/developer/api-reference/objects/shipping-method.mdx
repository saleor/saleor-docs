---
id: shipping-method
title: ShippingMethod
---

Shipping method are the methods you'll use to get customer's orders to them. They are directly exposed to the customers.

```graphql
type ShippingMethod implements Node, ObjectWithMetadata {
  id: ID!
  name: String!
  description: JSONString
  minimumOrderWeight: Weight
  maximumOrderWeight: Weight
  maximumDeliveryDays: Int
  minimumDeliveryDays: Int
  privateMetadata: [MetadataItem]!
  metadata: [MetadataItem]!
  type: ShippingMethodTypeEnum
  translation(languageCode: LanguageCodeEnum!): ShippingMethodTranslation
  channelListings: [ShippingMethodChannelListing!]
  price: Money
  maximumOrderPrice: Money
  minimumOrderPrice: Money
  postalCodeRules: [ShippingMethodPostalCodeRule]
  excludedProducts(
    before: String
    after: String
    first: Int
    last: Int
  ): ProductCountableConnection
}
```

### Fields

#### `id` ([`ID!`](/docs/developer/api-reference/scalars/id))

The ID of the object.

#### `name` ([`String!`](/docs/developer/api-reference/scalars/string))

#### `description` ([`JSONString`](/docs/developer/api-reference/scalars/json-string))

#### `minimumOrderWeight` ([`Weight`](/docs/developer/api-reference/objects/weight))

#### `maximumOrderWeight` ([`Weight`](/docs/developer/api-reference/objects/weight))

#### `maximumDeliveryDays` ([`Int`](/docs/developer/api-reference/scalars/int))

#### `minimumDeliveryDays` ([`Int`](/docs/developer/api-reference/scalars/int))

#### `privateMetadata` ([`[MetadataItem]!`](/docs/developer/api-reference/objects/metadata-item))

List of private metadata items.Requires proper staff permissions to access.

#### `metadata` ([`[MetadataItem]!`](/docs/developer/api-reference/objects/metadata-item))

List of public metadata items. Can be accessed without permissions.

#### `type` ([`ShippingMethodTypeEnum`](/docs/developer/api-reference/enums/shipping-method-type-enum))

Type of the shipping method.

#### `translation` ([`ShippingMethodTranslation`](/docs/developer/api-reference/objects/shipping-method-translation))

Returns translated shipping method fields for the given language code.

- ##### `languageCode` ([`LanguageCodeEnum!`](/docs/developer/api-reference/enums/language-code-enum))

A language code to return the translation for shipping method.

#### `channelListings` ([`[ShippingMethodChannelListing!]`](/docs/developer/api-reference/objects/shipping-method-channel-listing))

List of channels available for the method.

#### `price` ([`Money`](/docs/developer/api-reference/objects/money))

The price of the cheapest variant (including discounts).

#### `maximumOrderPrice` ([`Money`](/docs/developer/api-reference/objects/money))

The price of the cheapest variant (including discounts).

#### `minimumOrderPrice` ([`Money`](/docs/developer/api-reference/objects/money))

The price of the cheapest variant (including discounts).

#### `postalCodeRules` ([`[ShippingMethodPostalCodeRule]`](/docs/developer/api-reference/objects/shipping-method-postal-code-rule))

Postal code ranges rule of exclusion or inclusion of the shipping method.

#### `excludedProducts` ([`ProductCountableConnection`](/docs/developer/api-reference/objects/product-countable-connection))

List of excluded products for the shipping method.

- ##### `before` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come before the specified cursor.

- ##### `after` ([`String`](/docs/developer/api-reference/scalars/string))

Return the elements in the list that come after the specified cursor.

- ##### `first` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the first n elements from the list.

- ##### `last` ([`Int`](/docs/developer/api-reference/scalars/int))

Return the last n elements from the list.

### Interfaces

#### [`Node`](/docs/developer/api-reference/interfaces/node)

An object with an ID

#### [`ObjectWithMetadata`](/docs/developer/api-reference/interfaces/object-with-metadata)
