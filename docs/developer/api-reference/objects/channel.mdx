---
id: channel
title: Channel
---

Represents channel.

```graphql
type Channel implements Node {
  id: ID!
  name: String!
  isActive: Boolean!
  slug: String!
  currencyCode: String!
  hasOrders: Boolean!
  defaultCountry: CountryDisplay!
}
```

### Fields

#### `id` ([`ID!`](/docs/developer/api-reference/scalars/id))

The ID of the object.

#### `name` ([`String!`](/docs/developer/api-reference/scalars/string))

#### `isActive` ([`Boolean!`](/docs/developer/api-reference/scalars/boolean))

#### `slug` ([`String!`](/docs/developer/api-reference/scalars/string))

#### `currencyCode` ([`String!`](/docs/developer/api-reference/scalars/string))

#### `hasOrders` ([`Boolean!`](/docs/developer/api-reference/scalars/boolean))

Whether a channel has associated orders.

#### `defaultCountry` ([`CountryDisplay!`](/docs/developer/api-reference/objects/country-display))

New in Saleor 3.1. Default country for the channel. Default country can be used in checkout to determine the stock quantities or calculate taxes when the country was not explicitly provided.

### Interfaces

#### [`Node`](/docs/developer/api-reference/interfaces/node)

An object with an ID
