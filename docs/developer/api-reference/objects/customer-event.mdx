---
id: customer-event
title: CustomerEvent
---

History log of the customer.

```graphql
type CustomerEvent implements Node {
  id: ID!
  date: DateTime
  type: CustomerEventsEnum
  user: User
  app: App
  message: String
  count: Int
  order: Order
  orderLine: OrderLine
}
```

### Fields

#### `id` ([`ID!`](/docs/developer/api-reference/scalars/id))

The ID of the object.

#### `date` ([`DateTime`](/docs/developer/api-reference/scalars/date-time))

Date when event happened at in ISO 8601 format.

#### `type` ([`CustomerEventsEnum`](/docs/developer/api-reference/enums/customer-events-enum))

Customer event type.

#### `user` ([`User`](/docs/developer/api-reference/objects/user))

User who performed the action.

#### `app` ([`App`](/docs/developer/api-reference/objects/app))

App that performed the action.

#### `message` ([`String`](/docs/developer/api-reference/scalars/string))

Content of the event.

#### `count` ([`Int`](/docs/developer/api-reference/scalars/int))

Number of objects concerned by the event.

#### `order` ([`Order`](/docs/developer/api-reference/objects/order))

The concerned order.

#### `orderLine` ([`OrderLine`](/docs/developer/api-reference/objects/order-line))

The concerned order line.

### Interfaces

#### [`Node`](/docs/developer/api-reference/interfaces/node)

An object with an ID
