---
id: product-channel-listing
title: ProductChannelListing
---

Represents product channel listing.

```graphql
type ProductChannelListing implements Node {
  id: ID!
  publicationDate: Date
  isPublished: Boolean!
  channel: Channel!
  visibleInListings: Boolean!
  availableForPurchase: Date
  discountedPrice: Money
  purchaseCost: MoneyRange
  margin: Margin
  isAvailableForPurchase: Boolean
  pricing(address: AddressInput): ProductPricingInfo
}
```

### Fields

#### `id` ([`ID`](../../../developer/api-reference/scalars/id))

#### `publicationDate` ([`Date`](../../../developer/api-reference/scalars/date))

#### `isPublished` ([`Boolean`](../../../developer/api-reference/scalars/boolean))

#### `channel` ([`Channel`](../../../developer/api-reference/objects/channel))

#### `visibleInListings` ([`Boolean`](../../../developer/api-reference/scalars/boolean))

#### `availableForPurchase` ([`Date`](../../../developer/api-reference/scalars/date))

#### `discountedPrice` ([`Money`](../../../developer/api-reference/objects/money))

The price of the cheapest variant (including discounts).

#### `purchaseCost` ([`MoneyRange`](../../../developer/api-reference/objects/money-range))

Purchase cost of product.

#### `margin` ([`Margin`](../../../developer/api-reference/objects/margin))

Range of margin percentage value.

#### `isAvailableForPurchase` ([`Boolean`](../../../developer/api-reference/scalars/boolean))

Whether the product is available for purchase.

#### `pricing` ([`ProductPricingInfo`](../../../developer/api-reference/objects/product-pricing-info))

Lists the storefront product's pricing, the current price and discounts, only meant for displaying.

- ##### `address` ([`AddressInput`](../../../developer/api-reference/inputs/address-input))

Destination address used to find warehouses where stock availability for this product is checked. If address is empty, uses `Shop.companyAddress` or fallbacks to server's `settings.DEFAULT_COUNTRY` configuration.

### Interfaces

#### [`Node`](../../../developer/api-reference/interfaces/node)

An object with an ID
