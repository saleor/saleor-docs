---
id: gift-card
title: GiftCard
---

A gift card is a prepaid electronic payment card accepted in stores. They can be used during checkout by providing a valid gift card codes.

```graphql
type GiftCard implements Node, ObjectWithMetadata {
  code: String!
  isActive: Boolean!
  expiryDate: Date
  expiryType: GiftCardExpiryTypeEnum!
  tag: String
  created: DateTime!
  lastUsedOn: DateTime
  initialBalance: Money
  currentBalance: Money
  id: ID!
  privateMetadata: [MetadataItem]!
  metadata: [MetadataItem]!
  displayCode: String!
  createdBy: User
  usedBy: User
  createdByEmail: String
  usedByEmail: String
  app: App
  expiryPeriod: TimePeriod
  product: Product
  events: [GiftCardEvent!]!
  user: User
  endDate: DateTime
  startDate: DateTime
}
```

### Fields

#### `code` ([`String!`](/developer/api-reference/scalars/string.mdx))

Gift card code. Can be fetched by staff member with manage gift card permission when gift card wasn't used yet and by the gift card owner.

#### `isActive` ([`Boolean!`](/developer/api-reference/scalars/boolean.mdx))

#### `expiryDate` ([`Date`](/developer/api-reference/scalars/date.mdx))

New in Saleor 3.1. The gift card expiry date.

#### `expiryType` ([`GiftCardExpiryTypeEnum!`](/developer/api-reference/enums/gift-card-expiry-type-enum.mdx))

New in Saleor 3.1. The gift card expiry type.

#### `tag` ([`String`](/developer/api-reference/scalars/string.mdx))

New in Saleor 3.1. The gift card tag.

#### `created` ([`DateTime!`](/developer/api-reference/scalars/date-time.mdx))

#### `lastUsedOn` ([`DateTime`](/developer/api-reference/scalars/date-time.mdx))

#### `initialBalance` ([`Money`](/developer/api-reference/objects/money.mdx))

#### `currentBalance` ([`Money`](/developer/api-reference/objects/money.mdx))

#### `id` ([`ID!`](/developer/api-reference/scalars/id.mdx))

The ID of the object.

#### `privateMetadata` ([`[MetadataItem]!`](/developer/api-reference/objects/metadata-item.mdx))

List of private metadata items.Requires proper staff permissions to access.

#### `metadata` ([`[MetadataItem]!`](/developer/api-reference/objects/metadata-item.mdx))

List of public metadata items. Can be accessed without permissions.

#### `displayCode` ([`String!`](/developer/api-reference/scalars/string.mdx))

Code in format which allows displaying in a user interface.

#### `createdBy` ([`User`](/developer/api-reference/objects/user.mdx))

New in Saleor 3.1. The user who bought or issued a gift card.

#### `usedBy` ([`User`](/developer/api-reference/objects/user.mdx))

New in Saleor 3.1. The customer who used a gift card.

#### `createdByEmail` ([`String`](/developer/api-reference/scalars/string.mdx))

New in Saleor 3.1. Email address of the user who bought or issued gift card.

#### `usedByEmail` ([`String`](/developer/api-reference/scalars/string.mdx))

New in Saleor 3.1. Email address of the customer who used a gift card.

#### `app` ([`App`](/developer/api-reference/objects/app.mdx))

New in Saleor 3.1. App which created the gift card.

#### `expiryPeriod` ([`TimePeriod`](/developer/api-reference/objects/time-period.mdx))

New in Saleor 3.1. The gift card expiry period.

#### `product` ([`Product`](/developer/api-reference/objects/product.mdx))

New in Saleor 3.1. Related gift card product.

#### `events` ([`[GiftCardEvent!]!`](/developer/api-reference/objects/gift-card-event.mdx))

New in Saleor 3.1. List of events associated with the gift card.

#### `user` ([`User`](/developer/api-reference/objects/user.mdx))

<sub><sup><span className="deprecatedTag">DEPRECATED</span> This field will be removed in Saleor 4.0. Use `createdBy` field instead.</sup></sub>

The customer who bought a gift card.

#### `endDate` ([`DateTime`](/developer/api-reference/scalars/date-time.mdx))

<sub><sup><span className="deprecatedTag">DEPRECATED</span> This field will be removed in Saleor 4.0. Use `expiryDate` field instead.</sup></sub>

End date of gift card.

#### `startDate` ([`DateTime`](/developer/api-reference/scalars/date-time.mdx))

<sub><sup><span className="deprecatedTag">DEPRECATED</span> This field will be removed in Saleor 4.0.</sup></sub>

Start date of gift card.

### Interfaces

#### [`Node`](/developer/api-reference/interfaces/node.mdx)

An object with an ID

#### [`ObjectWithMetadata`](/developer/api-reference/interfaces/object-with-metadata.mdx)
