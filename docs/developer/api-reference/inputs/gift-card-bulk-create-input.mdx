---
id: gift-card-bulk-create-input
title: GiftCardBulkCreateInput
---

No description

```graphql
type GiftCardBulkCreateInput {
  count: Int!
  balance: PriceInput!
  tag: String!
  expiryDate: Date
  isActive: Boolean!
}
```

### Fields

#### `count` ([`Int!`](../../../developer/api-reference/scalars/int))

The number of cards to issue.

#### `balance` ([`PriceInput!`](../../../developer/api-reference/inputs/price-input))

Balance of the gift card.

#### `tag` ([`String!`](../../../developer/api-reference/scalars/string))

The gift card tag.

#### `expiryDate` ([`Date`](../../../developer/api-reference/scalars/date))

The gift card expiry date.

#### `isActive` ([`Boolean!`](../../../developer/api-reference/scalars/boolean))

Determine if gift card is active.
