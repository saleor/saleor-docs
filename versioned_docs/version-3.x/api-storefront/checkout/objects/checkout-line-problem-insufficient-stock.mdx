---
id: checkout-line-problem-insufficient-stock
title: CheckoutLineProblemInsufficientStock
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Indicates insufficient stock for a given checkout line.Placing the order will not be possible until solving this problem.

<Badge text="Added in Saleor 3.15" class="secondary margin-bottom--sm" />

<FeaturePreview />

```graphql
type CheckoutLineProblemInsufficientStock {
  availableQuantity: Int
  line: CheckoutLine!
  variant: ProductVariant!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>CheckoutLineProblemInsufficientStock.<b>availableQuantity</b></code>](#)<Bullet />[`Int`](../../../api-storefront/miscellaneous/scalars/int) <Badge class="badge badge--secondary" text="scalar"/>

> Available quantity of a variant.

#### [<code style={{ fontWeight: 'normal' }}>CheckoutLineProblemInsufficientStock.<b>line</b></code>](#)<Bullet />[`CheckoutLine!`](../../../api-storefront/checkout/objects/checkout-line) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

> The line that has variant with insufficient stock.

#### [<code style={{ fontWeight: 'normal' }}>CheckoutLineProblemInsufficientStock.<b>variant</b></code>](#)<Bullet />[`ProductVariant!`](../../../api-storefront/products/objects/product-variant) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

> The variant with insufficient stock.

### Implemented by

[`CheckoutLineProblem`](../../../api-storefront/miscellaneous/unions/checkout-line-problem) <Badge class="badge badge--secondary" text="union"/><Bullet />[`CheckoutProblem`](../../../api-storefront/miscellaneous/unions/checkout-problem) <Badge class="badge badge--secondary" text="union"/>

import FeaturePreview from "@site/components/FeaturePreview";
