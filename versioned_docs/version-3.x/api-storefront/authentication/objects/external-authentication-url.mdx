---
id: external-authentication-url
title: ExternalAuthenticationUrl
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Prepare external authentication URL for user by custom plugin.

```graphql
type ExternalAuthenticationUrl {
  authenticationData: JSONString
  accountErrors: [AccountError!]! @deprecated
  errors: [AccountError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ExternalAuthenticationUrl.<b>authenticationData</b></code>](#)<Bullet />[`JSONString`](../../../api-storefront/miscellaneous/scalars/jsonstring) <Badge class="badge badge--secondary" text="scalar"/>

> The data returned by authentication plugin.

#### [<code style={{ fontWeight: 'normal' }}>ExternalAuthenticationUrl.<b>accountErrors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-storefront/users/objects/account-error) <Badge class="badge badge--deprecated badge--secondary" text="deprecated"/> <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

> :::caution DEPRECATED
> This field will be removed in Saleor 4.0. Use `errors` field instead.
> :::

#### [<code style={{ fontWeight: 'normal' }}>ExternalAuthenticationUrl.<b>errors</b></code>](#)<Bullet />[`[AccountError!]!`](../../../api-storefront/users/objects/account-error) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

>

### Returned by

[`externalAuthenticationUrl`](../../../api-storefront/authentication/mutations/external-authentication-url) <Badge class="badge badge--secondary" text="mutation"/>
