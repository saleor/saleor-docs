---
id: app-extension-target-enum
title: AppExtensionTargetEnum
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

All available ways of opening an app extension.

    POPUP - app's extension will be mounted as a popup window
    APP_PAGE - redirect to app's page

```graphql
enum AppExtensionTargetEnum {
  POPUP
  APP_PAGE
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>AppExtensionTargetEnum.<b>POPUP</b></code>](#)

>

#### [<code style={{ fontWeight: 'normal' }}>AppExtensionTargetEnum.<b>APP_PAGE</b></code>](#)

>

### Member of

[`AppExtension`](../../../api-reference/apps/objects/app-extension) <Badge class="badge badge--secondary" text="object"/><Bullet />[`AppExtensionFilterInput`](../../../api-reference/apps/inputs/app-extension-filter-input) <Badge class="badge badge--secondary" text="input"/><Bullet />[`AppManifestExtension`](../../../api-reference/apps/objects/app-manifest-extension) <Badge class="badge badge--secondary" text="object"/>
