---
id: attribute-value-translate
title: attributeValueTranslate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Creates/updates translations for an attribute value.

<Permissions
  permissions={"MANAGE_TRANSLATIONS"}
  text={"Requires one of the following permissions"}
/>

```graphql
attributeValueTranslate(
  id: ID!
  input: AttributeValueTranslationInput!
  languageCode: LanguageCodeEnum!
): AttributeValueTranslate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>attributeValueTranslate.<b>id</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/>

> AttributeValue ID or AttributeValueTranslatableContent ID.

#### [<code style={{ fontWeight: 'normal' }}>attributeValueTranslate.<b>input</b></code>](#)<Bullet />[`AttributeValueTranslationInput!`](../../../api-reference/miscellaneous/inputs/attribute-value-translation-input) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/>

>

#### [<code style={{ fontWeight: 'normal' }}>attributeValueTranslate.<b>languageCode</b></code>](#)<Bullet />[`LanguageCodeEnum!`](../../../api-reference/miscellaneous/enums/language-code-enum) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="enum"/>

> Translation language code.

### Type

#### [`AttributeValueTranslate`](../../../api-reference/attributes/objects/attribute-value-translate) <Badge class="badge badge--secondary" text="object"/>

> Creates/updates translations for an attribute value.

<Permissions
  permissions={"MANAGE_TRANSLATIONS"}
  text={"Requires one of the following permissions"}
/>

import Permissions from "@site/components/Permissions";
