---
id: order-add-note
title: orderAddNote
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

:::caution DEPRECATED
This field will be removed in Saleor 4.0. Use `orderNoteAdd` instead.
:::

Adds note to the order.

DEPRECATED: this mutation will be removed in Saleor 4.0.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

```graphql
orderAddNote(
  order: ID!
  input: OrderAddNoteInput!
): OrderAddNote @deprecated
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>orderAddNote.<b>order</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/>

> ID of the order to add a note for.

#### [<code style={{ fontWeight: 'normal' }}>orderAddNote.<b>input</b></code>](#)<Bullet />[`OrderAddNoteInput!`](../../../api-reference/orders/inputs/order-add-note-input) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/>

> Fields required to create a note for the order.

### Type

#### [`OrderAddNote`](../../../api-reference/orders/objects/order-add-note) <Badge class="badge badge--secondary" text="object"/>

> Adds note to the order.
>
> DEPRECATED: this mutation will be removed in Saleor 4.0.

<Permissions
  permissions={"MANAGE_ORDERS"}
  text={"Requires one of the following permissions"}
/>

import Permissions from "@site/components/Permissions";
