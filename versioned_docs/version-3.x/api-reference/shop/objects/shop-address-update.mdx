---
id: shop-address-update
title: ShopAddressUpdate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Update the shop's address. If the `null` value is passed, the currently selected address will be deleted.

<Permissions
  permissions={"MANAGE_SETTINGS"}
  text={"Requires one of the following permissions"}
/>

```graphql
type ShopAddressUpdate {
  shop: Shop
  shopErrors: [ShopError!]! @deprecated
  errors: [ShopError!]!
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ShopAddressUpdate.<b>shop</b></code>](#)<Bullet />[`Shop`](../../../api-reference/miscellaneous/objects/shop) <Badge class="badge badge--secondary" text="object"/>

> Updated shop.

#### [<code style={{ fontWeight: 'normal' }}>ShopAddressUpdate.<b>shopErrors</b></code>](#)<Bullet />[`[ShopError!]!`](../../../api-reference/shop/objects/shop-error) <Badge class="badge badge--deprecated badge--secondary" text="deprecated"/> <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

> :::caution DEPRECATED
> This field will be removed in Saleor 4.0. Use `errors` field instead.
> :::

#### [<code style={{ fontWeight: 'normal' }}>ShopAddressUpdate.<b>errors</b></code>](#)<Bullet />[`[ShopError!]!`](../../../api-reference/shop/objects/shop-error) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="object"/>

>

### Returned by

[`shopAddressUpdate`](../../../api-reference/shop/mutations/shop-address-update) <Badge class="badge badge--secondary" text="mutation"/>

import Permissions from "@site/components/Permissions";
