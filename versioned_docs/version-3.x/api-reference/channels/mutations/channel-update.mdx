---
id: channel-update
title: channelUpdate
hide_table_of_contents: false
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="\_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Update a channel.

<Permissions
  permissions={"MANAGE_CHANNELS"}
  text={"Requires one of the following permissions"}
/>

<Permissions
  permissions={"MANAGE_CHANNELS,MANAGE_ORDERS"}
  text={
    "Requires one of the following permissions when updating only orderSettings field"
  }
/>

```graphql
channelUpdate(
  id: ID!
  input: ChannelUpdateInput!
): ChannelUpdate
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>channelUpdate.<b>id</b></code>](#)<Bullet />[`ID!`](../../../api-reference/miscellaneous/scalars/id) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="scalar"/>

> ID of a channel to update.

#### [<code style={{ fontWeight: 'normal' }}>channelUpdate.<b>input</b></code>](#)<Bullet />[`ChannelUpdateInput!`](../../../api-reference/channels/inputs/channel-update-input) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/>

> Fields required to update a channel.

### Type

#### [`ChannelUpdate`](../../../api-reference/channels/objects/channel-update) <Badge class="badge badge--secondary" text="object"/>

> Update a channel.

<Permissions
  permissions={"MANAGE_CHANNELS"}
  text={"Requires one of the following permissions"}
/>

>

<Permissions
  permissions={"MANAGE_CHANNELS,MANAGE_ORDERS"}
  text={
    "Requires one of the following permissions when updating only orderSettings field"
  }
/>

import Permissions from "@site/components/Permissions";
